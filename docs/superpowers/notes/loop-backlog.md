# Production-polish goal — backlog & tracker

Durable state for the impeccable production-polish **goal** (native `/goal` harness since 2026-06-10; originally a `/loop` — filename kept for stable references). Companion to [2026-06-09-impeccable-design-loop.md](2026-06-09-impeccable-design-loop.md) (the command catalog, law checklist, severity machinery, per-surface map). Re-read both at the start of a session, then run iterations **BACK-TO-BACK within the turn**: after an item is committed and tracker-marked, immediately pick the next highest-severity open item and continue — do **NOT** end the turn between iterations. End the turn only to AskUserQuestion on a genuine product/scope decision, when hard-blocked, or when the GOLD finish table has been surfaced. _(Debugged with Jonathan 2026-06-10: the earlier "one iteration per turn" phrasing — a holdover from the /loop wake model — made the agent stop after every single item; that pacing rule is dead.)_

## goal-config (resolved with Jonathan 2026-06-09 — binding)

| Key | Value |
|---|---|
| **responsive_scope** | Desktop-first, **honest to ~1024px**. Fix `xl:`→`lg:` rail (Focus+Builder) + stack Loupe grid below a documented min-width. Sub-tablet (<~768px) out of scope; `<600px` scoping foot-row wrap item **dropped**. |
| **duplicate_export** | **Export-figure surfaced now** (P1, presentation/placement). **Duplicate-series deferred** out of this goal (separate `shape`-gated feature; no backend duplicate path today). |
| **finish_bar** | **GOLD / flagship.** Every surface **critique ≥ 36/40 AND audit ≥ 18/20** AND all P0/P1 closed. Genuinely-infeasible flagship items → mark `blocked`, surface, do not force. |
| **ordering_vars** | Scoping "Ordered by" dropdown is **data-driven** from the real manifest/adapter variable set + a "define your own" affordance. Verify against backend before wiring; do not hardcode the mockup's time/dose/temperature set. |
| **branch** | `worktree-greenfield-ui-rebuild` — STAYS UNMERGED. Never merge, never `finishing-a-development-branch`, never `git add -A`, every commit ends with the mandated co-author line. |

## Score trend (per surface — Wave 1 baseline 2026-06-09)

| Surface | critique /40 | audit /20 | trend | target | gap |
|---|---|---|---|---|---|
| SamplesPage (contact sheet) | **36** ✅ | **18** ✅ | 31 → 33 → 31 → 35 → 35 → **36** | ≥36 / ≥18 | **★ GOLD — re-score #6 (2026-06-12): 36/40 · 18/20, BOTH axes clear, zero P0/P1. THIRD surface to GOLD** (after Loupe + Builder). Evaluator: roving grid "textbook" (one tab stop, Enter/F2 interaction, clean Tab-exit, NO keyboard trap), sort "a permalink", all informational text AA (5.99–14.9:1). Path: re-score #4 35/17 → [[SA-SORT]] (`9be1d6c`) → [[SA-ROVING]] full APG grid (`f745ed9`, ~1105→1 tab stop, 5 focus bugs caught live) → re-score #5 35/18 (2 P1) → #5 fixes `5fb5115` (checkbox 13→≥24px target · "N dropped" caption 4.40→6.00:1 ink-soft · interaction-Enter→Frame-1 regression · KbLegend gains ↑↓←→/Enter discoverability) → re-score #6 GOLD. Residual NON-blocking: [[SA-VIRTUAL]] P2 (no row virtualization — 678 thumbs mount; fine at 139, scales badly; evaluator's "highest-leverage" + prereq for aria-rowcount), [[SA-CULLHINT]] P2 (CullBar X/K glyph hint 2.69:1 — mitigated: button labels pass + legend restates), interaction-mode-doc P2, empty-view-honesty P2 (intro+legend render on the unknown-beamtime EmptyState), aria-rowcount/colindex P3. Snapshots `…rescore5.md` / `…rescore6.md` |
| SeriesFolioPage | **37** ✅ | **18** ✅ | 30 → 31 → 31 → 33 → 33 → 37 → **37** | ≥36 / ≥18 | **★ GOLD — re-score #7 (2026-06-12): 37/40 · 18/20, BOTH axes clear, zero P0/P1. FOURTH surface to GOLD** (after Loupe + Builder + Samples). Path: #6 33/18 → **flexibility + layout pass `40011a0`** (broadened search haystack [+author/phases/ordering-var], sort-direction toggle w/ aria-pressed + absent-default `&dir=` URL codec, `role=status` live count, NewSeriesTile ghost → /series/new, per-card retryFigure, Gallery column-cap to card count, filter-chip `title` tooltips) → re-score #7 GOLD which flagged two a11y nits I introduced → **a11y follow-ups `36f542d`**: FIX 1 ghost-tile subtext `text-ink-faint`→`text-ink-soft` (3.29→**6.76:1** canvas-measured, the AA-normal failure the ghost tile introduced) · FIX 2 filter-chip `description`→`aria-describedby` sr-only span (keyboard/SR-reachable explanation; name stays the label) — both render-verified live at :5182. Residual NON-blocking: sort-dir 44px overlay near-miss at 1120 (→ [[UI-ICONOVERLAY]] family) · "Has transition" vs intra-member coexistence copy nuance P3. Snapshots `…seriesfoliopage-rescore*` |
| SeriesScopingPage | **37** ✅ | **18** ✅ | 28 → 31 → 26 → 30 → 31 → 33 → **37** | ≥36 / ≥18 | **★ GOLD — re-score #7 (2026-06-12): 37/40 · 18/20, BOTH axes clear, zero P0/P1/P2. SIXTH and FINAL surface to GOLD.** Path: #6 33/18 → **density pass `cac7438`** (5 items): (1) [controls-don't-lie] count caption honest after reorder — derived `isCanonicalOrder` swaps "low to high"→"custom order" (reverts on undo); (2) [asymmetric-freedom] reorder undoable, parity with skip — `HistoryEntry` union gains a `reorder` member, `applyReorder` records prevOrder, `undo` restores it; **the impure-updater hazard (setHistory nested in the setOrder updater → StrictMode double-push) was caught by frontend-reviewer and fixed (change-detection hoisted out), render-verified one reorder = one history entry**; (3) [shared primitive] ordering dropdown real selected-state — `Menu` renders `role=menuitemradio`+`aria-checked` when `activeValue!==undefined` (value-selectors only; action menus stay menuitem, zero blast radius), menu named "Ordered by"; (4) grip 25×25 (asymmetric `px-2/-mx-2`, glyph-neutral) + skip min-h 24 (WCAG 2.5.8); (5) "▸ skipped" caption 9px→10.5px + grip arrow-key title hint. All render-verified live; frontend-reviewer APPROVE-WITH-NITS (nit fixed). Residual NON-blocking P3 → **SC-ROVING** (no roving-tabindex grid — the SA-ROVING pattern could extend here; ~12 tab stops on a 4-row worksheet) + candidate accent-key contrast 4.96 thin-margin at 10.5px. Snapshots `…seriesscopingpage-rescore6/7` |
| SeriesBuilderPage | **37** ✅ | **18** ✅ | 27 → 32 → 22 → 29 → **37** | ≥36 / ≥18 | **★ GOLD — re-score #5 (2026-06-12): 37/40 · 18/20, BOTH axes clear; zero P0/P1.** Fresh evaluator validated the focus-retention dance (WCAG 2.4.3 pass, `b68d811`) + read-mode honesty + inert-ordering-Field truthfulness; zero console errors, no AI slop. **Second surface to GOLD-done** (after Loupe). Remaining = 2 P2 + 4 P3, all non-blocking. **NB the evaluator's "sub-24px icon target" finding was VERIFIED in-browser and re-triaged: WCAG 2.5.8 spacing exception IS met (~27px center-to-center → 24px circles don't intersect), so NOT a strict AA failure / NOT P1 — my earlier P1 escalation was wrong. The real sub-issue is overlay cannibalization → logged cross-cutting [[UI-ICONOVERLAY]] P2.** Snapshot `2026-06-12T00-56-04Z__seriesbuilderpage-rescore.md` |
| FocusPage | **37** ✅ | **18** ✅ | 24 → 28 → 31 → 34 → **37** | ≥36 / ≥18 | **★ GOLD — re-score #4 (2026-06-12): 37/40 · 18/20, BOTH axes clear, zero P0/P1. FIFTH surface to GOLD** (after Loupe + Builder + Samples + Folio). Path: #3 34/18 → **density pass `f037f94`** (5 items): (1) keyboard focus re-anchor after a peak edit — new pure `plot/peakFocusOrder.ts:nextFocusPeakId` (q-next survivor, fallback q-previous, "+ Peak" button when none) consumed via a nonce-guarded `PlotPeaks` useLayoutEffect (RepresentativeBox/RecipeRow discipline; exclude issues no request) — WCAG 2.4.3, render-verified live on TWO branches (remove mid-list→q-next survivor; remove highest-q→previous; NOT `<body>`); (2) trace `<svg>` accessible name `role=img "Integration trace: intensity vs q"` threaded via PlotFrame svgRole/svgLabel — WCAG 1.1.1, symmetrical with detector/comb; (3) 1120–1180 toolbar whole-chip wrap (Button/SegmentedControl `whitespace-nowrap` + ToolBar `flex-wrap justify-end`) — even single-line row verified 1120/1000; (4) sample stepper ALREADY 44px IconButton overlay (verified, no change); (5) disarmed cue "Arm + Peak to edit peaks." (ink-soft 6.76:1, disarmed-only). frontend-reviewer APPROVE (shared-primitive blast radius + focus mechanism + house rules clean). Residual NON-blocking → **FO-FOCUSRETURN** P2 (CustomIndexModal close drops focus to body, no returnFocusRef to the "+ custom index" trigger) + Escape-disarm-focus-return P3 (both close with one returnFocusRef pattern) + no-explicit-undo P3 (L4-gated). Snapshots `…focuspage-rescore3/4` |
| LoupePage | **38** ✅ | **19** ✅ | 30→32→35→33→37→**38** | ≥36 / ≥18 | **★ GOLD — re-score #6: 38/40 · 19/20, BOTH axes clear.** First surface to GOLD. Cycle fixes: Esc-eject `32f777d`, +tag-AA+h1+modal-layout `b7cbc7c`, kicker-AA `88ef0fc`, h2-hierarchy `<this>` (rail h3→h2, no skipped level — re-score #6's lone P1, render-verified H1→H2→H2→H2). All text AA w/ margin (kickers + `+tag` 6.49:1). Residual NON-blocking craft → LO-DROPFOOTER P2 (add-row dropdown overlaps footer ~7px), LO-CANVASALT P3 (detector alt repeats), LO-DBLESC P3 (2nd Esc from a combobox), LO-EXPSPARSE P2, LO-MOBILE/touch P3, LO-TAGPROV P3(deferred). **Loupe DONE for GOLD; P1s all closed** |

**Scoping re-score #3 (2026-06-10, post SC-FLAG/SC-PARTIAL/SC-FOLD/SC-KBD): 31→26 / 17→15.** NOT a regression in landed work — the evaluator PRAISED the keyboard reorder, skip model, chain banner, and foot honesty — but the first evaluator to exercise the seeded-untagged flow (contact-sheet select of samples lacking the corpus's dominant key) and found an unrecoverable dead-end (SC-SEEDDEAD, new P1 ×2 same root cause) + an honesty cluster (preview ignores skips; duplicate-name candidates). Lesson recorded: a surface's score is bounded by its worst REACHABLE state, and each fresh evaluator probes deeper. Snapshot `2026-06-10T06-45-04Z__*seriesscopingpage*`. Re-scores (same single-evaluator method, fresh/unanchored): Scoping + Folio 2026-06-10 after their Wave-2 P1s — both rose, both still under GOLD; each re-score surfaced new P1s (SC-FLAG, SC-PARTIAL, UI-STRIP), merged below. Builder re-scored 2026-06-10 after all four Builder P1s (BU-RAIL/BU-DEAD/BU-FIELD/BU-EXPORT): 27→32 / 16→17, the largest rise so far; one new P1 (BU-WYSIWYG) + three P2s merged below (snapshot `2026-06-10T04-37-09Z__seriesbuilderpage-rescore.md`). All six below GOLD on both axes. No P0s anywhere. Baseline reads: theming + anti-patterns are 4/4 almost everywhere (design-guard working); **a11y is the universal drag** (a11y dim 2–3 on every surface). Snapshots persisted under `.impeccable/critique/` (impeccable-native trend; re-score in Wave 4 to show the rise). Slugs: SamplesPage=`imalayaui-…-samplespage-tsx`, Folio=`ayaui-…-seriesfoliopage-tsx`, Scoping=`aui-…-seriesscopingpage-tsx`, Builder=`aui-…-seriesbuilderpage-tsx`, Focus=`himalayaui-…-focuspage-tsx`, Loupe=`himalayaui-…-loupepage-tsx`.

**Note on faithfulness:** Wave-1 critiques ran a single capable evaluation agent per surface applying the real `critique.md`+`audit.md` rubrics over source + rendered screenshots (desktop 1440 + laptop 1120 for the rail/grid surfaces) + the SAXS `ignore.md`, rather than the two-fully-isolated-subagent design/detect split. The deterministic detector (`impeccable detect --json`) was run centrally on every page source and returned `[]` (zero anti-pattern tells) — that is the Assessment-B half, and it corroborates the 4/4 anti-patterns scores. Re-scores in Wave 4 use the same method for trend comparability.

## Wave status

- [x] **Wave 0 — Foundation refresh** — DONE (`c525c93`). DESIGN.md re-rooted to `src/print/ui`, stale two-theme/grain promises killed, four new sections (State taxonomy / Spacing & density / Motion / Copy & UX writing); PRODUCT.md a11y de-dark-themed + named persona seeded.
- [x] **Wave 1 — Scored baseline** — DONE (2026-06-09). SAXS `ignore.md` seeded; 6 surfaces rendered + scored (critique /40 + audit /20); snapshots persisted; new P1s merged below. No P0s.
- [ ] **Wave 2 — P1 fixes top-down** ← IN PROGRESS. ✅ F-LIVE (`c07cdce`). ✅ F-ERRSILENT (closed-by-architecture). ✅ F-CONTRAST (`275c421`). ✅ rail-adapt (FO-RAIL+BU-RAIL `6cee2a6`; FO-COMB closed-already-honest). **All cross-cutting P1s closed** + ✅ SC-HEAD (`32a11a4`) + ✅ F-A11Y contrast half (`046b440`, token-contrast regression guard). **Cross-cutting P1 work essentially complete** (F-A11Y's keyboard/aria half lives in the surface KEYD items). Surface-specific P1s: ✅ SA-SEM (`fa3eacc`) + ✅ FOL-KBD (`a100c2e`) + ✅ SC-FIELD (`21777a0`, order-Field accessible name) + ✅ SC-ORDER (`20a7b2b` + `99e1a8d`, data-driven dropdown + "Define your own…" inline-convert). **HARNESS = `/goal`** (switched from `/loop` mid-Wave-2; continuous evaluator-checked, no ScheduleWakeup). + ✅ UI-STRIP (`c0e0d80`, PhaseStrip role=img) + ✅ BU-FIELD (`65fbd64`) + ✅ BU-DEAD (`c9e7cd6`) + ✅ BU-EXPORT (`4d699f0`, ExportButton split on the plate head — **ALL Builder P1s closed**). + ✅ BU-WYSIWYG honest-state half (`14c6337`) + ✅ SC-PARTIAL (`efbe79a`, truthful chain-failure banner) + ✅ SC-FLAG (`7442b6f`, skip vocabulary + aria-pressed + constant accname) + ✅ SC-FOLD (`b6a9af7`, candidate preview cap + honest remainder note + fan-out cap) + ✅ SC-KBD (`0b5b1e9`, keyboard reorder — closed the original scoping P1 set; re-score #3 then surfaced SC-SEEDDEAD). + ✅ SC-SEEDDEAD (`27ef097`, seed-scoped vocabulary — scoping P1s closed AGAIN) + ✅ LO-KEYD (`ea73229`, shared `suppressGlobalKeys` guard across all 5 window keydown sites — also closes SA-KEYD's guard half; **ALL LOUPE P1s closed → Loupe re-score due**). + ✅ SA-KEYD (guard via `ea73229`; Enter half re-triaged to SA-THUMBKEY P2 — loupe-open was already keyboard-reachable via the SpecCell name button, pre-baseline `2806597`) + ✅ SA-RESP (`4f1809a`, shared scroller + sticky identity columns — **ALL SAMPLES P1s closed → Samples re-score due**). + ✅ F-STATE enforcement (`2edcf6c`, one base-layer focus-visible rule + 7 offset-0 redeclarations — **Wave 2 complete; every original P1 closed**). Loupe re-score #1 then surfaced **LO-REPDROP (P1, open)** — the re-score waterfall will keep minting these; close each before that surface's next re-score. Open lower: SC-COLD/SC-POLISH2/EM-DASH/UI-MENUKBD/FOL-EMPTY2/FA-SUNK/BU-PICKER/BU-ORDERINERT/BU-NAMES (P2), SC-CUSTSSE/BU-POLISH3 (P3). **Re-scores done 2026-06-10: Scoping 28→31/17, Folio 30→31/17, Builder 27→32/17** (all rose, all under GOLD; the gap is now P2-density + the new P1s, not the original drags). Samples re-score after SA-KEYD+SA-RESP.

> **⚠ 2026-06-12 (later) — GOLD-confirmation audit reopened one P1.** A Jonathan-directed critical re-review of all surfaces + interactions confirmed the per-surface *visual* GOLD holds, but the **interaction/data-model pass found a tag-duplication cluster the surface scoring missed** — now logged in the "Jonathan-directed GOLD-confirmation audit (2026-06-12)" section below. **[[TAG-DEDUP-MODEL]] is a new open P1** (every sample shows two identical tag chips per key), so the "zero open P0/P1" claim in the marker below is **no longer current**. Also new: BU-AUTOGROUP-STALE / BU-TITLE-FIELD / BU-TRACE-DIM (the three Series-Builder defects Jonathan flagged by name) + TAG-MANAGE-PROV / TAG-ERR-LAYOUT / TAG-MODAL-ZORDER. Review-only — nothing fixed yet.
>
> **⚠ 2026-06-12 (later still) — broad all-surface round 2 reopened a SECOND P1.** A full interaction sweep of all six surfaces (parallel source reviews + live verification) added the "Broad review round 2 (2026-06-12)" table below. **[[SA-STALESELECT]] is a new open P1** (selection survives a beamtime-filter change → cull toast over-counts a destructive action / stale "+ New series" carry; verified live) plus 10 P2/P3 interaction/honesty items (LO-STALEACTIVEID, FO-PARAM-VALIDATION, SC-TAGORDER, SC-PROVREAD, BU-EMPTYREMOVE [verified live — a draft empties to 0 members and Confirm still offers to publish], BU-NAVAWAY-DRAFT, et al.). **Two open P1s total now (TAG-DEDUP-MODEL + SA-STALESELECT).** Still review-only — nothing fixed.
>
> **▶ 2026-06-12 — FIXING began. BOTH audit P1s now CLOSED — zero open P0/P1 again.** (1) [[TAG-DEDUP-MODEL]] `34a1aa5` (+ TAG-MANAGE-PROV resolved-by-dedup): option (a) DB-level `UNIQUE(sample_id,key)` + heal migration (oldest wins) + add-tag 409; live-verified one chip per key; full Julia suite green. (2) [[SA-STALESELECT]] `2553aa8`: clear both selection grains on a beamtime change + toast counts only in-scope mutations; live-verified ComposeBar 1→0 on filter change; new Vitest; build + full FE suite (2770) green. **Remaining audit/round-2 work is all P2/P3.** The three Series-Builder defects Jonathan flagged by name are now **all done**: BU-AUTOGROUP-STALE `e4d8ca5`, BU-TRACE-DIM `82d04a1`, BU-TITLE-FIELD `50318f1` (each live-verified on series/2; build incl. lint:design + full FE suite 2770 green). **ALL source-logged P2s are now CLOSED.** **ALL discrete source/live-logged P3s are now CLOSED too.** The only remaining backlog item is the pre-existing "Next sprint P2 themes" table (8 thematic groups, not yet itemized into discrete fixes). **P3 done this pass:** TAG-MODAL-ZORDER `beb6f0b`, FO-ALLEXCLUDED-CAPTION `d9bd035`, BU-EXPORT-EMPTYALLOWED `59388a6`, FOL-ALLCHIP-TOOLTIP `90dd270`, LO-LASTFRAME-NOGUARD `283d70d`, SA-ZEROEXP `aaff911`. **Done this pass:** TAG-ERR-LAYOUT `f109d81`, BU-EMPTYREMOVE `fb23a3b`, FO-PARAM-VALIDATION `599e4db`, SC-TAGORDER `74fe8ed`, SC-PROVREAD `0d28ba6`, LO-STALEACTIVEID `2c92ca9`, BU-NAVAWAY-DRAFT `c112e06`.
>
> **🎯 2026-06-12 — ALL SIX SURFACES AT GOLD. GOAL COMPLETE (finish bar met).** Every surface clears **critique ≥36/40 AND audit ≥18/20 with zero open P0/P1**: Loupe **38/19** · Builder **37/18** · Samples **36/18** · Folio **37/18** · Focus **37/18** · Scoping **37/18**. Gate green at the final commit `cac7438`: lint:design · vitest 2769 (272 files) · e2e 51/3 skipped · build. This turn closed the last THREE surfaces — Samples (`f745ed9` SA-ROVING grid + `5fb5115`), Folio (`40011a0`+`36f542d`), Focus (`f037f94`), Scoping (`cac7438`) — each re-score-baseline-first → density-pass → render-verify-live → frontend-reviewer → re-score-to-confirm. **Branch STAYS UNMERGED** (no merge, no finishing-a-development-branch — awaiting Jonathan). Residuals are ALL non-blocking P3 craft (FO-FOCUSRETURN P2 is the lone P2, plus SC-ROVING, candidate-key contrast, UI-ICONOVERLAY P2 owner-decided-shrink, the various *-RESCORE*-P3 clusters) — optional polish, tracked below, not GOLD blockers. The per-surface finish table is the goal's DONE proof.
>
> **▶ 2026-06-12 — FIVE surfaces at GOLD; ONE remains.** This turn took **Samples** (#6 **36/18**, 3rd), **Folio** (#7 **37/18**, 4th), and **Focus** (#4 **37/18**, 5th) to GOLD. Samples via the flagship [[SA-ROVING]] APG roving grid (`f745ed9`, ~1105→1 tab stop; 5 focus bugs caught ONLY by live render-verify) + `5fb5115`. Folio via flexibility pass `40011a0` + a11y follow-ups `36f542d`. Focus via density pass `f037f94` (keyboard focus re-anchor after peak edit `peakFocusOrder.ts` WCAG 2.4.3 + trace-svg name 1.1.1 + 1120 toolbar whole-chip wrap + disarmed cue), all render-verified live, frontend-reviewer APPROVE. **Scoreboard: Loupe 38/19✅ · Builder 37/18✅ · Samples 36/18✅ · Folio 37/18✅ · Focus 37/18✅ · Scoping 31/18.** **ONLY SCOPING remains sub-GOLD** (audit-bar MET, needs critique density +5 to ≥36) — then the GOLD finish table. No surface-choice decision left (Scoping is forced). Tree clean, zero open P0/P1, gate green (lint · vitest 2756 · e2e 51/3 · build). NEXT: Scoping re-score #6 (baseline-first) → close its density gap → GOLD → surface the finish table.
>
> **▶ RESUMED 2026-06-11 (limit reset).** Resume-queue item (1) **FOL-HONEST-DERIVED closed** (`2af9897`, P2) — the folio honesty pair, re-dispatched from scratch and landed full-gate-green (vitest 2547 · e2e 46/3 · build) with both reviewers APPROVE-WITH-NITS and a real-browser render-verify of the detail-error state. Resume queue now leads with **(2) Loupe #3 re-score (re-run)** → (3) BU-RESCORE3-P2 remainder + BU-PICKER/BU-ORDERINERT/BU-NAMES → (4) SA F4-sort/F6 → (5) SC-MISC + SC-POLISH2 remainder → (6) cross-cutting UI-RINGCLIP/FA-SUNK/EX-SPACING/AN-MOTION → (7) P3 bands; re-scores due: Folio #6 (after FOL-HONEST-DERIVED), Loupe #3, Samples #4, Focus #3, Builder #5, Scoping #6 — then the GOLD table. Scoreboard unchanged at the pause values below until the next re-scores run.
>
> **⏸ BLOCKED-PAUSED 2026-06-11 ~11:30Z — the Claude weekly usage limit exhausted mid-flight (resets 5am America/Los_Angeles).** Two subagents died on the limit: the FOL-HONEST-DERIVED implementer (test-first half reverted — tree clean; the item is fully scoped in its row) and the Loupe #3 re-score evaluator (no report produced — re-run from scratch). The goal's sequential subagent cadence cannot run without subagent capacity; stopping per the blocked rule. **State at pause: tree CLEAN at the tracker commit after `948a21e`; gate at `64db4eb` = vitest 2525 · e2e 46 · build ✓ · Julia 2050/2050 (at `d8f2d18`); zero open P0/P1s; harness :8091 (new backend code) + :5182 RUNNING.** Scoreboard: Samples 31/17 · Folio 33/18✓ · Scoping 31/18✓ · Builder 29/17 · Focus 31/18✓ · Loupe 32/18✓ (audit bar met on 4 of 6; Samples/Builder one point shy; critique gap +3..+7 everywhere = the P2/P3 density bands). **Resume queue, in order: (1) FOL-HONEST-DERIVED (re-dispatch; spec in row) · (2) Loupe #3 re-score (re-run) · (3) BU-RESCORE3-P2 remainder + BU-PICKER/BU-ORDERINERT/BU-NAMES · (4) SA F4-sort + SA-F6 · (5) SC-MISC + SC-POLISH2 remainder · (6) cross-cutting UI-RINGCLIP/FA-SUNK/EX-SPACING/AN-MOTION · (7) P3 bands as critique fuel · re-scores due: Samples #4, Focus #3, Builder #5, Scoping #6, Folio #6 — then the GOLD table.**
>
> **▶ (prior) RESUMED 2026-06-11.** The pause-queue P1s are closed (LO-TAGTARGET `fe7a84b` + LO-KEPTSTRIP `efd3fa1` — zero open P1s) and the P2 band is moving: FO-F2 `c4c8c02` + FO-F3/F4 `60f57f6`. **Scoping re-score #5 (2026-06-11): 30→31 / 17→18 — audit GOLD bar MET (fourth surface), zero new P1s** (snapshot `2026-06-11T04-54-55Z__seriesscopingpage-rescore.md`; Confirm-busy + Discard-unguarded re-confirm SC-POLISH2; new SC-PREVCLAIM + SC-SKIPDISC P2s, SC-RESCORE5-P3 cluster; evaluator verified seed-carry works and the earlier empty-seed worry was a synthetic-click artifact). FO-RESCORE2-P2 fully closed (`3f1a85c` 8/8). **Builder re-score #3 (2026-06-11): 32→22 / 17 — the first evaluator to exercise Confirm against LIVE SSE and inspect a real export file. Found P0 BU-CONFIRMSTALL (the documented queue SSE-wins-partial gotcha biting in production code: isFullSeries on the synthesized own-op partial → Confirm silently no-ops) + P1s BU-EXPORTDIVERGE (refutes the BU-WYSIWYG "mirrors exactly" closure claim) / BU-INVERT / BU-TOGGLELIE** (snapshot `2026-06-11T05-34-13Z__seriesbuilderpage-rescore.md`). Lesson re-learned: mocked suites drain SSE — live verification is the only honest gate for queue-touching flows. **Queue order now: BU-CONFIRMSTALL (P0) → BU-EXPORTDIVERGE/BU-INVERT/BU-TOGGLELIE (P1s) → SA-RESCORE3-P2 remainder (F3/F4/F6), FOL-RESCORE3-P2, SC-PREVCLAIM/SC-SKIPDISC/SC-COLD+SC-COLDHEAD/SC-POLISH2/SC-MISC, BU-RESCORE3-P2/BU-PROGRESS/BU-PICKER/BU-ORDERINERT/BU-NAMES, LO-RESCORE2-P2, UI-RINGCLIP, FA-SUNK, EX-SPACING, AN-MOTION; then Samples #4 + Focus #3 + Builder #4 re-scores.**

> **✅ HARNESS STATUS (rebuilt 2026-06-09):** the dev backend died mid-session and its `/tmp/himalaya-uat/` DB copy was reclaimed; **now restored** — sourced a surviving persistent DB at `/Users/me/projects/himalaya-devdata/himalaya.db` (139 samples / 1 series / 3 experiments), `sqlite3 .backup`'d it to a disposable `/tmp/himalaya-uat/himalaya.db`, and restarted `env HIMALAYA_DB_PATH=/tmp/himalaya-uat/himalaya.db julia --project=packages/HimalayaUI -e 'using HimalayaUI; main(ARGS)' -- serve --port 8091` (from-source). `/api/samples` → HTTP 200. vite :5182 (VITE_API_PORT=8091) proxies to it; prod :8080 untouched. Render-verify is available. The canonical devdata DB stays pristine (we serve the copy). See [[feedback_live_audit_harness]].
- [ ] **Wave 3 — P2/P3 enhancement**
- [ ] **Wave 4 — Converge (`polish`) + re-score to GOLD**

## Backlog

Severity: P0 blocking · P1 major/any-WCAG-AA · P2 minor · P3 polish. Status: todo / in-progress / done / blocked.
`✓W1` = confirmed by the Wave-1 baseline. Prefer items that fix multiple surfaces at once.


## Triage reconciliation (2026-06-12) — post all-GOLD

A 7-surface parallel re-verification of every open row against **current source** (the six GOLD passes closed many items without flipping their status cells). Authoritative verdicts; the per-surface open rows below are the reconciled remainder. **Raw 59 open rows → ~16 real P2s (in 8 themes) + a ~40-item P3 cosmetic tail; zero P0/P1.** ~18 rows were stale/dup/superseded/carve-out (now in [loop-backlog-complete.md](loop-backlog-complete.md)).

- **Closed (status cell was stale, work already landed):** `FA-SUNK` · `SA-RESCORE3-P2` (last sub-item F6 → SA-ROVING `f745ed9`) · `FO-RESCORE2-P2` (all 8) · `FO-RESCORE1-P2` (focus-reanchor `f037f94` + empty-cart `60f57f6` + toolbar `f037f94`) · `FOL-EMPTY2` · `FOL-RESCORE3-P2` · `LO-THUMB` · `LO-ERR` · `SC-PREVCLAIM` (`310b48e`) · `SC-SKIPDISC` (`310b48e`) · `BU-ORDERINERT` (Field `isStatic` read-only render).
- **Superseded:** `SA-SCALE` → SA-SORT/⌘K · `LO-POLISH3` + `LO-RESCORE2-P3` → folded into `LO-CANVASALT`/`LO-FOCUSRET`/ManageTagsModal.
- **Duplicate:** `SC-PREVCLAIM` (P3 copy of the P2) · `FO-RESCORE1-P3` ≈ `FO-RESCORE2-P3` (net-new = score-semantics caption + detector-canvas label).
- **Carve-out (settled decision, not a defect):** `SA-UNDO2` (deferred L4 undo) · `LO-GRID` (desktop-first ≥1024) · `LO-RESCORE3-P3` (WCAG 2.5.8 met, AAA tradeoff) · `SC-CUSTSSE` (LWW multiplayer corner).
- **Re-severity:** `FO-FOCUSRETURN` **P2→P3** — its P2 premise (modal-close drops focus to `<body>`) is ALREADY FIXED (`useFocusTrap` restores `prevFocus` + a passing test); only the Esc-disarm-of-"+Peak" focus return remains (P3). **Focus now has zero P2 residuals.**

### Next sprint — P2 themes (~16 items, 8 groups; close related fixes together)

| # | theme | items | effort |
|---|---|---|---|
| 1 | **Focus-return** | `LO-FOCUSRET` (goBack→sheet cell) + `FO-FOCUSRETURN` (Esc-disarm→"+Peak") — one `returnFocusRef` idiom | S |
| 2 | **Searchable picker / labels** | `BU-PICKER` (flat `<select>`→combobox, also closes BU-KBDADD) + `FOL-MISC` (search accname) + `FOL-SORT` (visible SORT label) | M |
| 3 | **Fixed-overlay collisions** | `LA-COLLIDE` (3 bottom bars) + `LO-DROPFOOTER` + `UI-ICONOVERLAY` (owner: shrink-to-gap) | M |
| 4 | **Terminology** | `LO-TERM` (exposure/frame) + `BU-NAMES` (draft/figure register) | S |
| 5 | **Loupe sample-flow** | `LO-NEXT` (next/prev-sample nav) + `LO-EXPSPARSE` (one-row metadata stub) | M |
| 6 | **Ring / motion polish** | `UI-RINGCLIP` (clipped in scrollports) + `AN-MOTION` (drop-edge + toast) | S |
| 7 | **Scoping honesty slivers** | `SC-POLISH2` (inert title idiom, value-correct path, discard-guard) + `SC-MISC` (candidate sparkline color-only) | M |
| 8 | **Tokens + error copy** | `EX-SPACING` (spacing scale) + `TY-DISPLAY` (31px step) + `BU-PROGRESS` (cause-named confirm error) | M |

**P3 long tail (~40 sub-items, cosmetic — cherry-pick):** Focus `FO-RESCORE2-P3` (~7) · Builder `BU-POLISH3`/`BU-EXPORT-P3`/`BU-RESCORE3-P3` (13) · Folio 3 `FOL-RESCORE*-P3` clusters (~7 distinct after dedup) · Samples `SA-RESCORE3-P3` (~6) + `DI-SAHEAD` · Scoping `SC-PROV`/`SC-RESCORE5-P3`/`SC-RETRYBANNER`/`DI-SCOPECOLD` · Loupe `LO-POPKEY`/`LO-MOBILE` · singletons `UI-MENUTAB`/`FO-NFHEAD`/`FO-ZOOMEDIT`/`ON-EMPTY`. Two shared one-shot wins: **`document.title`** management (Focus/Loupe/Builder all lack it) and **figure/sparkline phase text-channel** (color-only a11y across Scoping/Builder/Focus).

> **Completed/closed + carve-out + iteration-log moved to [loop-backlog-complete.md](loop-backlog-complete.md)** (2026-06-12 split). This file lists only OPEN work: the Next-sprint themes above + the per-surface open rows below.

## Jonathan-directed GOLD-confirmation audit (2026-06-12) — NEW findings

A critical impeccable-based re-review of all six surfaces + interactions against the live harness (:5182 → :8091, dev-DB copy), run to **confirm the reported all-GOLD status**. Verdict: the six surfaces' *visual/layout* GOLD holds (Folio/Focus/Scoping/Samples render clean, zero console errors), but the **interaction + data-model review surfaced a real cluster the per-surface scoring missed** — chiefly the sample-tag duplication system and three Series-Builder semantics defects Jonathan flagged by name. These are logged below with explicit success criteria; **not yet fixed** (review-only pass). Several **override the earlier "deferred per Jonathan" tag decisions** (LO-TAGDUP / LO-TAGPROV) — Jonathan re-raised duplicate tags as a must-fix this session, so the deferral is rescinded.

Root-cause map for the tag cluster verified against current source (Explore pass): batch tag upsert keys on `(sample_id, key)` only and the schema has **no UNIQUE constraint** on `(sample_id, key)` (`packages/HimalayaUI/src/db.jl:40-46`); the scoping build writes a `source:"scoping"` ordering-variable tag (`lib/queue/mutators/scopeSeries.ts:46` → `POST /api/samples/tags/batch`, `routes_samples.jl:270-393`) on top of any pre-existing `source:"manual"` tag with the same key/value, so both rows persist. All three render paths map raw tags with no dedup: contact-sheet `SampleTableRow.tsx:361`, Loupe `LoupeSidePanel.tsx:113`, Manage modal `ManageTagsModal.tsx:188`. Provenance (`tag.source`) is carried in the model (`loupeAdapters.ts:9`) but never rendered.

| id | surface | opportunity / problem | success criteria | command | sev | status |
|---|---|---|---|---|---|---|
| TAG-DEDUP-MODEL | Samples + Loupe (system) | **Every sample renders two identical tag chips for one key** — e.g. `dose 0` `dose 0` (#1), `dose 5` `dose 5` (#2), `dose 10` `dose 10` (#3). Root cause: a `source:"manual"` and a `source:"scoping"` row coexist with identical `(sample_id, key, value)` because the upsert is keyed on `(sample_id, key)` only and the schema has no uniqueness constraint; the series-scoping build blindly writes a scoping `dose` tag even when an identical manual one exists. On a "show its work / earn trust" scientific tool this reads as a data-integrity bug. Jonathan's directive: "tags such as dose should not have duplicated values; each sample shouldn't have tags with duplicated keys." | A sample has **at most one tag per key** as seen anywhere in the UI. Either (a) enforce `(sample_id, key)` uniqueness at the DB + upsert layer so scoping **updates/owns** the existing key instead of inserting a second row, and heal the existing dupes via a migration/reingest; or (b) if two sources must coexist in the data, **collapse them to one chip per key in every render path** (contact sheet, Loupe chips, Manage modal) with last-writer/most-authoritative value winning. No surface ever shows two chips/rows with the same key. Verified live on samples 1-3. | harden | **P1** | **done `34a1aa5`** (chose (a): `migrate_sample_tags_unique_key!` heals dupes [oldest wins] + `UNIQUE(sample_id,key)` index; add-tag route 409s on collision. Live-verified: contact sheet now one chip per key. Note: the forward-write upsert had already landed pre-audit; this closes the DB-uniqueness + legacy-heal gap.) |
| TAG-MANAGE-PROV | Loupe | **Manage-tags modal shows two anonymous, independently-editable `dose / 0` rows** with no source label and two separate × deletes — the user cannot tell why there are two or which to keep, and editing one to a new value yields `dose=0` + `dose=5` (two values for one key, exactly the state Jonathan says must not exist). | After TAG-DEDUP-MODEL, the modal shows **one row per key**. If any provenance distinction survives, the row **names its source** (e.g. a "from scoping" caption) rather than presenting two identical unlabeled rows. Editing/deleting acts on the single canonical tag. | harden+clarify | P2 | **resolved-by-dedup (`34a1aa5`)** — the two-row symptom is structurally impossible now (one tag per `(sample_id,key)`); no surviving provenance distinction to label. Render-check of the modal pending but the data can no longer produce two rows. |
| TAG-ERR-LAYOUT | Loupe | **Inline `+ tag` duplicate-key error wraps one-word-per-line.** When the key collides, "This sample already has a tag with that key." renders in a ~40px-wide column to the right of the Add button, breaking into a vertical word stack ("This / sample / already / has / a / tag / with / that / key."). Unreadable and visually broken. | The validation error renders as a single readable line (or normal 2-3 line wrap) **below the key/value/Add row**, full-width within the tag editor, in the `error`/`accent` token. No vertical word-stacking at the 286px rail width. | clarify+layout | P2 | **done `f109d81`** — TagEditor restructured to a column: controls wrap on one row, error renders full-width below in the error token. Live-verified loupe/1: dup-key error is a single 209px line, no stacking. |
| TAG-MODAL-ZORDER | Loupe | **Manage-modal key autocomplete dropdown overlaps the inline dup-key error.** Typing a colliding key in the add-row shows both a key-suggestion popover ("dose 4") and the "…that key" error in the same space; the popover paints over the error text (half-hidden). Also the suggestion's trailing count ("4") is an unexplained affordance. | The suggestion popover and the validation error do not occupy the same pixels (suppress suggestions once the key is a known duplicate, or stack them). The suggestion count, if kept, has a discoverable meaning (tooltip/label) or is removed. | harden | P3 | **done `beb6f0b`** (listbox + aria-expanded gated on `!invalid` → a known-dup key hides the popover, error owns the space; count gets title+aria-label "used on N samples"; covers add-row + edit-row; live-verified on HEPES-Only/dose: error fully visible, listbox suppressed, aria-invalid true) |
| BU-AUTOGROUP-STALE | Series Builder | **"Auto-grouped" notice + "Confirm series" / "Adjust" are shown unconditionally — including for already-saved, user-confirmed series.** `BuilderRail.tsx:118-134` hard-codes `title="Auto-grouped"` and body "Himalaya read N samples as one series, ordered by dose." (`builderAdapters.ts:119`) with no state branch. On a saved series (id=2, "Series by dose") this claims the series was machine-grouped and still needs confirming, when the user already created/confirmed it. Jonathan: "the user confirmed these so they aren't auto-grouped; terminology for confirm series and adjust don't make sense." | The notice is **state-aware**: a freshly auto-detected/unsaved grouping may say "Auto-grouped" + offer a confirm path; an **already-saved series shows no "Auto-grouped" claim and no "Confirm series"**. Action labels match real verbs: entering edit mode is "Edit" (not "Adjust"); committing a draft is "Save"/"Save changes" (not "Confirm series" on something already a series). No saved series tells the user to confirm what they already built. | clarify | **P2** (comprehension; tie-breaker leans P1) | **done `e4d8ca5`** — dropped "Auto-grouped" title + the "Himalaya read…as one series" claim → neutral "N samples, ordered by X." (draft-count-aware); "Confirm series"/"Confirming…"→"Save changes"/"Saving…"; "Adjust"→"Edit". Live-verified series/2. |
| BU-TITLE-FIELD | Series Builder | **Builder series title renders as a permanent sans-serif input box** (`PlateTitle` → bare `Input`, `SeriesBuilderPage.tsx:660-668`), inconsistent with (a) the Scoping page's title — same string "Series by dose" — which is a **Newsreader serif display title with a dotted-underline click-to-edit idiom**, and (b) DESIGN.md's Serif-Means-Title + "confident ink, re-openable (not a permanent open field)" rules. The figure/plate's name reads as a form field, not a title. | Builder title matches the Scoping idiom and the design laws: **serif display type, confident ink at rest, click/focus to re-open** for editing (dotted-underline editable affordance), not an always-bordered sans-serif input. The two title surfaces use one shared idiom. | typeset+clarify | P2 | **done `50318f1`** — new Input `title` variant (serif text-display confident ink, no box, dotted underline firming to accent on focus); PlateTitle opts in; stays a real <input> so it edits in place. Live-verified series/2 (Newsreader 27px, ink 0.265, dotted underline, transparent). |
| BU-TRACE-DIM | Series Builder (+ system) | **Unassigned ("Form factor") traces render in faint `ink-faint` grey (oklch ~0.64) at rest, reading as "dimmed"** next to the phase-colored trace — measured at full opacity, so it is a low-contrast *color* choice, not a hover state, but it looks like a stuck hover-emphasis. Jonathan: "series builder should not dim any of the traces when nothing is hovered." Same convention appears on Folio mini-waterfalls (system-wide). | At rest (nothing hovered) **all traces read at equal, full visual strength** — no trace appears de-emphasized relative to its siblings. Unassigned/form-factor traces use a neutral as visually present as the phase-colored traces (e.g. `ink`/`ink-soft`, not `ink-faint`). Any dim/de-emphasis is reserved for an **explicit hover or focus** on a sibling trace and clears completely on mouse-out. Verify on Builder series/2 and Folio cards. | colorize+animate | P2 | **done `82d04a1`** — `UNINDEXED_COLOR` ink-faint→ink-soft in both trace renderers (TracePlot builder+focus; CardFigure folio). Hover-dim is row-opacity-gated (unchanged). Live-verified series/2: form-factor rows stroke ink-soft, equal weight to the phase trace. |

### Broad review round 2 (2026-06-12) — all-surface interaction sweep

A second pass over **all six surfaces** (Jonathan: "broad review of all of the surfaces in the same way, looking for the same kinds of things") via six parallel source-level interaction-honesty reviews + live verification of the strongest leads. New items below are deduped against everything already in this tracker. Verification column: **live** = reproduced in the browser against the dev-DB copy; **source** = confirmed by file:line citation only (not yet browser-repro'd). Dups of already-logged items are folded as notes, not new ids.

| id | surface | problem | success criteria | command | sev | verified |
|---|---|---|---|---|---|---|
| SA-STALESELECT | Samples | **Cull/compose selection is NOT cleared when the beamtime filter changes, and the cull toast over-counts.** `selected` + `checkedSamples` are page state (`SamplesPage.tsx:171,177`) with no effect clearing them on `beamtime` change; `batchSet` announces `n = selected.size` (`:236,251`) but the mutation loop **skips** exposures whose sample left the filtered list (`:237-240`). VERIFIED LIVE: checked sample #1 (SSRL April) → ComposeBar "1 sample"; switched filter to SSRL Jan → sample #1 no longer in the list but ComposeBar still reads "1 sample · + New series" with no visible row to uncheck. The exposure/cull path is worse: a cross-filter `X` would toast "N frames dropped" while dropping 0 in-scope (a false success claim on a destructive action). | Changing the beamtime filter **clears both selection sets** (or filters them to the visible scope) so the bars and the toast count always match what is actually actionable/visible. No toast ever claims a drop/keep count larger than what was mutated. The "+ New series" carry never includes samples outside the current filter. | harden | **P1** | **done `2553aa8`** (effect clears both grains + shift anchor on `beamtime` change; `batchSet` acts on/counts only in-scope exposures via `ownerOf.has`. Live-verified ComposeBar 1→0 on filter change; new Vitest pins it; full FE suite green.) |
| SA-ZEROEXP | Samples | Samples with **zero exposures** (#271 "A1", #272 "B1", #273 "C1") render a `0 / 0` Kept cell and a live "Index" door into Focus with nothing to index. A row that can't do its job offers no honest signal of why (empty manifest? failed ingest?). | A zero-exposure sample either shows a clear "no exposures" status (not just `0 / 0`) or its Index/loupe doors are disabled with a reason. Not a dead door into an empty workspace. | harden+clarify | P3 | **done `aaff911`** (StatusCell `noExposures` state + Focus door suppressed for confirmed-empty samples; page computes loaded-AND-empty, never `total===0` [mid-load false-signal guard]; name→loupe stays since the loupe shows its own honest empty state; live-verified on real #271/272/273) |
| LO-STALEACTIVEID | Loupe | **A stale `activeId` makes the loupe falsely claim "This sample has no exposures."** The init effect early-returns once `activeId` is set (`LoupePage.tsx:115-124`) and never re-selects when the exposures list changes (e.g. another user drops the shown frame via SSE); `activeExposure` then resolves `undefined` and the same empty-state copy (`:355`) fires whether the sample is truly frameless or the active frame just vanished. | When `activeId` is no longer in `exposures`, **re-select a valid default frame** (or show "that frame is gone, here's another"), distinct from the genuinely-frameless message. The loupe never says "no exposures" while frames exist. | harden | P2 | **done `2c92ca9`** (heal effect re-selects when activeId is unset OR absent → computed default, not the stale ?exposure= seed; activeExposure/frameIndex/gallery resolve resiliently so no empty-state flash; empty state reserved for exposures.length===0; new rerender test simulates the SSE frame-vanish) |
| LO-LASTFRAME-NOGUARD | Loupe | `handleDropToggle` (`LoupePage.tsx:169-183`) + the `X` key (`:253`) drop the **last/only remaining frame** with no guard or undo affordance; if the backend rejects it the failure is silent. | Dropping the last kept frame is either prevented with an inline reason or clearly reversible (visible undo). A rejected drop surfaces a cause, not silence. | harden | P3 | **done `283d70d`** (last-kept detection → warning toast naming the consequence + the X-restore reversal; non-last drop keeps plain copy; rejection already toasted by the queue layer so not silent; rendered test covers both paths) |
| FO-PARAM-VALIDATION | Focus | **Custom-index modal "Add to assignment" is always enabled**, with no client validation of the lattice parameter (`CustomIndexModal.tsx:100-108`; `FocusPage.tsx:318-323` sends `basisFor(sym, Number(customParam))` unguarded; empty/0/NaN → backend 400). Contrast the "Add peak at q" field which correctly does `disabled={!qValid}` (`TracePlate.tsx:257`). | The "Add to assignment" button is **disabled when the parameter is empty, non-finite, or out of the symmetry's min/max** — same client-validation pattern as the q-add field — so a bad value never round-trips to a server error. | harden | P2 | **done `599e4db`** (FocusPage computes `customParamValid` vs symmetry min/max → `addDisabled` + `paramInvalid` second-channel on lattice field; `commitCustom` bails; live-verified: 99999 → Add disabled + red field, empty → Add disabled, in-range → enabled) |
| FO-ALLEXCLUDED-CAPTION | Focus | When peaks exist but are **all excluded**, the cart shows "No phase assigned. No candidate fits these peaks. Try a custom index." (`FocusPage.tsx:339-344`, `peaksEmpty` counts all peaks incl. excluded), implying candidates were tried and failed when there are simply zero indexable peaks. | The all-excluded state has its own honest copy (e.g. "All peaks are excluded — restore one or add a peak"), distinct from the genuine no-candidate-fits message. | clarify | P3 | **done `d9bd035`** (new `allExcluded` branch → "All peaks are excluded. Restore a peak, or add one, to index." + candidate-list hint "Candidates appear once a peak is restored."; rendered test asserts both surfaces) |
| SC-TAGORDER | Scoping | **After a manual reorder, the scoping build writes tags in the original proposal order, not the displayed order.** Series positions use the reordered `keptInOrder` (`SeriesScopingPage.tsx:579`) but `buildScopePayload(rows)` uses the never-reordered proposal `rows` (`:583`, `:299`). Backend dedup prevents corruption, but tag-write order and series position diverge. | Both the position-create and the tag-write consume the **same displayed/reordered member order** (`buildScopePayload(keptInOrder)`), so write order and series position always agree. | harden | P2 | **done `74fe8ed`** (`buildScopePayload(rows)` → `buildScopePayload(sorted)`; page-level test reorders C up then asserts Op A tags `[1,3,2]` + Op B positions match) |
| SC-PROVREAD | Scoping | **"read from the sample names" overstates provenance.** The grouping notice + order note (`SeriesScopingPage.tsx:821-822,831`) claim values were parsed from sample names, but they are read from the `sample_tags` table — which may hold `manual` or prior-`scoping` tags (e.g. `dose=0` on "HEPES Only", whose name has no number). Sharper restatement of the already-logged SC-PROV; this one names the false *mechanism* claim. | The provenance copy accurately states where the value came from (a stored tag, possibly hand-entered), not "read from the sample names" unless it was actually name-parsed. Ties to [[TAG-DEDUP-MODEL]] (these are the same `dose` tags). | clarify | P2 | **done `0d28ba6`** ("grouped … by their stored `<key>` value" + "Each value is the sample's stored `<key>` tag"; "skip a misread" → "skip that sample"; stories/fixture updated; live-verified on `/series/new` rendering "stored dose value" — the dose=0/"HEPES Only" case) |
| BU-EMPTYREMOVE | Series Builder | **A draft can be emptied to zero members with no guard, and the stale "Auto-grouped" notice then offers to publish the empty series.** Remove (×) has no disabled gate (`SeriesBuilderPage.tsx` RecipeRow ~805-810; `removeRecipeRow` no validation). VERIFIED LIVE on series/2: removed all 3 members → TRACES rail shows only "+ Add sample…"/Cancel, yet the notice still reads "Himalaya read **3 samples** as one series" with an active **"Confirm series"** (compounds [[BU-AUTOGROUP-STALE]] — the count is now a lie and Confirm would commit an empty series). | The last member cannot be removed (Remove disables at length 1) OR Confirm is blocked + clearly explained while the draft is empty; the grouping notice reflects the **draft** count, not the stale committed count. | harden | P2 | **done `fb23a3b`** — last row's Remove disabled at `count===1` (reason in label); grouping count is draft-aware (BU-AUTOGROUP-STALE pass). Live-verified series/2: remove→1 shows "1 sample", final Remove inert; Cancel restored 3 members. |
| BU-NAVAWAY-DRAFT | Series Builder | **No navigate-away guard for an unsaved draft.** Cross-route navigation / tab close silently abandons an in-progress draft (`SeriesBuilderPage.tsx`, no `useBlocker`/`beforeunload`). sessionStorage restores on reload but not on intentional navigation. | Navigating away with a live unsaved draft prompts a confirm (router `useBlocker` and/or `beforeunload`), or the draft is durably recoverable across navigation. | harden | P2 | **done `c112e06`** (`beforeunload` guard armed only when the draft DIFFERS from the saved series via new pure `isSeriesDraftDirty`; in-app nav already recovers via store+sessionStorage so no router-blocker needed [<BrowserRouter>, no data router]; live-verified clean=unguarded / edit=armed / cancel=disarmed) |
| BU-EXPORT-EMPTYALLOWED | Series Builder | The Export button correctly disables when no trace has data (`SeriesBuilderPage.tsx:561`) but gives **no reason** — user can't tell empty vs still-loading vs fetch-error. | A disabled Export states why (aria-describedby / adjacent hint), distinguishing no-data from loading from error. | clarify+harden | P3 | **done `59388a6`** (ExportButton `disabledReason` → caption beside group + aria-describedby on both actions; page distinguishes loading/error/empty from the trace query; live-verified the error branch via routed 500: reason rendered + trigger disabled + describedby wired) |
| FOL-ALLCHIP-TOOLTIP | Folio | The "All" filter chip lacks the `title`/`description` its siblings carry (`SeriesFolioPage.tsx:299-302` vs the "Has transition"/"Cross-experiment" chips), so it's the only chip with no hover/SR explanation. | All three filter chips carry consistent tooltip + accessible description. (Small; pairs with the already-logged FOL-MISC search-label nit.) | harden | P3 | **done `90dd270`** ("All" chip gets title + sr-only aria-describedby "Every saved series, no filter applied"; live-verified all three chips consistent) |

### Cross-cutting (the system / multiple surfaces)

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| UI-RINGCLIP | focus ring partially clipped on full-bleed buttons inside overflow scrollports — NavModal result items (left/right edges, `overflow-y-auto` list) + ThumbnailGallery/filmstrip thumbs (top/bottom, no scrollport padding). Net improvement over the off-brand UA ring, but the on-brand ring deserves to be whole: inset ring or scrollport padding | craft+harden | P2 | **done `37cb7fc`** | FIXED via a centralized inset-ring opt-in (NOT scrollport padding — the gallery renders per-row ×139, and padding the NavModal list would kill the full-bleed selected-row bg). New `[data-focus-ring="inset"]:focus-visible { outline-offset: -2px }` in @layer base (same 2px accent, drawn inside the box, on top of content); applied to the Thumbnail button + both NavModal result-button maps. Zero layout change; ring stays within the scrollport clip at any scroll position. 2 regression tests assert the hook. Live-verified BOTH in Chrome: computed outline=2px accent @ offset -2px on :focus-visible, focused box within the clip bounds, and screenshots show a whole ring painted ON TOP of the thumbnail's detector canvas (CSS outline paints above content — the Thumbnail's "inset sits behind" caveat was about box-shadow/border, not outline) and around the full-bleed NavModal row with intact left/right edges. Build + full vitest 2822 green. |
| UI-ICONOVERLAY | **`.icon-button::before` 44px touch overlay CANNIBALIZES horizontally-adjacent siblings** — VERIFIED in-browser on the Builder recipe row (Move-up / Move-down / × cluster, ~27px center-to-center): each button's 44×44 `pointer-events:auto` overlay extends ~22px past its center, so the *later* sibling's overlay paints over the *earlier* button's right half. `document.elementFromPoint` at up-center+10/+18/+21 returns the **neighbor** ("Move down"), not "Move up" — clicking the right portion of one icon button fires the adjacent one. Effective exclusive horizontal target collapses to ~10px (5px each side of center); vertical is fine (~32px). NOT a strict WCAG 2.5.8 failure (spacing exception holds at ≥24px centers) but a real pointer mis-map. **Multi-surface**: every clustered IconButton (Focus peak/exclude controls, Loupe, Samples, Builder). **Owner decision resolved 2026-06-12 (Jonathan): SHRINK the overlay to ≤ the cluster gap** so adjacent overlays don't overlap and each button wins its closer half — meets WCAG 2.5.8 AA by size, accepts losing the AAA-2.5.5 44px touch target. (Rejected: keep-44px-as-is; horizontal-clamp-in-clusters.) Implementation note: a generic primitive can't see its neighbors, so likely cap the `::before` to a fixed ≤24–28px (the minimum that still meets AA while clearing the tightest gap=8px cluster), OR expose an overlay-size prop the dense-cluster callers set. Keep vertical expansion generous. | harden+adapt | P2 | **done `e4e4a7b`** | verified 2026-06-12 during Builder re-score #5 (mis-triaged P2-target-size → real mechanism); fix approach owner-resolved 2026-06-12. FIXED: `.icon-button::before` width 44px→24px (height stays 44px). Live-re-verified on `/series/2` edit: all 3 recipe-row clusters, all 6 adjacent pairs now return the probed button at +12px from centre (was the neighbour). Build + full vitest 2802 green. |
| EX-SPACING | spacing/density scale underspecified; promote shared-grid/alignment constants to tokens | extract | P2 | **done `ee5dd18`** (grid-constant half) | FIXED the concrete, named part: three page layout grids were duplicated VERBATIM between a loading-skeleton placeholder and the loaded render (a skeleton↔real ALIGNMENT INVARIANT that drifts if edited in one place). Single-sourced each to a named const (LOUPE_BODY_GRID 286px ×2, FOCUS_PAGE_GRID 350px ×2, FOCUS_SPLIT_GRID ×2) — literal class strings so Tailwind JIT still emits them. Pure DRY, no visible change: built CSS still emits the same `grid-template-columns` decls; live-verified computed templates unchanged (loupe 722+286, focus 1090+350). (SAMPLE_TABLE_COLS was already single-sourced; the Builder's two grids genuinely differ skeleton-vs-real, left untouched.) **RESIDUAL:** the broader "spacing/density scale → semantic @theme tokens" is a larger, visible-change-risk app-wide effort — deferred (not a contained extract) → now tracked as **EX-SCALE** (P3). |
| EX-SCALE | spacing/density scale → semantic `@theme` tokens (the broad part of EX-SPACING; the drift-prone grid-constant half is done `ee5dd18`) | extract | P3 | todo | Split from EX-SPACING 2026-06-13. Downgraded from the parent's P2: NO user-facing defect — pure internal system tidiness. Large + visible-change-risk app-wide (a spacing token applied inconsistently DOES change appearance), so not a contained extract. |
| FO-NFHEAD | Focus/Loupe not-found routes render EmptyState's h2 with no h1 anywhere (level-skip; the found branch's PlateHeader is h2 too so it's a route-structure condition, inherited not introduced by EX-EMPTY — prior code had ZERO headings there) | harden | P3 | **done `aaf8748`** | FIXED. EmptyState gained `as?: "h1"\|"h2"` (default "h2" — it usually nests under a page that owns the h1; every existing usage unchanged). The not-found views — where EmptyState IS the page (shell has no h1, the branch returns before any plate header) — pass `as="h1"`. Corrected the row's premise while here: the FOUND pages are NOT headingless — TracePlate's PlateHeader is `as="h1"` (Focus) and Loupe found is `as="h1"`; the defect was ONLY the not-found sole-EmptyState. **Builder not-found shares the exact defect** (its normal-page title is an editable `Input`, not a heading) so fixed here too; Folio empty sits under FolioHeader's h1 and Samples under its page h1 (both already fine). 5 tests (EmptyState default-h2 + as-h1; one per not-found route). Live-verified /sample/99999 + /samples/loupe/99999 + /series/abc in Chrome (each: exactly one h1, sole heading, no h2 skip, 0 console errors). Build + full vitest 2869. → discovered sibling logged as **BU-NOHEAD**. |
| BU-NOHEAD | the SeriesBuilder NORMAL page has no heading element at all — the series title is an editable `Input variant="title"` (PlateTitle), and the shell carries no h1, so the built-series page presents to assistive tech with zero headings / no h1 (distinct from the not-found case [[FO-NFHEAD]] fixed it for) | harden | P3 | **done `70c33cc`** | PREMISE REFINED via live probe: an `<h1>` DID exist (SeriesPlate→PlateHeader as="h1") but it WRAPPED the editable Input — so its accessible name was EMPTY (input value isn't heading text) and an interactive control was nested inside a heading (impl-dependent accname, heading-nav lands on a nameless heading). Shape decision = the backlog's first option (visually-hidden real heading + visible field demoted out of heading semantics): PlateHeader gains opt-in `headingText` — when the visible `title` is interactive, it renders a `sr-only` `<TitleTag>{headingText}` as the real heading and puts `title` in a NON-heading `<div className="text-display">`. Default branch (static title → title IS the heading) unchanged, so Focus/Loupe/Folio are byte-identical (zero blast radius). SeriesPlate forwards it (conditional spread for exactOptionalPropertyTypes); Builder passes `effectiveTitle \|\| "Untitled series"` (placeholder fallback matches the input's visible placeholder so an untitled draft heading is never empty). 2 tests (PlateHeader headingText branch + Builder normal-page real-h1). Live-verified /series/2 in Chrome: exactly one h1 named "Series by dose" (was ""), sr-only (position:absolute, 1×1), input NOT a heading descendant, visible title still 27px text-display, and the h1 TRACKS title edits in lockstep ("Series by dose EDITED"). Build + full vitest 2878. Mirrors the Focus title-as-Input→h1 precedent the row named. |
| UI-MENUTAB | Menu doesn't close on Tab/focus-out (APG: Tab closes the menu) — also what makes the "trigger focused while menu open" state reachable; fix focus-out rather than special-casing arrows-on-open-trigger. Note: arrow-open/escape-return are JSDOM-only pins (e2e covers click paths) | harden | P3 | **done `1432665`** | FIXED with two coordinated changes. (1) Menu items get `tabIndex={-1}` — the popup becomes ONE tab stop (APG roving-tabindex menu), so one Tab from a focused item leaves the WHOLE popup rather than walking item→item (focus still arrives via the open effect + arrow nav, which are imperative `.focus()`, unaffected). Live-discovered this was necessary: with the items as plain tabbable buttons, a single Tab just walked to the next item and the menu never closed. (2) The OWNERS (Field, ExportButton) close on focus-out of the whole widget — an `onBlur` on the same `wrapRef` their outside-pointerdown close already uses (relatedTarget outside wrapRef, or null → setOpen(false), no refocus). **Owner-scope, not menu-scope, is load-bearing:** a close scoped to the menu div alone fires when focus moves to the trigger, and the trigger's toggle onClick then REOPENS it (close-then-reopen) — scoping to the widget keeps focus-to-trigger inside, so a toggle-click closes cleanly. Mirrors the existing outside-pointerdown close on the focus axis; Menu stays presentational. 5 tests (single-tab-stop tabindex + focus-out-closes + focus-to-trigger-keeps-open ×2 consumers). Live-verified /series/new "Ordered by" dropdown in Chrome: single real Tab closes + advances focus; trigger-toggle closes (no reopen); outside-click + item-select still close; 0 console errors. Build + full vitest 2874. |
| FO-ZOOMEDIT | zoomed + armed: off-window peak marks stay tabbable (clipped glyph AND clipped focus ring = invisible tab stops for sighted kb users); add-at-q validates the full data extent so an off-screen add succeeds invisibly until zoom-out. Pointer parity today (cannot click them either) | harden | P3 | **done `3281ce9`** | FIXED both halves. (1) `PlotPeaks` gates the focusable `activateAttrs` (role/tabIndex/aria/onKeyDown) on the peak's q being inside `projection.x.domain` — d3 scales don't clamp, so an off-window q maps outside the range. The `<g data-peak-id>` still renders (the focus-re-anchor looks survivors up by id) but is inert: no phantom tab stop, pointer/keyboard parity. The re-anchor effect also falls back to the "+ Peak" button when the nearest survivor is off-window (non-focusable `tabindex!=="0"`) instead of dropping focus to `<body>`. (2) `TracePlate` add-at-q now validates against the VISIBLE window (`xDomain` clamped into `[qMin,qMax]`), not the full trace extent, so a typed q that's in the data but off-screen can't seat an invisible peak; native Input min/max follow. No-zoom path unchanged (window == extent). 2 tests (off-window peak inert + zoomed add-at-q rejects off-window/accepts in-window). **Live-verified /sample/3 in Chrome** (the real win, since jsdom has no layout/zoom): added a manual peak at q=0.356 (role=button/tabindex=0), wheel-zoomed about a low-q cursor to [0.006, 0.264] → its `<g>` lost role/tabindex/aria while staying in the DOM; the add field rejected q=0.308 (off-screen) + accepted q=0.133 (visible), bound shrinking with the zoom; 0 console errors. Build + full vitest 2876. |
| AN-MOTION | app essentially static: selection bars snap, drag drop-edge hard hairline, candidate-dim instant, no toast/modal motion | animate | P2 | **done `0374d6d`** (expressive register, Jonathan's pick) | Added a livelier entrance pass, centralized as keyframe classes in styles.css: floating bars (CullBar/ComposeBar) rise+fade (translate-y-3→0, 300ms ease-out) instead of opacity-snap; toast `anim-toast-in` (slide-from-right + scale 0.96 + fade, 240ms); modal `anim-modal-in` (scale 0.94 + 10px rise + fade, 240ms — replaces the subtler shared anim-pal-scale, kept for popovers); drawer `anim-drawer-in` (slide from right edge, 260ms). Entrance ease `cubic-bezier(.34,1.26,.64,1)` = single ~3% overshoot (gentle spring, NOT elastic — design law honored); drawer/scrim pure ease-out. prefers-reduced-motion free via the existing `*` reduce block. No unit test (CSS motion = no semantic to assert); live-verified each in real Chrome (bar transition translate+opacity 300ms ease-out · toast anim 240ms spring · modal anim 240ms spring). **RESIDUAL (intentionally left):** drop-edge stays an instant hairline (immediate drag feedback is correct UX, not a snap); candidate-dim deferred (fuzzier/low-value) → now tracked as **AN-DROPEDGE** (WONTFIX) + **AN-CANDDIM** (P3). |
| AN-DROPEDGE | drag drop-edge stays an instant hairline (no fade/grow) | — | WONTFIX | by-design 2026-06-13 (split from AN-MOTION) | NOT a defect: immediate drop feedback during a drag is correct UX; animating a drop indicator reads as sluggish/laggy. Recorded so it isn't re-surfaced as a motion gap. |
| AN-CANDDIM | candidate-dim is instant (no ease) | animate | P3 | **resolved — already eased, no change (2026-06-13)** | SCOPED both named surfaces; the observation is STALE. (1) **Plot hypothetical-preview dim** — ALREADY eases. The candidate-hover preview feeds `highlightPeakIds`; both `PlotPeaks` (glyphs) and `PlotLabels` (labels) paint the dim via CSS `color: var(--color-ink-faint)` + `fill/currentColor`, and `color` IS in the global 120ms ease-out transition list (`fill` is not), so dim AND release ease instead of snapping. This was done by **FO-DIM `df47fa4` (2026-06-10)** — TWO DAYS BEFORE the AN-MOTION note (`0374d6d`, 2026-06-12) that re-flagged it — and is regression-guarded (PlotPeaks.test.tsx + PlotLabels.test.tsx assert color=ink-faint/fill=currentColor). Live-confirmed on /sample/3: the peak `<g>` computes `transition-property: color … opacity` @ 0.12s (rest-dimmed peaks ease too). (2) **Focus assignment rail** — has NO candidate-dim mechanism (`AssignmentRail.tsx` carries no opacity/dim/selected state; `highlightPeakIds` reaches only the peaks/labels layers via TracePlot, never the trace line or rail rows). Nothing to ease there. No code change: writing a transition would fabricate motion the code already performs, or animate an element that doesn't dim. |
| TY-DISPLAY | restore display-xl (31px) step (titles app-wide quieter than designed) | typeset | P3 | **done `cce43a8`** (Jonathan approved the app-wide enlargement 2026-06-13) | FIXED. The greenfield shell set `--text-display` conservatively at 27px; the mockups want 31px (series-folio.html:113 = 31px, sample-table.html:128 = 30px). Bumped the token 27→31px (the ONE Fixed-Scale-Rule extension point, no inline sizes) so the figure/sample/series hero titles read at the designed weight everywhere `text-display` lands (PlateHeader hero, FolioHeader, ScopePlate, SamplesPage, Input title-variant, dev shell h1). DESIGN.md reconciled in the SAME commit (typography.display 27→31px · hierarchy `~31px` · the Fixed-Scale-Rule example text) — closes the row's "reconcile w/ DESIGN.md display token". No jsdom test (a CSS px value has no semantic to assert — AN-MOTION precedent; no test/guard pinned 27px). Live-verified in Chrome: Samples ("The corpus"), Focus ("LL37 Only" at BOTH 1440 and 1000 — title↔toolbar never overlap/wrap, the toolbar's own whitespace-nowrap handles tighter), Builder (editable title input, coexists with the BU-NOHEAD sr-only h1), Folio ("Saved series") all compute 31px with NO horizontal overflow + 0 console errors. Build + full vitest 2879. |

### SamplesPage (contact sheet)

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| SA-RESCORE3-P3 | re-score #3 P3 cluster: F7 mixed cursor language · F8 representative dot meaning only in aria-label · F9 Index door reads static "Not indexed" · F10 frame captions barely legible over busy frames (no scrim) · F11 kb-legend under empty table · F12 no select-all/range for series checkboxes | polish | P3 | **4 of 6 done `a7ea1dc`** | F9 FIXED: StatusCell gains a `door` prop → an unindexed door reads "Index →" in accent (affords the click) instead of dead "Not indexed"; indexed door keeps PhaseChip; non-door cells keep the passive status. Live: 136 doors invite, 0 static. F10 FIXED: soft bottom scrim (gradient from the frame-edge token) behind the mono frame number so it stays legible over BRIGHT frames (mockup assumed uniform-dark). F8 FIXED: rep + kept markers gain hover `title`s ("Representative exposure" / "Kept (screened in)") — meaning was SR-only (button aria-label) before. F11 FIXED: kb-legend gated on `sortedSamples.length > 0` (was rendering under empty/errored/unknown-beamtime tables). 8 tests; build + full vitest 2889; live-verified F8/F9/F10 on /samples (0 console errors). **NOT done (2):** F7 mixed cursor language (fuzzy, low-value — deferred), F12 select-all (the shift-range half ALREADY exists at SamplesPage:270 + the legend documents "⇧ click extend the range"; a select-all checkbox is a small feature, deferred). |
| SA-THUMBKEY | thumbnail double-click→loupe has no per-thumbnail keyboard parity (Enter on a focused thumbnail = select-toggle, its native click). Function itself is keyboard-reachable via the SpecCell name button, so this is parity polish, not 2.1.1 | harden | P2 | **done `2589dce`** | FIXED: Shift+Enter on a focused thumbnail fires its onDoubleClick (the loupe-at-frame activate), preventDefault'd so the same keystroke can't ALSO fire the native select-click; plain Enter unchanged (select-toggle); documented in the sheet legend. The "frame-aware loupe route" prerequisite was satisfied — the sheet already passes ?exposure=<id> (LO-NEXT confirmed). 2 Thumbnail unit tests + 1 SamplesPage integration test. Live-verified: Shift+Enter on focused frame 3 → /samples/loupe/1?exposure=3; plain Enter stays on the sheet. Build + full vitest 2849 green. |
| DI-SAHEAD | head body is a long single tagging-philosophy paragraph | distill | P3 | **done `780da33`** | FIXED. The contact-sheet head `<p>` led with the screening action then drifted into two asides (tags-are-a-light-optional-note + ordering-named-later) across 3 sentences / 42 words. Distilled to action-first, 2 sentences / 25 words: "a light, optional note on what each sample is" → "optional"; the ordering aside folds into the same clause ("you name the ordering variable later, when you scope a series"). All three facts kept (drop flares/artifacts · tags optional · ordering deferred to scope), no em-dash, quieter register. Copy-only (no brittle exact-string test — same call as TY-DISPLAY/AN-MOTION; no test referenced the old copy). Build + full vitest 2879; live-verified the rendered head on /samples (2 sentences, 25 words, 0 console errors). |
| ON-EMPTY | empty corpus dead-ends ("No samples yet") with no add-data/learn path | onboard | P3 | **done `0ee0cad`** | FIXED. The corpus-empty table slot was a lone `<div>…No samples in the corpus yet.</div>` — the only one of the page's THREE empty states (error, unknown-beamtime, corpus-empty) lacking an EmptyState + way forward. Swapped for the same `EmptyState` primitive the siblings use: title ("No samples yet" / beamtime-aware variant), body naming how samples appear ("Samples appear after an experiment is ingested and analyzed. If you just added data, reload to pull it in." — the CLI ingest path; NO in-app uploader to fake, controls-don't-lie), and a "Reload the corpus" `Button` wired to the SAME `corpusQuery.refetch()` the error state uses (covers "I just ran reingest"). Default `as="h2"` nests correctly under the page's "The corpus" h1 (no level skip — distinct from the FO-NFHEAD not-found case where EmptyState IS the page). 1 test (empty corpus exposes EmptyState + reload→refetch). Live-verified on /samples by driving the `["corpus","samples"]` query empty via the live QueryClient (React-fiber reach, LA-COLLIDE technique): EmptyState "No samples yet" h2, body present, enabled reload, 0 rows, 0 console errors. Build + full vitest 2880. |
| LA-COLLIDE | CullBar/ComposeBar can collide with bottom InfrastructureBanner | layout+animate | P2 | **done `c1783c5`** | FIXED: the banner (fixed bottom-centre, z-40) was fully OCCLUDED behind the opaque higher-z CullBar (bottom-7, z-50) / ComposeBar (bottom-20, z-50) whenever a selection bar was up. Resolved via a small dedicated coordination store (`src/print/shell/floatingDock.ts`, NOT a brittle px offset): SamplesPage publishes `centerLaneOccupied = selected.size>0 \|\| checkedSamples.size>0` (released on unmount); the banner reads it and steps aside to the bottom-right corner (free — toasts own top-right) while occupied, staying centred otherwise. `data-dock="center\|aside"` semantic hook. 3 new tests (banner two positions + page publishes/releases). Live-verified on /samples by injecting a pending mutation into the live QueryClient via React fiber (the queue's own-op SSE confirm resolves real mutations under the 500ms banner threshold, so the network can't stage it): selection active → aside at x=1230 (clear of the centred CullBar 517–923); cleared → center, centreX=720=viewport centre. Build + full vitest 2816 green. |

### SeriesFolioPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| FOL-SORT | visible "SORT" label missing left of segmented control (mockup series-folio.html:326) | typeset | P2 | **done `4aba6d8`** | FIXED: "Sort" Kicker (uppercase) left of the control, aria-hidden so SR keeps the control's own "Sort series" name (no double-announce). New test; live-verified on /series. Build + vitest 2811 green. |
| FOL-MISC | search input unlabeled (P2, WCAG 3.3.2) + no on-page new-series CTA + indistinguishable empty drafts | harden+craft+clarify | P2 | **done `323b159`** | FIXED (search label): SearchInput had no accname passthrough → its name rested on the disappearing placeholder "Search series…". Added an `ariaLabel` prop (forwarded to the inner input's `aria-label`, which beats the placeholder) and pass "Search series" from the folio (the only SearchInput consumer). New test asserts the searchbox accname is exactly "Search series" (no ellipsis) so it passes only when a real label wins. Live-verified on /series. The other two bundled parts were already closed by the time of the re-score: the on-page new-series CTA is the F3 `NewSeriesTile` (dashed door → /series/new at the wall's end), and the empty / filtered-empty states each render an honest `EmptyState` door — no indistinguishable empty drafts. Build + full vitest 2817 green. |

### SeriesScopingPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| SC-POLISH2 | re-score P2 cluster: busy half DONE `b2e845b` ("Building…" aria-busy on all three feet); "low to high" caption goes stale after manual drag; inert dotted-underline editable idiom on title (no onEditTitle); skip-only model can't correct a misread value (shape-gated); Discard/DYO-re-entry destroy typed work silently | polish/harden | P2 | **4 of 5 done `734148f`** | (1) busy half `b2e845b`; (2) stale "low to high" caption already honest via SC-COUNTHONEST ("custom order" once `order` ≠ value-sorted `seededOrder` — verified in code); (3) inert editable-title idiom FIXED `446c8d3` (ScopePlate gates dotted-underline+click+text-cursor on `onEditTitle`, absent on the worksheet → plain ink; `data-editable` hook); (4) Discard FIXED `734148f` — moved from page-top into each plate foot beside "Confirm & build" (presentational `discardSlot` on ScopePlate + ColdAssignSection) AND guarded: clean worksheet leaves immediately, dirty (history reorder/skip OR typed cold value) arms an inline two-step confirm ("Discard your work? Keep editing / Discard") — no modal, no nav until confirmed. 3 page tests; live-verified /series/new (foot placement same-row; skip→Discard→confirm, no nav; Keep editing preserves work). **RESIDUAL (5, own follow-up):** skip-only can't correct a misread value — SHAPE-GATED (needs an inline value-edit model); the dropdown DYO-re-entry still re-seeds (deliberate decision — a "preserve typed values on re-entry" enhancement beats a select-change confirm) → the skip-correct part is now tracked as **SC-VALUECORRECT** (P2, shape-gated). |
| SC-VALUECORRECT | scoping worksheet: a MISREAD auto-read ordering value can only be SKIPPED, not CORRECTED — there is no inline value-edit on a member row, so a wrong value forces you to drop the sample | craft+harden | P2 | **done `2fab07f`** | FIXED via **click-to-edit-in-place** (Jonathan's design nod 2026-06-13, picked over cold-path-converge + correction-drawer): a pencil IconButton beside the FlagButton swaps just that value into a mono `Input` (`ScopeSampleRow` gains `onEditValue`); commit (Enter / ✓ button / blur) fires onEditValue ONLY when the value CHANGED and is non-empty (guards `value:""` out of the batch write — the `isKept` invariant), Escape cancels. Edit + skip stay DISTINCT affordances (correcting ≠ dropping). The editing cluster carries `draggable={false}` so the field is text-selectable under the row's `draggable=true` reorder wrapper (`useDragReorder`) — the one real-browser-only hazard, structurally fixed + live-confirmed. Page records a typed `value` history entry → skip/reorder/**edit** reach full Undo + ⌘Z parity. 8 component + 2 page tests (write-payload + ⌘Z-revert). Live-verified /series/new (pencil → focused+selected input replacing the skip toggle → Enter commits into the row + tag write → "Undo last change" reverts to original + clears history → cluster released from the draggable parent). Build + full vitest 2859 green. Closes the last genuinely-open P2: **open-P2 count now 0.** |
| SCOPE-LOOSEADD | the "Himalaya also found" loose candidates (samples lacking the ordering key) were read-only discovery — bringing one in meant leaving for the contact sheet to tag it. Inert because "no value → an add would lie" | craft+harden | P2 | **done `04faecd`** | FIXED (Jonathan: "it is weird to just present these options without allowing the user to actually do something with them"; cap is fine — a few exemplars + the contact-sheet door reaches the rest). The "would lie" objection dissolved once inline value-entry existed ([[SC-VALUECORRECT]]): each visible loose row now carries a mono value `Input` + a "+ Add to series" `Button` GATED on a non-empty value (a value-less add stays disabled — it could only seat a `value:""` member the gate drops; controls-don't-lie). Naming a value + Add (button or Enter) folds the sample into the members WITH that value (a real included/unflagged member joining the display order). Typed `add` history entry → skip/reorder/edit/**add** all reach Undo + ⌘Z parity; undo pops the candidate back to the FRONT of the loose pool (visible above the preview cap), not the hidden tail. **Load-bearing gotcha:** loose matches carry `include:false`, so `addLoose` must override `include:true` or `isKept` silently drops the new member from the write (caught LIVE — the build payload omitted the added sample until the override landed; the DOM showed the row, only the payload betrayed it). 5 page tests; live-verified /series/new (3 gated candidates → type enables Add → promotes to a kept member 4→5 + "5 values ready to commit" + cap refills → Undo reverts members/foot/candidate-to-front). Build + full vitest 2864. Note: the value-field aria-label uses the (duplicable) sample name — minor a11y nit; the row's mono smp_ id disambiguates visually. The story-only `ScopeCandidateRow` composite stays unused (its testid/signature diverge); convergence deferred. |
| SC-PROV | "read from the sample names" provenance claim not obviously true (dose=5 on "HEPES Only") + header "6 samples" vs foot "5 · 1 skipped" diverge silently | clarify | P3 | **done `5eff5da`** | BOTH halves now closed. HALF 1 (provenance claim) was already resolved by **[[SC-PROVREAD]] `0d28ba6`** (notice → "grouped N samples by their stored `<key>` value", orderNote → "Each value is the sample's stored `<key>` tag" — no name-parsing claim survives). HALF 2 (count divergence) FIXED here: `orderCaption` counted ALL rows ("N samples") while the foot split kept/skipped ("M values ready · K skipped"), so a skip made header (6) and foot (5) silently diverge. Annotate the skip inline: `${rows.length} samples${skippedCount>0 ? ` (${skippedCount} skipped)` : ""} · <order>` so N total = M committed + K skipped reads across both ends; no-skip caption unchanged. 1 test (caption gains "(1 skipped)" on skip, beside the foot's split). Live-verified /series/new: skip 1 of 4 → caption "4 samples (1 skipped) · low to high" + foot "3 values ready to commit · 1 skipped", 0 console errors. Build + full vitest 2881. |
| BU-EXPORT-P3 | export-WYSIWYG residuals (review MINORs, accepted-not-blocking): plate legend shows coexistence-tail phases while export legends dominants only (one color per trace; half-truth under the footnote) · peak GLYPH register diverges (plate forces auto-triangles; export keeps p.source diamonds for manual) · dead code: computeYBands lost its last production consumer + lib/comparison/labels.ts speaks the retired "Exposure #N" register · exposure-less members: plate renders an empty band, export filters them | polish | P3 | todo | BU-EXPORTDIVERGE reviews 2026-06-11 |
| BU-RESCORE3-P3 | re-score #3 P3 cluster: title keystroke abruptly swaps rail to edit mode · no description UI despite draft/save support · static document.title · ink-faint data traces near graphical-contrast floor · dev SSE reconnect → full 5-query refetch churn | polish | P3 | **N3 done `d26bacf` · N4 already-resolved** | N3 (static document.title) FIXED via a new reusable **`useDocumentTitle(lead)`** hook (sets "`<lead> · Himalaya`", restores prior on unmount, falls back to bare app name for load states) wired into the Builder off the committed series title (hooks-safe above every early return). 4 hook tests; live-verified /series/2 tab reads "Series by dose · Himalaya". **This hook is the shared "static document title" fix — it ALSO closes [[FO-RESCORE2-P3]] F14 when that cluster lands.** N4 (ink-faint traces) was ALREADY resolved (waterfall/CardFigure use `ink-soft`; `UNINDEXED_COLOR = var(--color-ink-soft)`). **Deferred:** N2 (description editor — a feature-add, not a polish-bug), N1 (abrupt title→edit swap — fuzzy), N5 (dev SSE reconnect → 5-query refetch churn — dev-only perf). Build + full vitest 2893. |
| FOL-RESCORE4-P3 | #4 P3 cluster: CardFigure CLS (reserve height from member_count) · header count lacks "showing N of M" + hardcodes count=0 in the error state (unknown != 0) · search is title-only without saying so · filter chips lack group/exclusivity semantics · search input named by placeholder only · uneven right whitespace at wide columns | polish | P3 | **partly done `6166161` · rest GOLD-closed** | Gallery key={i} remount (FOL-RESCORE5) FIXED (`React.Children.toArray` + child-derived wrapper key → re-sort moves the wrapper, no card remount/figure-refetch; mount-counter test). Dangling '· ' author separator (FOL-RESCORE3) FIXED (footer gates the '· author' clause on a non-empty author). VERIFIED-ALREADY-CLOSED by the folio GOLD pass + FOL-MISC (no code): 'Showing N of M' live count (F2 role=status), error-state count=null-not-0, search haystack+accessible-name (`323b159`), filter-chip title+aria-describedby (`40011a0`/`36f542d`). Deferred minor: mini-waterfall right-border kiss, 'Has transition' coexistence-card nuance, CardFigure CLS height reserve. Build + full vitest 2898; live-verified /series. |
| FOL-RESCORE5-P3 | #5 P3 cluster: "Has transition" excludes the coexistence-caption card (dominant-phases-only scent contradiction) · search title-only + silently blames the user's filter · no "showing n of N" under filter · Gallery positional key={i} discards child keys (re-sorts remount every card) · search accname = placeholder only · empty folio still renders the control row | polish | P3 | **partly done `6166161` · rest GOLD-closed** | Gallery key={i} remount (FOL-RESCORE5) FIXED (`React.Children.toArray` + child-derived wrapper key → re-sort moves the wrapper, no card remount/figure-refetch; mount-counter test). Dangling '· ' author separator (FOL-RESCORE3) FIXED (footer gates the '· author' clause on a non-empty author). VERIFIED-ALREADY-CLOSED by the folio GOLD pass + FOL-MISC (no code): 'Showing N of M' live count (F2 role=status), error-state count=null-not-0, search haystack+accessible-name (`323b159`), filter-chip title+aria-describedby (`40011a0`/`36f542d`). Deferred minor: mini-waterfall right-border kiss, 'Has transition' coexistence-card nuance, CardFigure CLS height reserve. Build + full vitest 2898; live-verified /series. |
| SC-RESCORE5-P3 | re-score #5 P3 cluster: phase hue color-only on member/candidate sparklines (aria-hidden svg; labeled only in preview strip) · "▸ SKIPPED" caption 9px (decorative) · reorder lacks undo (⌘Z covers skips only) · disabled build-button contrast ~2.1 (WCAG-exempt, legibility) | polish | P3 | **resolved by prior work (2026-06-13)** | Triaged — NO new code needed, the listed nits were closed by the scoping GOLD pass + SC-MISC: (1) "reorder lacks undo" → DONE by **`cac7438`** (`HistoryEntry` gained a `reorder` type + `applyReorder` records `prevOrder`; ⌘Z now reverts reorders, not just skips); (2) "▸ SKIPPED caption 9px" → DONE by **`cac7438`** (now `text-caption` = 10.5px, FlagButton); (3) candidate sparkline phase color-only → DONE by **`58fe96b`** (SC-MISC: indexed candidates carry a PhaseChip text second channel). Residuals NON-blocking: member sparkline is aria-hidden/color-only but members reach the **preview strip that labels phases by text** (second channel exists; the SeriesScopingPage:1023 comment confirms loose candidates are the only ones that miss it — and those got the PhaseChip); disabled build-button contrast ~2.1 is WCAG-EXEMPT (disabled control) and bumping it means changing the GLOBAL `Button disabled:opacity-45` app-wide (visible-change scope creep for a non-WCAG legibility nit) → deliberately deferred. |
| SC-MISC | candidate rows dead-end (no inline tag-add) + ~~Discard no focus-visible ring~~ (closed by F-STATE `2edcf6c`) + candidate sparkline aria-hidden phase | craft+harden | P2 | **done `58fe96b`** | FIXED (phase a11y, WCAG 1.4.1): a loose-candidate row conveyed its phase ONLY via the dimmed aria-hidden sparkline hue, and loose candidates never reach the preview strip that labels member phases — so colour was the sole channel. Added a trailing PhaseChip (phase-name text = always-on second channel) on indexed candidates; omitted when unindexed (controls-don't-lie). New test; live-verified /series/new (Pn3m / Lamellar chips on indexed, none on unindexed, sparkline still aria-hidden). The tag-add "dead-end" is already closed by design — loose matches are Option-A informational discovery (no commit action) with a section-note recovery + Open-the-contact-sheet door; the story-only ScopeCandidateRow composite shares the gap but isn't used live. Build + full vitest 2820 green. |
| SC-RETRYBANNER | stale chain-failure banner (incl. its "Try again" CTA) stays visible while a retry is already in flight — suppress while scopeSeries.isPending/createSeries.isPending | harden | P3 | **done `03082b3`** | FIXED. The lingering case: after an Op-B (create) failure, `createSeries.error` stays set through the retry's TAGGING phase (handleBuild only clears `scopeSeries.error`), so the banner + its "try again" copy kept shouting next to the "Building…" control. Gated the banner on the SAME single in-flight truth the busy button reads — new `showErrorBanner = chainErrorCopy !== null && !buildBusy` (buildBusy ⊇ scopeSeries/createSeries.isPending AND the inter-op stage gap, so the banner is hidden for the WHOLE in-flight chain, not just the isPending edges; banner + busy-button stay consistent). On resolution the banner returns only if a FRESH error surfaces (existing 3 banner tests still green → no resting-state regression). 1 test: stale-Op-B-error → Confirm-&-build click → "Building…" up + banner gone (the in-flight frame is un-stageable via network interception — queue own-op-SSE-confirm resolves real mutations first, same constraint documented on [[BU-PROGRESS]]; jsdom drives the real component logic). Live-smoked /series/new resting render (4 warm rows, no banner, 0 console errors). Build + full vitest 2879. |
| DI-SCOPECOLD | cold-panel stacked explanatory prose competes for one eye position | distill | P3 | **done `2a317c5`** | FIXED. The cold-assign panel stacked an h2 "Name the ordering variable" directly above an intro that restated it: "These samples share no tag key yet. Name the variable (e.g. 'lipid ratio') and assign each sample's value, then Confirm & build." — the example duplicates the key input's placeholder, the step list duplicates the h2 + value inputs + foot button. Distilled to WHY + one compressed action: "These samples share no ordering variable yet. Name one and give each a value." (also "tag key"→"ordering variable" to match the h2 / page vocabulary; dropped the redundant example + "then Confirm & build"). Both call-site doc comments updated. Updated the one cold-path test that pinned the old string (it renders the REAL cold plate, not a mock). Static copy → no jsdom/Chrome divergence risk, so the real-component test IS the rendering verification (cold path is router-state-seeded via contact-sheet compose, not URL-reachable for a manual probe). Build + full vitest 2881. |

### SeriesBuilderPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| BU-PROGRESS | confirm chain no visible progress + generic causeless error copy | clarify | P2 | **done `2bf1f1b`** | FIXED both halves. PROGRESS: derive a step-named busy label from the SAME reactive mutation states the chain reads — "Saving order…" (save) / "Publishing the figure…" (commit) / "Confirming…" (awaiting-fresh cache gap) — threaded as BuilderRail's new `confirmLabel`, replacing the static "Saving…". No change to the load-bearing stage REF or its single-fire effect gating (pure derivation; busy register still reverts in-render on every terminal path). ERRORS: `chainErrorMessage` branches save.error vs commit.error and feeds BOTH the inline role=alert and the assertive toast ("Couldn't save your changes…" vs "Your order was saved, but publishing the figure failed…"), each naming a recovery. 2 new tests (both phases + both causes); 5 pre-existing busy-revert tests updated to the step labels (revert semantics unchanged). Live-confirmed the draft→Save-enabled→Cancel round-trip on /series/2; the chain error/pending STATES can't be staged via client network interception (queue mutationFn races HTTP vs SSE own-op confirm → an intercepted request the server never sees hangs pending, same architecture that blocks staging the InfrastructureBanner) — new copy is conditional text off props, fully covered by jsdom render tests. Build + full vitest 2819 green. |
| BU-PICKER | add-sample is a ~130-option flat native select, duplicate display names, no search/grouping | clarify | P2 | **done `f05109a`** | FIXED: new AddSamplePicker — a search-first ARIA combobox in a Popover. Filter by name / smp_{id} / experiment; results grouped under experiment headers; every row carries its mono smp_{id} (duplicate display names distinguishable); focus stays in the input, Arrow keys move the active option (aria-activedescendant), Enter/click adds; stays open for multi-add (added sample drops out as recipe grows) + refocuses search; Escape/outside-click close (Popover). Popover gained additive `initialFocusRef` (search-first focus; default unchanged for other consumers); builder reads useExperiments for labels. 8 picker + 1 Popover test; 2 builder tests migrated select→picker. Live-verified /series/2: opens search-focused, 136 options across 3 experiment groups with smp_ids, all 3 filters, ArrowDown advances activedescendant, click-add starts draft (0→4 rows) + option leaves list + picker stays open. Build + full vitest 2836 green. |
| BU-NAMES | same member labeled three ways across one screen (figure "exp 42" / read list "Im3m · exp 6" / draft recipe sample name) | clarify | P2 | **done `dcd43cc`** | ✓re-score 2026-06-10. FIXED: each recipe row now shows the figure trace's token (memberRowLabel: "exp N" / label_override) as a muted suffix after the sample name, so recipe ↔ figure ↔ reading share ONE identity anchor. Bridge = sample → picker exposure → member (members are exposure-keyed, no sample_id), built outer + threaded BuilderBody→RecipeEditor→RecipeRow; no export-register change. Draft-only samples (not yet committed) get no token. 2 new tests. Live-verified /series/2: "LL37 Only · exp 20" etc. matching plate tokens. Build + vitest 2810 green. |
| BU-POLISH3 | re-score P3 cluster: inert read-state "Confirm series" styled like active "Adjust" · waterfall q-readout pointer-only + unnamed trace img · no help on offset/ticks/scale · no unsaved-draft navigate-away guard | craft+harden | P3 | todo | ✓re-score 2026-06-10 |

### FocusPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| FOL-RESCORE3-P3 | re-score #3 P3 cluster: Fig. N renumbers on sort/filter · dangling "· " separator when author null · error header asserts "0 SERIES" while count unknown · search accname from placeholder only · "Variable" sort label cryptic · mini-waterfall kisses right border | polish | P3 | **partly done `6166161` · rest GOLD-closed** | Gallery key={i} remount (FOL-RESCORE5) FIXED (`React.Children.toArray` + child-derived wrapper key → re-sort moves the wrapper, no card remount/figure-refetch; mount-counter test). Dangling '· ' author separator (FOL-RESCORE3) FIXED (footer gates the '· author' clause on a non-empty author). VERIFIED-ALREADY-CLOSED by the folio GOLD pass + FOL-MISC (no code): 'Showing N of M' live count (F2 role=status), error-state count=null-not-0, search haystack+accessible-name (`323b159`), filter-chip title+aria-describedby (`40011a0`/`36f542d`). Deferred minor: mini-waterfall right-border kiss, 'Has transition' coexistence-card nuance, CardFigure CLS height reserve. Build + full vitest 2898; live-verified /series. |
| FO-RESCORE2-P3 | re-score #2 P3 cluster: F9 rail-vs-comb notation drift · F10 8x8 peak targets · F11 no glyph legend + provenance absent from peak aria-labels · F12 aria-pressed dropped not "false" on disarm · F13 custom modal offers 5 of 8 phases · F14 static document title · F15 q-link hover-only · F16 wheel-zoom undiscoverable · F17 toolbar wrap at 1024 · F18 no skip link/sub-headings | polish | P3 | **F11/F12/F14 done `a17e8ec` · F17 already-done** | F12 FIXED: the `Button` primitive dropped `aria-pressed` when disarmed (`armed ? true : undefined`); removed the `armed=false` default + bind the raw value so a toggle reads "false" off / non-toggle carries none (APG). F11 (provenance half) FIXED: armed peak aria-label now "Auto/Manual peak at q = N (excluded)" (was glyph-only). F14 FIXED: Focus tab names the sample via the [[BU-RESCORE3-P3]] `useDocumentTitle` hook ("LL37 Only · Himalaya"). F17 was ALREADY done (ToolBar `flex-wrap`). 6 tests; live-verified /sample/3 tab + disarmed aria-pressed="false"; build + full vitest 2895. **Deferred:** F13 (custom modal 5-of-8 phases — adding Fm3m/Fd3m/Square is a PHYSICS call, needs the combs validated → saxs-physics reviewer / Jonathan), F11 glyph-LEGEND half (separate UI), F9 (rail/comb notation drift), F10 (8×8 peak targets), F15 (q-link hover-only), F16 (wheel-zoom undiscoverable), F18 (skip link / sub-headings — moderate a11y). |
| DI-FOCUSNOTE | candidate rail hint is a dense 2-sentence indexing-theory run-on | distill | P3 | todo | domain-guard: don't dumb down |
| FO-FOCUSRETURN | **Focus not returned to the invoker on dialog close / disarm** — CustomIndexModal Escape-close drops focus to `<body>` (no returnFocusRef to the "+ custom index…" trigger; WCAG 2.4.3 best practice, P2); Escape-disarm of "+ Peak" likewise lands on body (P3, lesser — the last-focused mark was unmounted). ONE shared `returnFocusRef` pattern closes both. | harden | P3 | **done `773a0ee`** | Focus re-score #4 2026-06-12 (the only residual after GOLD; non-blocking). FINDINGS: the two named sub-cases were already handled and re-verified LIVE — modal Escape-close returns to the "+ custom index" trigger (useFocusTrap prevFocus restore), and a plain disarm from the "+ Peak" button keeps focus there. The REAL gap was Escape-disarm while an armed-ONLY control (a peak mark, which loses tabIndex/role on disarm; or the add-at-q field, which unmounts) held focus → dropped to `<body>`. FIXED: the TracePlate Escape handler re-anchors to the "+ Peak" button when `document.activeElement` is inside `[data-role="plot-peaks"]` or is the add-at-q field; no-yank otherwise. 3 new TracePlate tests; live-verified the add-at-q branch on /sample/1; mark branch unit-covered. Build + vitest 2805 green. |

### LoupePage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| LO-POPKEY | single-letter page shortcuts still fire from non-input controls inside open non-modal Popovers (TagList suggestion button + x drops the frame) — the residual the aria-modal-only suppression design accepts | harden | P3 | todo | spec-review observation from LO-KEYD 2026-06-10 |
| LO-NEXT | no next/prev-SAMPLE navigation from the loupe — culling N samples = N contact-sheet roundtrips (arrows already flip frames; sheet roundtrip is the only sample step) | flow | P2 | **done `ac6a8eb`** | FIXED: the sheet hands the loupe its exact visible order (sorted + beamtime-filtered) via router state; the loupe walks it with a "‹ Prev / N of M / Next ›" header cluster (opposite Back, disabled at ends, shown only for a >1 walk) + "[" / "]" keys (arrows still flip frames; legend updated). Each step carries the order forward + drops ?exposure= (default frame). Direct-URL fallback = beamtime-scoped corpus order (= sheet default). 10 LoupePage tests. Live-verified on the real loupe: sheet→1/139 (Prev disabled), Next→2/139 "1-1 Only" (exposure param dropped), "]"→3/139, "["→2/139, order preserved. Build + full vitest 2846 green. |
| LO-FOCUSRET | Esc-return to the sheet drops focus to `<body>` — no focus restoration to the originating row/cell | harden | P2 | **done `c80c120`** | ✓Loupe re-score #1 · WCAG 2.4.3. FIXED: loupe goBack passes `state.focusSampleId`; SamplesPage resolves it to a 1-based row → SheetTable `focusOnMountRow` → new `autoFocusInitial` one-shot in useRovingGrid seeds + focuses that row's Sample cell on mount (through the grid, keeping roving state consistent). 2 new SheetTable tests. Live-verified on /samples?beamtime=4: loupe LL2 #245 → Escape → focus back on the "LL2" name button in row #245. Build + vitest 2807 green. |
| LO-TERM | "exposure" vs "frame" used interchangeably for the same object on one screen (kicker THIS EXPOSURE vs caption/keys "frame") | clarify | P2 | **done `5a40efc`** | ✓Loupe re-score #1. FIXED: standardized the loupe on "frame" (kicker "This frame", subtitle "frame N of M", Verdict "screen this frame", RepresentativeBox "One frame per sample"); the sheet's "Exposures" count column is a different screen, out of scope. Added `data-role="plate-subtitle"` testability hook. Tests updated + new subtitle assertion. Live-verified /samples/loupe/245: zero "exposure" word visible. Build + vitest 2808 green. |
| LO-MOBILE | **Loupe breaks ≤390px** (re-score #4 P3) — the detector hero canvas crushes to ~10×10px with horizontal scroll (429px in 390px); the two-column split is preserved instead of stacking. Mobile is not the target viewport (desktop scientific tool) but the break is real. Stack to single column below the tablet breakpoint | adapt | P3 | **done `4fdec59`** | FIXED. `LOUPE_BODY_GRID` was a fixed `grid-cols-[minmax(0,1fr)_286px]` with no breakpoint → the 286px panel + gap crushed the hero at phone widths. Added `grid-cols-1 md:` so it stacks to one column under the tablet breakpoint (md) and keeps the two-column split at tablet+ . The const is the skeleton↔real alignment invariant (EX-SPACING single-source) so both render paths move together. CSS-media-query change (no jsdom semantic — TY-DISPLAY precedent; live-verified by resize). At 390px: single column, detector hero **324×324** (was ~10×10), side panel stacks below. At 1024px: two columns 646px + 286px, hero 498px (desktop UNCHANGED). Build + full vitest 2881. Discovered residual → **TOP-CHIPMOBILE** (the topbar beamtime-chip select still overflows to 429px at 390px — separate cross-cutting chrome, not the loupe body). |
| TOP-CHIPMOBILE | the topbar `beamtime-chip` `<select>` (CorpusTopbar chrome, present on every page) overflows the viewport at phone widths — at 390px it sits left=253 right=430, forcing a 39px horizontal scroll (the residual behind [[LO-MOBILE]]'s "429px in 390px") | adapt | P3 | todo | Discovered during LO-MOBILE 2026-06-13. Cross-cutting (topbar, not a single page). Likely cap the chip's max-width / let it shrink-truncate at narrow widths, or wrap the topbar controls below the brand row under sm. Mobile is not the target viewport (desktop scientific tool) so P3. |
| LO-TAGPROV | **Resting loupe shows two byte-identical `dose=10` chips with no provenance** (re-score #4 raised as P1; **downgraded P3 — deferred per Jonathan**) — the fresh evaluator (unanchored) re-flagged the manual+scoping duplicate as a trust gap. But Jonathan already resolved this: editability dissolves it (the modal lets the user edit/delete to reconcile), and provenance-display + did-you-mean were explicitly deferred. A resting-state provenance marker (derived dot / merge) remains a possible later enhancement, not a defect | (deferred) | P3 | **SUPERSEDED — deferral rescinded 2026-06-12** | Jonathan re-raised duplicate tags as a must-fix in the GOLD-confirmation audit; the "editability dissolves it" rationale no longer holds (editing one of two `dose` rows yields two *values* for one key). Now tracked as [[TAG-DEDUP-MODEL]] (P1) + [[TAG-MANAGE-PROV]] (P2) in the 2026-06-12 audit section above |
| LO-EXPSPARSE | **"This exposure" `<dl>` renders a single `frame N of 8` row** (re-score #3 P2) — integration/collected correctly omitted (controls-don't-lie, `loupeAdapters.ts:63-71`) but the lone row reads as an unfinished section duplicating the caption/subtitle. Fold the position into the verdict block, or suppress the kicker until a second real row (e.g. a dropped frame's reason) exists | clarify | P2 | **done `f49b777`** | Loupe re-score #3 2026-06-11. FIXED (option B): LoupeSidePanel renders the block only when meta has >1 row; a normal frame suppresses it (position lives in subtitle + caption). Adapter unchanged. 2 new tests + LoupePage controls-don't-lie test updated. Live-verified /samples/loupe/245. Build + vitest 2813 green. |

## Iteration log

Moved to [loop-backlog-complete.md](loop-backlog-complete.md).
