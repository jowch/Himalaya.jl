# Production-polish goal — backlog & tracker

Durable state for the impeccable production-polish **goal** (native `/goal` harness since 2026-06-10; originally a `/loop` — filename kept for stable references). Companion to [2026-06-09-impeccable-design-loop.md](2026-06-09-impeccable-design-loop.md) (the command catalog, law checklist, severity machinery, per-surface map). Re-read both at the start of a session, then run iterations **BACK-TO-BACK within the turn**: after an item is committed and tracker-marked, immediately pick the next highest-severity open item and continue — do **NOT** end the turn between iterations. End the turn only to AskUserQuestion on a genuine product/scope decision, when hard-blocked, or when the GOLD finish table has been surfaced. _(Debugged with Jonathan 2026-06-10: the earlier "one iteration per turn" phrasing — a holdover from the /loop wake model — made the agent stop after every single item; that pacing rule is dead.)_

## goal-config (resolved with Jonathan 2026-06-09 — binding)

| Key | Value |
|---|---|
| **responsive_scope** | Desktop-first, **honest to ~1024px**. Fix `xl:`→`lg:` rail (Focus+Builder) + stack Loupe grid below a documented min-width. Sub-tablet (<~768px) out of scope; `<600px` scoping foot-row wrap item **dropped**. |
| **duplicate_export** | **Export-figure surfaced now** (P1, presentation/placement). **Duplicate-series deferred** out of this goal (separate `shape`-gated feature; no backend duplicate path today). |
| **finish_bar** | **GOLD / flagship.** Every surface **critique ≥ 36/40 AND audit ≥ 18/20** AND all P0/P1 closed. Genuinely-infeasible flagship items → mark `blocked`, surface, do not force. |
| **ordering_vars** | Scoping "Ordered by" dropdown is **data-driven** from the real manifest/adapter variable set + a "define your own" affordance. Verify against backend before wiring; do not hardcode the mockup's time/dose/temperature set. |
| **branch** | `worktree-greenfield-ui-rebuild` — STAYS UNMERGED. Never merge, never `finishing-a-development-branch`, never `git add -A`, every commit ends with the mandated co-author line. |

## Score trend (per surface — Wave 1 baseline 2026-06-09)

| Surface | critique /40 | audit /20 | trend | target | gap |
|---|---|---|---|---|---|
| SamplesPage (contact sheet) | **33** | **16** | 31 → 33 | ≥36 / ≥18 | +3 / +2 |
| SeriesFolioPage | **31** | **17** | 30 → 31 | ≥36 / ≥18 | +5 / +1 |
| SeriesScopingPage | **30** | **17** | 28 → 31 → 26 → 30 | ≥36 / ≥18 | +6 / +1 |
| SeriesBuilderPage | **32** | **17** | 27 → 32 | ≥36 / ≥18 | +4 / +1 |
| FocusPage | 24 | 16 | 24 | ≥36 / ≥18 | +12 / +2 |
| LoupePage | 30 | 15 | 30 | ≥36 / ≥18 | +6 / +3 |

**Scoping re-score #3 (2026-06-10, post SC-FLAG/SC-PARTIAL/SC-FOLD/SC-KBD): 31→26 / 17→15.** NOT a regression in landed work — the evaluator PRAISED the keyboard reorder, skip model, chain banner, and foot honesty — but the first evaluator to exercise the seeded-untagged flow (contact-sheet select of samples lacking the corpus's dominant key) and found an unrecoverable dead-end (SC-SEEDDEAD, new P1 ×2 same root cause) + an honesty cluster (preview ignores skips; duplicate-name candidates). Lesson recorded: a surface's score is bounded by its worst REACHABLE state, and each fresh evaluator probes deeper. Snapshot `2026-06-10T06-45-04Z__*seriesscopingpage*`. Re-scores (same single-evaluator method, fresh/unanchored): Scoping + Folio 2026-06-10 after their Wave-2 P1s — both rose, both still under GOLD; each re-score surfaced new P1s (SC-FLAG, SC-PARTIAL, UI-STRIP), merged below. Builder re-scored 2026-06-10 after all four Builder P1s (BU-RAIL/BU-DEAD/BU-FIELD/BU-EXPORT): 27→32 / 16→17, the largest rise so far; one new P1 (BU-WYSIWYG) + three P2s merged below (snapshot `2026-06-10T04-37-09Z__seriesbuilderpage-rescore.md`). All six below GOLD on both axes. No P0s anywhere. Baseline reads: theming + anti-patterns are 4/4 almost everywhere (design-guard working); **a11y is the universal drag** (a11y dim 2–3 on every surface). Snapshots persisted under `.impeccable/critique/` (impeccable-native trend; re-score in Wave 4 to show the rise). Slugs: SamplesPage=`imalayaui-…-samplespage-tsx`, Folio=`ayaui-…-seriesfoliopage-tsx`, Scoping=`aui-…-seriesscopingpage-tsx`, Builder=`aui-…-seriesbuilderpage-tsx`, Focus=`himalayaui-…-focuspage-tsx`, Loupe=`himalayaui-…-loupepage-tsx`.

**Note on faithfulness:** Wave-1 critiques ran a single capable evaluation agent per surface applying the real `critique.md`+`audit.md` rubrics over source + rendered screenshots (desktop 1440 + laptop 1120 for the rail/grid surfaces) + the SAXS `ignore.md`, rather than the two-fully-isolated-subagent design/detect split. The deterministic detector (`impeccable detect --json`) was run centrally on every page source and returned `[]` (zero anti-pattern tells) — that is the Assessment-B half, and it corroborates the 4/4 anti-patterns scores. Re-scores in Wave 4 use the same method for trend comparability.

## Wave status

- [x] **Wave 0 — Foundation refresh** — DONE (`c525c93`). DESIGN.md re-rooted to `src/print/ui`, stale two-theme/grain promises killed, four new sections (State taxonomy / Spacing & density / Motion / Copy & UX writing); PRODUCT.md a11y de-dark-themed + named persona seeded.
- [x] **Wave 1 — Scored baseline** — DONE (2026-06-09). SAXS `ignore.md` seeded; 6 surfaces rendered + scored (critique /40 + audit /20); snapshots persisted; new P1s merged below. No P0s.
- [ ] **Wave 2 — P1 fixes top-down** ← IN PROGRESS. ✅ F-LIVE (`c07cdce`). ✅ F-ERRSILENT (closed-by-architecture). ✅ F-CONTRAST (`275c421`). ✅ rail-adapt (FO-RAIL+BU-RAIL `6cee2a6`; FO-COMB closed-already-honest). **All cross-cutting P1s closed** + ✅ SC-HEAD (`32a11a4`) + ✅ F-A11Y contrast half (`046b440`, token-contrast regression guard). **Cross-cutting P1 work essentially complete** (F-A11Y's keyboard/aria half lives in the surface KEYD items). Surface-specific P1s: ✅ SA-SEM (`fa3eacc`) + ✅ FOL-KBD (`a100c2e`) + ✅ SC-FIELD (`21777a0`, order-Field accessible name) + ✅ SC-ORDER (`20a7b2b` + `99e1a8d`, data-driven dropdown + "Define your own…" inline-convert). **HARNESS = `/goal`** (switched from `/loop` mid-Wave-2; continuous evaluator-checked, no ScheduleWakeup). + ✅ UI-STRIP (`c0e0d80`, PhaseStrip role=img) + ✅ BU-FIELD (`65fbd64`) + ✅ BU-DEAD (`c9e7cd6`) + ✅ BU-EXPORT (`4d699f0`, ExportButton split on the plate head — **ALL Builder P1s closed**). + ✅ BU-WYSIWYG honest-state half (`14c6337`) + ✅ SC-PARTIAL (`efbe79a`, truthful chain-failure banner) + ✅ SC-FLAG (`7442b6f`, skip vocabulary + aria-pressed + constant accname) + ✅ SC-FOLD (`b6a9af7`, candidate preview cap + honest remainder note + fan-out cap) + ✅ SC-KBD (`0b5b1e9`, keyboard reorder — closed the original scoping P1 set; re-score #3 then surfaced SC-SEEDDEAD). + ✅ SC-SEEDDEAD (`27ef097`, seed-scoped vocabulary — scoping P1s closed AGAIN) + ✅ LO-KEYD (`ea73229`, shared `suppressGlobalKeys` guard across all 5 window keydown sites — also closes SA-KEYD's guard half; **ALL LOUPE P1s closed → Loupe re-score due**). + ✅ SA-KEYD (guard via `ea73229`; Enter half re-triaged to SA-THUMBKEY P2 — loupe-open was already keyboard-reachable via the SpecCell name button, pre-baseline `2806597`) + ✅ SA-RESP (`4f1809a`, shared scroller + sticky identity columns — **ALL SAMPLES P1s closed → Samples re-score due**). + ✅ F-STATE enforcement (`2edcf6c`, one base-layer focus-visible rule + 7 offset-0 redeclarations — **Wave 2 complete; every original P1 closed**). Loupe re-score #1 then surfaced **LO-REPDROP (P1, open)** — the re-score waterfall will keep minting these; close each before that surface's next re-score. Open lower: SC-COLD/SC-POLISH2/EM-DASH/UI-MENUKBD/FOL-EMPTY2/FA-SUNK/BU-PICKER/BU-ORDERINERT/BU-NAMES (P2), SC-CUSTSSE/BU-POLISH3 (P3). **Re-scores done 2026-06-10: Scoping 28→31/17, Folio 30→31/17, Builder 27→32/17** (all rose, all under GOLD; the gap is now P2-density + the new P1s, not the original drags). Samples re-score after SA-KEYD+SA-RESP.

> **✅ HARNESS STATUS (rebuilt 2026-06-09):** the dev backend died mid-session and its `/tmp/himalaya-uat/` DB copy was reclaimed; **now restored** — sourced a surviving persistent DB at `/Users/me/projects/himalaya-devdata/himalaya.db` (139 samples / 1 series / 3 experiments), `sqlite3 .backup`'d it to a disposable `/tmp/himalaya-uat/himalaya.db`, and restarted `env HIMALAYA_DB_PATH=/tmp/himalaya-uat/himalaya.db julia --project=packages/HimalayaUI -e 'using HimalayaUI; main(ARGS)' -- serve --port 8091` (from-source). `/api/samples` → HTTP 200. vite :5182 (VITE_API_PORT=8091) proxies to it; prod :8080 untouched. Render-verify is available. The canonical devdata DB stays pristine (we serve the copy). See [[feedback_live_audit_harness]].
- [ ] **Wave 3 — P2/P3 enhancement**
- [ ] **Wave 4 — Converge (`polish`) + re-score to GOLD**

## Backlog

Severity: P0 blocking · P1 major/any-WCAG-AA · P2 minor · P3 polish. Status: todo / in-progress / done / blocked.
`✓W1` = confirmed by the Wave-1 baseline. Prefer items that fix multiple surfaces at once.

### Cross-cutting (the system / multiple surfaces)

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| F-DOC | DESIGN.md/PRODUCT.md doc-debt | document+teach | P1 | **done** | `c525c93` Wave 0 |
| MO-DOC | motion vocabulary undocumented as a system | document+animate | P3 | **done** | `c525c93` Wave 0 Motion section |
| F-LIVE | **No `aria-live`/status-message on mutations & destructive keys** — Samples X-drop, Focus peak/phase/custom-index, Loupe X/R/flip, Builder confirm-progress. One polite+assertive live-region pattern fixes status feedback across 4 surfaces. | harden | P1 | **done** | `c07cdce` Wave 2 · new `lib/announce.ts`+`LiveRegion` SR-only announcer + assertive error toasts; visible-toast/SR-only split; 84 vitest + e2e 35 + build green |
| F-ERRSILENT | **Failed mutations fail silently** — Focus (`queries.ts:888` error read by nobody), Loupe, Builder; legacy of the cancelled conflict-UI decision. Surface plain-language error + retry. | harden | P1 | **closed (resolved-by-architecture)** | **Verify-before-review (Wave 2) found the baseline mis-cited.** Live surfaces mutate via `useQueueMutation`: 4xx→assertive validation toast w/ per-kind copy (`errors.ts:buildValidationMessage`, assertive via `c07cdce`); 5xx/network→auto-retry + `InfrastructureBanner` ("Saving…" >500ms → "Error. Couldn't save." + **Refresh** button >30s). Both asks (surface + retry) satisfied. Cited `queries.ts:888` "error read by nobody" = **retired comparison code** (pin/unpin + saveComparison, Compare retired #177) = dead/out-of-scope. Evidence-based (onError handler + InfrastructureBanner + queue contract tests); forced-error render-verify deferred as contrived. Linked P2s SA-RETRY/LO-ERR/BU-PROGRESS likewise mostly covered — re-triage in Wave 3. |
| F-CONTRAST | **`ink-faint` muted text fails AA (~3.16:1 on paper / 2.92 on paper-sunk)** — used for column headers, sample IDs, "Not indexed", kb legends across surfaces. Darken token to ≥L0.58 or restrict to large/decorative. | colorize | P1 | **done** | **Measured first** (oklch→linear-sRGB→luminance): ink-faint L0.640=**3.16:1** (AA-large only); ink-soft L0.467=6.50:1; ink=14.39. **Owner chose** (AskUserQuestion): keep ink-faint as tuned, reassign small-normal informational TEXT → `text-ink-soft`; leave kickers (AA-large), aria-hidden glyphs, icon controls (non-text 3:1), swatch fills, data-viz `var(--color-ink-faint)`. **50 files, ~72 swaps** + WCAG-context doc comments in styles.css. frontend-reviewer APPROVE (0 misclassifications); render-verified on Focus (captions/legends readable, kickers preserved). Commit `275c421` (gate: 2036 vitest + e2e 35 + build all green). |
| F-A11Y | a11y not operationalized: pin ink/accent/status contrast pairs as tests (like phase-on-plate); per-widget keyboard maps/aria | harden | P1 | **done (contrast half)** | `046b440` · new `test/contrast-tokens.test.ts` reads REAL `--color-*` from styles.css + asserts role floors (ink≥7 / ink-soft≥4.5 AA-normal / accent·success≥4.5 on paper·plate / ink-faint·warning≥3.0 large-non-text); shared `test/helpers/contrast.ts` (extracted from phases.test.ts). Reviewer empirically verified fail-on-regression. **Per-widget keyboard/aria half = the surface KEYD items (SA-KEYD, LO-KEYD, FOL-KBD)** — tracked there. |
| FA-SUNK | **accent (4.40) / success (4.46) dip just under AA-normal, and warning (≤3.66) + ink-faint (2.92) fail it, on `--color-paper-sunk`** (the rail bg) | colorize/audit | P2 | todo | surfaced by F-A11Y measurement · OK if non-text/large only — AUDIT that none render as <18px normal text on a paper-sunk surface; if they do, darken or restrict |
| UI-RINGCLIP | focus ring partially clipped on full-bleed buttons inside overflow scrollports — NavModal result items (left/right edges, `overflow-y-auto` list) + ThumbnailGallery/filmstrip thumbs (top/bottom, no scrollport padding). Net improvement over the off-brand UA ring, but the on-brand ring deserves to be whole: inset ring or scrollport padding | craft+harden | P2 | todo | spec-review follow-up from F-STATE 2026-06-10 |
| F-STATE | state taxonomy: doc-half done (`c525c93`); enforcement half (rest/hover/active/focus-visible/disabled/busy/selected/error/read-only consistently applied) | harden | P1 | **done** | `2edcf6c` · enforcement audit found ONE systemic gap: focus-visible. ~15 bespoke `<button>`/`<a>` sites rode the UA-default ring (loupe back, scoping Discard, NavModal items, thumbnails, SpecCell names, table status cells, RailBack, AutoGroup, AssignmentCart, StaleUrlPage, InfrastructureBanner). Fixed as ONE `@layer base` rule in styles.css (2px accent, offset 2) — cascade layers (base<components<utilities) preserve every refinement/suppression, EXCEPT layer override is PER-PROPERTY: the 7 1px-ghost sites (Button ghost/ghostInverse, Menu, StageTabs, Chip ×2, TagList) redeclare `outline-offset-0` (spec-review caught the offset leak from the emitted dist CSS). Other states audited clean: disabled routes through primitives (opacity-45) or AutoGroup's documented inert treatment; aria-pressed on toggles (Thumbnail/FlagButton); read-only = Field static branch; busy/error = boneyard/EmptyState/InfrastructureBanner per Honest-Surface-State. Pins: stylesheet-text unit test (contrast-tokens precedent) + real-browser e2e (Tab-walk → computed solid/2px/accent; mouse-click → none). Closes LO-FOCUS + SC-MISC's ring half. Live-verified at :5182 (loupe-back ring, thumbnail ring, Chip × 1px offset-0, mouse no-ring). spec-review APPROVE-WITH-NITS (offset leak, fixed) → frontend-reviewer APPROVE (utility names verified against installed Tailwind 4.2.4 + built CSS). Gate 2158 vitest (+3) + e2e 41/3 (+1) + build. Residual → UI-RINGCLIP (P2) |
| SC-HEAD | **Region labels are non-heading `div`s** (Kicker default) on Scoping (and Folio) — only h1 in the heading tree; pass `as="h2"/"h3"`. | harden | P1 | **done** | `32a11a4` · `Kicker` already polymorphic (no primitive change). ScopePlate 4 faint section labels → `as="h2"` (accent "New series" stays div = h1 eyebrow); SeriesCard title `<h3>`→`<h2>` fixing folio h1→h3 level-skip. Render-verified Folio live (`h1 Saved series → h2 Bundle A`, skip:none); ScopePlate covered by fail-on-revert semantic tests (live waived — invisible tag change, h2+.text-kicker ≡ div). frontend-reviewer APPROVE. |
| SC-FIELD | **Order `Field` trigger has no descriptive accessible name** — dropdown mode renders a bare `<button>` whose a11y name is just its current value (e.g. "LL37 : lipid ratio"); the visible "Ordered by" h2 is a sibling, not programmatically tied. Give the trigger an `aria-label`/`aria-labelledby` (or associate the "Ordered by" label). | harden | P1 | **done** | `21777a0` · NOT `aria-label` (would override content + DROP the value) — added optional **`srLabel`** prop to the `Field` primitive rendering an `sr-only` text prefix inside the trigger (+ static div), so the name composes to **"Ordered by {value}"** (label+value, WAI select-only-combobox pattern); chevron stays aria-hidden. ScopePlate passes `srLabel="Ordered by"`. Prop named `srLabel` not `ariaLabel` so it doesn't lie about setting an aria-label attr (rename done post-review). Additive (other consumers byte-identical), zero visual change. Tests assert composed name + the negative "value-never-dropped" case. frontend-reviewer APPROVE (confirmed real-browser name = "Ordered by {value}"). → follow-up **BU-FIELD**. |
| UI-STRIP | **PhaseStrip cells invisible to AT** — per-segment `aria-label`/`title` sit on role-less `<div>`s (ui/PhaseStrip.tsx); apply the house Semantic-Dot pattern (`role="img"` + label) per cell. Multi-surface: folio cards, scoping preview. WCAG 1.1.1/4.1.2 | harden | P1 | **done** | `c0e0d80` · role=img on all 3 segment branches + sr-only "to" beside the aria-hidden transition arrow. Spec-review SPEC-COMPLIANT + frontend-reviewer APPROVE (per-cell img RIGHT vs group label: member identity is the strip's point; img RIGHT vs presentation: title would resurrect exposure inconsistently). Live-verified on /series (cells expose as img Im3m/Pn3m/Lamellar/Unindexed). Gate: 2080 vitest + e2e 36 + build green |
| EM-DASH | **No-Em-Dash law violations in shipped print copy** — scoping grouping line/candidate row/footNote/ColdAssignSection caption, folio subtitle (`SeriesFolioPage.tsx:133`); survived the #278 sweep | clarify | P2 | **done** | `b05fcd2` · 11 prose strings swept (mid-dot for label pairs, comma/sentence-break for prose — all constructions on DESIGN.md's enumerated list; spec-review nit converted 4 semicolons to sentences); standalone `"—"` missing-value placeholders ruled a typographic convention, NOT copy (settled interpretation); mirroring stories + 4 matcher test files in lockstep; NEW pin `test/print-copy-no-emdash.test.ts` (comment-strips src/print non-story sources, exempts exact-"—" literals, asserts zero — fires on JSX text + interpolated template literals, silent on comments/placeholders, spec-review red-checked all four classifications). Live: scoping + folio render 0 em dashes. Gate 2185 vitest (+1) + e2e 42/3 + build |
| UI-MENUKBD | **Field/Menu menu-button keyboard pattern half-implemented** — open doesn't move focus into menu, ArrowDown on trigger inert, Escape only inside menu (Tab-reachable so 2.1.1 passes) | harden | P2 | **done** | `e84149c` · APG menu-button completed in the primitives: Menu mount-effect focuses active-else-first (initialFocus first/last); Field+ExportButton trigger arrows/Escape + focus-return on keyboard closes (outside-click deliberately not); Escape stopPropagation = innermost-popup containment (fixes a latent leak past suppressGlobalKeys + ModalShell double-close); IconButton→forwardRef; ExportButton parked-focus across the pending window (blur-on-disable repair, discriminating re-red proved the layout effect load-bearing). Gains scoping Ordered-by + builder field + both export menus. spec-review APPROVE (ratified active-not-first, stopPropagation, no-Home/End) → frontend-reviewer APPROVE (1 Low advisory → wiring contract documented). Live keyboard-driven on /series/new. Follow-up UI-MENUTAB P3. Gate 2212 vitest (+19) + e2e 42/3 + build |
| SC-CUSTSSE | **Custom ("define your own") worksheet doesn't track SSE corpus changes** — coldRows seed at sentinel-select; a concurrently deleted sample stays in the worksheet (backend rejects on commit, error banner — recoverable, consistent with LWW); whole-key deletion while in custom mode can clobber mid-edit coldRows via the cold-seed effect | harden | P3 | todo | surfaced by SC-ORDER commit-2 review (frontend-reviewer NIT) · multiplayer corner only |
| SC-COLD | **Cold-path Scoping screen is headingless** — when `isColdPath`, `ColdAssignPanel` renders in a bare Card with NO page `<h1>` and no headings (its "Name the ordering variable" label is a `div` Kicker). Warm path now has a sound tree; cold path doesn't. Give the cold path an h1 + section headings. | harden | P2 | todo | surfaced by SC-HEAD review · WCAG 1.3.1 · cold path only |
| EX-EMPTY | unify bespoke error/not-found divs (Samples, Focus, Loupe) onto EmptyState + action/retry slot | extract | P2 | **done** | `2b4382c` · EmptyState `action?: ReactNode` slot (primitive-owned spacing); 4 sites migrated (Samples reload-refetch disabled-while-fetching, Focus navigate door, Loupe goBack + Escape preserved, Folio try-again refetch + state-not-instruct body); old testids kept as pass-through (AppRoutes route-mount proof). Closes SA-RETRY + FO-ERR + FOL-ERR. Live: loupe not-found unified + action navigates; error branches JSDOM-covered (failure-state-only). Follow-up FO-NFHEAD P3. Gate 2193 vitest (+8) + e2e 42/3 + build |
| EX-SPACING | spacing/density scale underspecified; promote shared-grid/alignment constants to tokens | extract | P2 | todo | |
| FO-NFHEAD | Focus/Loupe not-found routes render EmptyState's h2 with no h1 anywhere (level-skip; the found branch's PlateHeader is h2 too so it's a route-structure condition, inherited not introduced by EX-EMPTY — prior code had ZERO headings there) | harden | P3 | todo | spec-review follow-up from EX-EMPTY 2026-06-10 |
| UI-MENUTAB | Menu doesn't close on Tab/focus-out (APG: Tab closes the menu) — also what makes the "trigger focused while menu open" state reachable; fix focus-out rather than special-casing arrows-on-open-trigger. Note: arrow-open/escape-return are JSDOM-only pins (e2e covers click paths) | harden | P3 | todo | spec-review follow-up from UI-MENUKBD 2026-06-10 |
| FO-ZOOMEDIT | zoomed + armed: off-window peak marks stay tabbable (clipped glyph AND clipped focus ring = invisible tab stops for sighted kb users); add-at-q validates the full data extent so an off-screen add succeeds invisibly until zoom-out. Pointer parity today (cannot click them either) | harden | P3 | todo | spec-review note from FO-EDIT 2026-06-10 |
| AN-MOTION | app essentially static: selection bars snap, drag drop-edge hard hairline, candidate-dim instant, no toast/modal motion | animate | P2 | todo | reduced-motion preserved |
| TY-DISPLAY | restore display-xl (31px) step (titles app-wide quieter than designed) | typeset | P3 | todo | reconcile w/ DESIGN.md display token |

### SamplesPage (contact sheet)

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| SA-SEM | **Non-semantic data table** — grid divs, no `role=table/row/columnheader/cell`, span headers, unlabeled thumbnail `<button>`s | harden | P1 | **done** | `fa3eacc` · ARIA roles layered on the existing two-grid divs (native `<table>` can't span them): SheetTable Card=`role=table`+`aria-label="Samples"`, sheet-head=rowgroup, header grid=row, 5 Kicker + checkbox-cell=columnheader; SampleTableRow outer=row, inner grid=`role=presentation` (flatten), 6 cells; Thumbnail button=computed `aria-label` ("Frame N[, representative][, dropped]")+`aria-pressed` when toggle. Render-verified /samples (role=table, 6 colheaders, 140 rows, 6 cells/row, labels OK, pixel-identical). frontend-reviewer APPROVE: body-rowgroup asymmetry left intentionally (rowgroup-wrapping-empty-msg would be invalid); `role=table` not `grid`. NOTE: header `checkboxColumn` + row `onCheck` must stay in sync (SamplesPage passes both). |
| SA-RESP | **Unbuilt responsive scroll** — row min-width ~1018–1054px overflows near 1024px; sticky-Sample `overflow-x` container the comments describe doesn't exist | adapt | P1 | **done** | `4f1809a` · live-verify found it WORSE than baselined: the Card's `overflow-hidden` silently CLIPPED the Status column at 1024 (data loss, not page scroll). Built the pattern the code's own comment promised: ONE shared `overflow-x-auto` scroller (header+rows together = alignment invariant), inner **`min-w-min`** wrapper (first cut was `min-w-max` → fr tracks absorbed an unwrapped flex-nowrap gallery, 3397px vs 1054 against the LIVE corpus — mocked e2e couldn't see it; e2e now pins a 12-exposure mock + scrollWidth<1200 + internal gallery scroll, red-checked), role=table moved off the Card for a contiguous ARIA chain (sheet-rows = rowgroup; empty slot outside the tree), sticky checkbox+Sample cells/headers (CHECKBOX_TRACK_PX single-sources track + offsets; opaque bg-plate/bg-paper-sunk mirroring row surface; NAMED group/row so Thumbnail's bare group-hover doesn't light every thumb on row hover — spec-review NEEDS-CHANGES catch). frontend-reviewer APPROVE. Live: scrollW exactly 1054, Sample frozen at 76.5 through full scroll, Status readable; 1440 unchanged (1159==1159). Gate 2150 vitest (+9) + e2e 39/3 (+2) + build. **ALL SAMPLES P1s closed → re-score due** |
| SA-KEYD | window keydown ("X" drops frames) guards only INPUT/TEXTAREA; no isContentEditable/open-modal check; no keyboard loupe-open (Enter) | harden | P1 | **done** | ✓W1 · announce half → F-LIVE · guard half closed in `ea73229` (LO-KEYD's shared `suppressGlobalKeys` on the Samples X/Escape handler + contenteditable/open-dialog tests). **Enter-loupe-open half re-triaged by evidence:** the Wave-1 snapshot itself rated it P2 ("no Enter on a focused thumbnail"), and loupe-open has been keyboard-reachable since `2806597` (2026-06-04, pre-baseline): SpecCell renders the sample name as a real `<button onClick={onOpenLoupe}>` — Enter/Space native. The thumbnail double-click is a redundant mouse shortcut to the SAME sample-level destination (its frame id is discarded: `onActivateExposure={() => navigate(loupeHref(s.id))}`), so WCAG 2.1.1 is satisfied; per-thumbnail parity split to SA-THUMBKEY (P2, the snapshot's own severity) |
| SA-RETRY | error div has no retry CTA | clarify | P2 | **done** | closed by EX-EMPTY `2b4382c` (reload control + busy-disabled) |
| SA-HDRCONTRAST | **SheetTable column headers fail AA** — 5 faint kickers at 11.5px ink-faint on the paper-sunk header band: measured 2.92:1 (re-score, = FA-SUNK's predicted audit hitting true here). NOT a re-litigation of F-CONTRAST: the owner's settled principle was "small-normal informational TEXT → ink-soft; decorative kickers stay faint" — column headers are load-bearing informational text on a sunk surface, so they qualify under the approved rule | colorize | P1 | **done** | `4535bd3` · new Kicker `tone="soft"` (`.text-kicker-soft` named class, ink-soft @ kicker geometry); ONLY the 5 sheet headers switched; styles.css "700-uppercase = large text" claim corrected (false at 11.5px; large-bold ≥18.66px; faint = decorative-on-paper only, soft = informational/any sunk surface). ink-soft-on-sunk already pinned at 6.0:1 in contrast-tokens.test.ts:82. Live: headers render oklch 0.467, hierarchy preserved |
| SA-CULLFOCUS | **CullBar applies aria-hidden while a child button holds focus** (Drop/Restore/Clear click → bar hides → focused node becomes aria-hidden; live console warning, WCAG 4.1.2). Move/blur focus before hiding (or use `inert`) | harden | P1 | **done** | `4535bd3` · CullBar + ComposeBar (same hazard) swapped to the **`inert` attribute** (ref + useLayoutEffect `toggleAttribute("inert", !show)` — React 18.3 has no inert prop; idempotent under StrictMode; first paint covered by layout-effect timing). Deletes aria-hidden + every per-Button tabIndex spread. inert natively blurs focused descendants — live-verified: mouse-click Drop → focus on BODY, zero warnings. e2e pin red-checked vs old code (focus assertion is load-bearing; console assertion comment-downgraded to belt-and-braces — headless Chrome doesn't surface the warning). **JSDOM ignores inert** → test/AGENTS.md section added; one role query scoped within(compose-bar). Ledger CullBar row updated (the B11 aria-hidden+tabindex gotcha is superseded by inert) |
| SA-PLURAL | CullBar reads "1 frames selected" (pluralization) | clarify | P2 | **done** | `4535bd3` · ternary matching ComposeBar's style; singular live-verified ("1 frame selected") |
| SA-UNDO2 | no immediate undo after batch Drop: toast reports but selection clears; recovery = re-select greyed frames then Restore | harden | P2 | todo | re-score #2 |
| SA-SCALE | dual bottom bars (frame-cull vs sample-check) conflatable + no in-page search/sort across 139 rows | clarify/shape | P2 | todo | re-score #2 · overlaps LA-COLLIDE |
| SA-THUMBKEY | thumbnail double-click→loupe has no per-thumbnail keyboard parity (Enter on a focused thumbnail = select-toggle, its native click). Function itself is keyboard-reachable via the SpecCell name button, so this is parity polish, not 2.1.1 | harden | P2 | todo | split from SA-KEYD 2026-06-10 (the Wave-1 snapshot's own P2 rating); note Enter-on-button = click, so a frame-level loupe key would need a different binding (e.g. Shift+Enter) or a frame-aware loupe route first (the current double-click discards the frame id) |
| DI-SAHEAD | head body is a long single tagging-philosophy paragraph | distill | P3 | todo | |
| ON-EMPTY | empty corpus dead-ends ("No samples yet") with no add-data/learn path | onboard | P3 | todo | |
| LA-COLLIDE | CullBar/ComposeBar can collide with bottom InfrastructureBanner | layout+animate | P2 | todo | |

### SeriesFolioPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| FOL-KBD | **Series cards not keyboard-operable** — `onClick` on `<article>`, no role/tabIndex/onKeyDown; primary navigation mouse-only | harden | P1 | **done** | `a100c2e` · inclusive card pattern (NOT whole-article-as-button, which would flatten the card's count/phase/provenance into one button name): the **title** becomes a native `<button>` inside the `<h2>` (Enter+Space, `stopPropagation` to avoid double-nav with the mouse-only `<article onClick>`), focus-visible outline verbatim from Button (token util, lint:design OK), plain-text when `onClick` absent. Render-verified: Enter on the focused title navigated /series→/series/1, title pixel-identical (19px/500/Newsreader), h2 preserved (SC-HEAD). frontend-reviewer APPROVE (mouse-only article is correct — function is keyboard-reachable via the title). |
| FOL-SORT | visible "SORT" label missing left of segmented control (mockup series-folio.html:326) | typeset | P2 | todo | |
| FOL-ERR | EmptyState used but no action passed on error | extract | P2 | **done** | closed by EX-EMPTY `2b4382c` ("Try again" refetch + reworded body) |
| FOL-MISC | search input unlabeled (P2, WCAG 3.3.2) + no on-page new-series CTA + indistinguishable empty drafts | harden+craft+clarify | P2 | todo | ✓W1 ✓re-score (accname rests on placeholder fallback) |
| FOL-EMPTY2 | re-score P2 cluster: truly-empty folio masquerades as filtered-empty ("Clear the search or filter" with no filter set — Honest-Surface-State violation); FolioCard ignores useSeries/useSeriesTraces errors (silent blank figure = "no data" misread); 2N detail-fetch fan-out per card (optimize, measure first) | clarify+harden | P2 | todo | ✓re-score 2026-06-10 · details in snapshot · P3s: Fig-N renumbers under sort, missing Plate-Lift on cards, empty footer-left slot, text-only empty states |

### SeriesScopingPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| SC-ORDER | **"Ordered by" renders as an editable input but is inert** (static Field, no options/onClick) — wire data-driven dropdown + custom + restore of-note copy | clarify | P1 | **done** | `20a7b2b` (commit 1) + `99e1a8d` (commit 2) · **Backend verified: NO manifest/adapter variable schema** — `ordering_variable` is free-form TEXT, variables = distinct `sample_tags` keys. Commit 1 = data-driven existing-key dropdown + `proposeOrdering(preferredKey?)` override + data-driven of-note. Commit 2 = decision A "Define your own…" sentinel (always-rendered dropdown, last entry) → inline-converts to ColdAssignPanel seeded with current members (Kicker/h1/order-Field escape hatch/caption/gated foot), SAME two-op chain; ColdAssignPanel gained `intro` prop (null suppresses the cold-corpus copy that would be false in custom mode); `canColdBuild` now requires ≥1 row (empty-series build hole found by frontend-reviewer). Render-verified live end-to-end (build wrote 6 dose tags + series 2). → follow-up SC-CUSTSSE (P3) |
| SC-KBD | **Drag-reorder has no keyboard alternative** (GripHandle aria-hidden; page never adds the path its primitive documents) | harden | P1 | **done** | `0b5b1e9` · ScopeSampleRow optional `onMoveBy?: (delta: -1\|1)` contract: real grip button (sr-only "Reorder {name}" accname + aria-keyshortcuts + FOL-KBD focus tokens) with ArrowUp/Down + preventDefault; modifier-key guard (Cmd/Ctrl/Alt arrows stay native — spec-review catch); no contract → visual-only grip, no dead control. Page moveRow converges with drag on ONE applyReorder; boundary presses announce "already first/last" (no dead air); persistent sr-only polite region "Moved {name} to position {i} of {n}." with LiveRegion trailing-space flip (raw-textContent regression test — toHaveTextContent normalizes whitespace and can't see it). Focus persists via sampleId keying (BOTH reviewers verified in real Chromium). Builder untouched (BU-ORDERINERT P2 adopts the same contract later); reorder-not-in-undo stays P3. New mocked e2e arrow-key test. spec-review APPROVE-WITH-NITS (all applied) → frontend-reviewer APPROVE (0 findings). Live-verified at :5182. Gate 2118 vitest (+13) + e2e 37/3 (+1) + build · **ALL SCOPING P1s CLOSED → re-score due** |
| SC-FLAG | **FlagButton lies three ways** — tooltip says "re-open this value" but click toggles skip; flagged caption "▸ check the read" vs foot "skipped"; accessible name is the bare value ("5"). One control, one vocabulary, one aria-label. WCAG 2.4.6/4.1.2 | clarify | P1 | **done** | `7442b6f` · skip vocabulary end-to-end: aria-pressed toggle + sr-only "Skip this read: " constant-accname prefix (Field srLabel pattern, NOT aria-label) + aria-hidden "▸ skipped" caption (mutation-verified anchored accname tests) + titles Skip/Restore. `flagged` prop + data-state KEPT as stable API/e2e keys. Spec-review caught the unanchored-regex vacuous pin + the ScopingAssembly story still simulating the retired flagged-blocks-build model → story now imports the real buildFootState, gates on kept===0; AutoGroup story "needs a look" swept. frontend-reviewer APPROVE. Live-verified /series/new (pressed + foot "2 ready · 1 skipped"). Gate 2098 vitest (+2) + e2e 36/3 + build |
| SC-PARTIAL | **Partial-chain failure misreports durable state** — Op A (tags) succeeded + Op B (create) failed still shows "Nothing was saved"; the tags WERE written. False statement about committed data | harden | P1 | **done** | `efbe79a` · banner copy derived from WHICH op failed (same div/testid/role=alert). Op-A copy avoids "Nothing was saved" (prior attempt's tags may persist); Op-B copy states the tags committed. scopeSeries.error checked FIRST (fresher attempt; stale createSeries.error can linger). Spec-review verified backend batch atomicity (single with_idempotency txn) + precedence trace; corrected a comment (retry = duplicate INSERT rows + first-match reads, NOT LWW). frontend-reviewer APPROVE (0 findings). Tests 25→27 incl. precedence pin. Live waived w/ note (failure-state-only). Gate 2096 vitest + e2e 36/3 + build. Follow-up P3 logged: stale banner CTA visible while a retry is in flight (suppress while scopeSeries.isPending) |
| SC-POLISH2 | re-score P2 cluster: no busy state on Confirm & build chain (→BU-PROGRESS pattern); "low to high" caption goes stale after manual drag; inert dotted-underline editable idiom on title (no onEditTitle); skip-only model can't correct a misread value (shape-gated); Discard/DYO-re-entry destroy typed work silently | polish/harden | P2 | todo | ✓re-score 2026-06-10 · details in snapshot |
| SC-FOLD | **Candidate "also found" list inverts the fold** over the 2-member series + build action | layout | P1 | **done** | `b6a9af7` · root cause = NO DATA CAP: unseeded /series/new rendered one row per loose candidate (133 rows / 8,077px live; build at y=9042) + eager trace fan-out over all 139. Fix: SCOPE_CANDIDATE_PREVIEW_COUNT=3 exemplar rows + ONE honest section note (computed remainder "…and 130 more lack the dose", pluralized, + real navigate("/samples") control) + fan-out capped to rows+visible (9 fetches, wire-verified). Also closes the snapshot P3 eager-fan-out + the P3 candidate-row-no-link nit. Spec-review APPROVE-WITH-NITS (discriminating 7-loose fixture so sliced-list remainder mutant fails; row-level e2e zero-button pin replacing name-based /add/i) → frontend-reviewer APPROVE. **Re-score render gotcha: page.goto to the same URL re-uses history.state — the Wave-2 evaluator carried seedSampleIds=[1,2,3] and missed this; clear with history.replaceState(null) before judging direct-visit states.** Gate 2105 vitest (+7) + e2e 36/3 + build |
| SC-SEEDDEAD | **Seeded-untagged selection is an unrecoverable dead-end** (re-score #3, two faces of one root cause): (a) contact-sheet seed of samples lacking the corpus's dominant key → "Series by dose · 0 samples", build gate unsatisfiable; (b) DYO from that state → zero-row custom worksheet, selection dropped | shape/harden | P1 | **done** | `27ef097` · `scopedTags` memo: direct visit = corpus pairs (byte-identical), seeded visit = seeded samples' OWN tags (first-match-per-key dedup, mirrors readers-take-first-match). Feeds BOTH proposeOrdering AND the orderKeys dropdown (the two freq maps can't diverge; dropdown can't offer a key the seed lacks). Seeded-no-shared-key → orderingKey undefined → the EXISTING cold-assign card fires (its designed purpose; effect's rows.length===0 guard verified incl. the warm→cold re-fire path). DYO carries whole selection ([...sorted, ...loose]) on seeded visits, members-only on direct (whole-corpus DYO must not fan in 100+ loose — pinned). spec-review APPROVE-WITH-NITS (stale comments + dedup, applied) → frontend-reviewer APPROVE (memo dep chain no-loop verified, structural sharing). 5 new tests (cold escape, seed-scoped key, dropdown honesty, mixed-seed DYO=4 rows, direct-DYO pin). Live-verified: 3 untagged samples → + New series → cold worksheet w/ all 3 rows. Gate 2123 vitest (+5) + e2e 37/3 + build |
| SC-PREVIEWSKIP | Preview strip ignores skips: foot says "5 ready · 1 skipped" but the phase preview still draws all 6 segments incl. the skipped sample | clarify | P2 | **done** | `37e6128` · new shared `isKept` predicate in scopingDerive.ts = THE commit-membership definition (payload/gate/counts/create-body/preview/story all derive through it — can never diverge again); preview filters `sorted` (displayed order, pinned by a keyboard-reorder test red-checked vs the rows-mutation); ScopePlate omits the whole preview section when nothing will be committed (zero-segment PhaseStrip paints an empty bar + orphaned "No clear phase"); committed-but-unindexed members still paint (no understatement during index loading — spec-review traced the loading path). spec-review APPROVE-WITH-NITS (vacuous order pin strengthened; stale test title) → frontend-reviewer APPROVE (0 findings). Live-verified at :5182 (skip → cell leaves strip + foot "3 ready · 1 skipped"; all-skip → section gone, foot warns; restore). Gate 2180 vitest (+9) + e2e 42/3 + build |
| SC-CANDID | Loose candidate rows carry no sample id; duplicate display names (member smp_5 "1-2 Only" vs candidate "1-2 Only") are indistinguishable and read as a contradiction | clarify | P2 | **done** | `04612bb` · mono `smp_{id}` sibling span beside the candidate name (member-row idiom, ScopeSampleRow:98 classes verbatim; inline not stacked so the dimmed rows don't grow); composite mirrors via optional `sampleId` prop; assembly story passes its smp_NN ids. Duplicate-name discrimination test red-checked. spec-review SPEC-COMPLIANT → frontend-reviewer APPROVE. Live: "1-2 Only smp_5" vs "1-2 Only smp_7" distinguishable, zero buttons. Gate 2184 vitest (+4) + e2e 42/3 + build |
| SC-PROV | "read from the sample names" provenance claim not obviously true (dose=5 on "HEPES Only") + header "6 samples" vs foot "5 · 1 skipped" diverge silently | clarify | P3 | todo | re-score #3 · re-confirmed #4 |
| SC-COLDHEAD | cold-scope worksheet drops the "New series" kicker + serif h1 the warm/custom plates carry (plate identity inconsistency; overlaps SC-COLD's heading gap) | craft | P2 | todo | re-score #4 |
| SC-PREVCLAIM | preview caption "Im3m throughout" overclaims when most preview cells are unindexed (e.g. 2/3 null) — caption should reflect the unindexed majority | clarify | P3 | todo | re-score #4 |
| SC-MISC | candidate rows dead-end (no inline tag-add) + ~~Discard no focus-visible ring~~ (closed by F-STATE `2edcf6c`) + candidate sparkline aria-hidden phase | craft+harden | P2 | todo | ✓W1 · ring half done |
| SC-RETRYBANNER | stale chain-failure banner (incl. its "Try again" CTA) stays visible while a retry is already in flight — suppress while scopeSeries.isPending/createSeries.isPending | harden | P3 | todo | spec-review follow-up from SC-PARTIAL 2026-06-10 |
| DI-SCOPECOLD | cold-panel stacked explanatory prose competes for one eye position | distill | P3 | todo | |

### SeriesBuilderPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| BU-DEAD | **Two inert controls in COMPOSE rail** — outline "+ Add sample" Button + collapse "›" chevron render live but get no handler; controls-don't-lie violation; duplicates working native select | harden | P1 | **done** | `c9e7cd6` · render-only-when-wired (the rail's onAdjust pattern); props stay on interface (story collapse demo works); Copy-as-PNG full-row when alone. Reviews: SPEC-COMPLIANT + APPROVE (judged hide CORRECT vs focus-the-select: two add affordances in one column is worse, and the button would focus nothing when addable=[]). Live-verified /series/2. Gate 2084 vitest + e2e 36 + build. Noted: onCopyPng is the one remaining can-render-inert prop (live page always wires it) |
| BU-FIELD | **Ordering-variable `Field` trigger has no descriptive accessible name** — same gap as SC-FIELD: BuilderRail's `<Field>` sits under a visible "Ordering variable" `RailSection` label that isn't programmatically tied; trigger a11y name is just the value. Pass `srLabel="Ordering variable"` (the `Field` `srLabel` prop already exists from SC-FIELD). | harden | P1 | **done** | `65fbd64` · one-prop mirror of SC-FIELD; tests cover both live modes (page=static, story=dropdown); spec-review SPEC-COMPLIANT + frontend-reviewer APPROVE; live-verified on /series/2 ("Ordering variable dose"). Gate 2082 vitest + e2e 36 + build |
| BU-RAIL | **Work·rail split gated at `xl:` (1280px)** drops rail below plate across 1024–1279 laptop band (Confirm/ordering/offset/export off-screen) | adapt | P1 | **done** | SeriesBuilderPage:367 `xl:`→`lg:`. Render-verified at 1120: COMPOSE rail sits beside the plate. frontend-reviewer APPROVE. Commit `6cee2a6` |
| BU-EXPORT | surface Export-figure to topbar (mockup series-builder.html:378) — builder exposes only rail "Copy as PNG" | clarify+layout | P1 | **done** | `4d699f0` · ExportButton split (Copy + PNG/SVG menu) via useFigureExport in a NEW SeriesPlate `actions` slot (TracePlate mirror, Focus precedent); mockup-topbar→plate-head adaptation (live topbar is the global shell; "the plate is the figure as it will export"). Rail copy-only duplicate removed (single-contextual-control, same reasoning as the plate-owned q-scale toggle) — closes BU-DEAD's onCopyPng can-render-inert follow-up. Spec-review caught the gate trap: toWaterfallRows pads EMPTY_TRACE so rows.length lies → WYSIWYG-honest `!rows.some(r => r.trace.q.length > 0)` (covers traces-loading/failed-fetch/empty series). frontend-reviewer APPROVE (0 findings). Live-verified /series/2: split on plate head, menu PNG/SVG, real download produced a clean-scientific figure. Gate 2091 vitest (+7) + e2e 36/3 + build |
| BU-PROGRESS | confirm chain no visible progress + generic causeless error copy | clarify | P2 | todo | → F-LIVE/F-ERRSILENT |
| BU-WYSIWYG | **Draft figure preview stale for membership edits** — plate renders committed members until Save→Commit while the title updates live and the rail caption promises "what you compose is what you publish" (SeriesBuilderPage.tsx:285,296; BuilderRail.tsx caption) | clarify/harden | P1 | **done (honest-state half)** | `14c6337` · treatment (b): draft-only plate subtitle ("Editing the recipe. Membership and order changes appear here after you confirm.") via the existing SeriesPlate subtitle slot + new BuilderRail `caption` prop (default = WYSIWYG sentence) overridden mid-draft. Spec-review tightened precision: only membership/order are stale (draft title + offset slider update live) — copy scopes its claim exactly. frontend-reviewer APPROVE. Render-verified both states /series/2. Gate 2095 vitest (+4) + e2e 36/3 + build. Treatment (a) live draft preview stays a logged PRODUCT DECISION (→ future-feature territory, not required for honesty) |
| BU-PICKER | add-sample is a ~130-option flat native select, duplicate display names, no search/grouping | clarify | P2 | todo | ✓re-score 2026-06-10 |
| BU-ORDERINERT | ordering-variable Field renders as a bordered input-like well but is static/inert in the builder (rail supports orderOptions/onOrderSelect; page passes neither) — either wire data-driven options like SC-ORDER or render as read-only text, not a control-shaped well | harden | P2 | todo | ✓re-score 2026-06-10 |
| BU-NAMES | same member labeled three ways across one screen (figure "exp 42" / read list "Im3m · exp 6" / draft recipe sample name) | clarify | P2 | todo | ✓re-score 2026-06-10 |
| BU-POLISH3 | re-score P3 cluster: inert read-state "Confirm series" styled like active "Adjust" · waterfall q-readout pointer-only + unnamed trace img · no help on offset/ticks/scale · no unsaved-draft navigate-away guard | craft+harden | P3 | todo | ✓re-score 2026-06-10 |

### FocusPage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| FO-COMB | **Comb/reflections panel `hidden lg:flex` drops at <1024px laptop band** (stack under detector, don't hide a primary analysis tool) | adapt | P1 | **closed (already honest)** | Verify-before-review: the comb wrapper is `hidden lg:flex` = visible at **≥1024**, so it's present across the whole in-scope band; the "<1024" drop is BELOW the ~1024 floor (out of scope, decision #1). Render-verified at 1024: Detector\|Comb side-by-side, compact but functional — no change needed. |
| FO-RAIL | work·rail split gated at `xl:` — confirm whether the assignment rail (not just comb) also drops; fix WITH BU-RAIL | adapt | P1 | **done** | Confirmed: FocusPage:419 (live) + :68 (fixture) work·rail grid was `xl:grid-cols-[1fr_350px]` → `lg:`. Render-verified at 1120 + 1024: ASSIGNMENT/CANDIDATES rail sits beside work; inner Detector\|Comb split left at `lg:` (honest at floor). Commit `6cee2a6` |
| FO-EDIT | trace-edit model undiscoverable (arm "+Peak"/click=add/click-peak=remove/alt-click=exclude) + no keyboard accelerators / plot not keyboard-operable + generic detector canvas label | onboard+harden | P2 | **done** | `45c9d42` · OWNER DECISION (AskUserQuestion): discoverability + minimal keyboard; roving-focus/peak-move stays M4-deferred. Armed-only per-verb hint (composed from wired verbs — spec-review MEDIUM catch: an onAddPeak-only hint promising "click a peak to remove" would actually duplicate-add via fall-through); armed marks = role=button + "Peak at q = X.XXXX" + Enter/Space remove + Alt+Enter exclude (repeat-guarded, Alt-on-Enter-only) — the old inert role-from-onPeakFocus lie removed; add-at-q ui/Input (QNumInput legacy-only, no-port) extent-validated, draft cleared on disarm; SVG focus ring scoped in styles.css (data-observed-hit precedent). Live kb round-trip: add q=0.21 → real peak → Enter removes + announces; net-zero DB. Follow-ups FO-ZOOMEDIT P3 (logged). Gate 2226 vitest (+14) + e2e 42/3 + build |
| FO-ERR | bespoke "Sample not found" instead of EmptyState | clarify+extract | P2 | **done** | closed by EX-EMPTY `2b4382c` (unified + "Back to the contact sheet" door) |
| FO-DIM | candidate-hover losing-peak dim instant; ~~drag drop-edge hard hairline~~ (re-scoped: Focus has NO drag today — the drop-edge hairlines live on scoping/builder reorder → AN-MOTION) | animate | P2 | **done** | `df47fa4` · zero-new-CSS fix: peak paint routed through CSS color + currentColor so the EXISTING global 120ms ease-out color transition animates the dim; reduced-motion free from the global 0.01ms rule; paper halo stays literal; export untouched by construction (detached SVG, resolved strings). spec-review APPROVE (glyph matrix complete, export-leak clean, broader assigned-color-ease judged consistent w/ motion-conveys-state) → frontend-reviewer APPROVE (q-line stroke pin added). Live: hover Lamellar on /sample/2 dims 11 groups, computed transition color 0.12s ease-out. Gate 2212 vitest + e2e 42/3 + build |
| DI-FOCUSNOTE | candidate rail hint is a dense 2-sentence indexing-theory run-on | distill | P3 | todo | domain-guard: don't dumb down |

### LoupePage

| id | opportunity | command | sev | status | notes |
|---|---|---|---|---|---|
| LO-KEYD | **Window keydown (X/R/arrows/Esc) guard incomplete** (no isContentEditable/open-popover) + no aria-live announcement | harden | P1 | **done** | `ea73229` · new shared `lib/keys.ts` `suppressGlobalKeys(e)` (typing targets INPUT/TEXTAREA/SELECT/contenteditable w/ JSDOM closest-fallback, OR any open `[role=dialog][aria-modal=true]` — document-level on purpose: aria-modal inert-outside + focus-trap race) applied at ALL FIVE window keydown sites (Loupe, Samples, useGlobalShortcuts, scoping ⌘Z, StaleUrlPage). Loupe also gained the meta/ctrl/alt guard (⌘R was firing set-representative, ⌘X was dropping — worse than baselined); Escape no longer navigates behind an open modal. Scoping ⌘Z guard sits BEFORE preventDefault so native text-undo in inputs survives (real adjacent bug, same class). Announce half was ALREADY closed by F-LIVE (flip announces "Frame N of M"; drop/restore/representative toast w/ aria-live). spec-review APPROVE-WITH-NITS (story drift + ⌘K pass-through pin, applied) → frontend-reviewer APPROVE (1 test-type advisory, applied). Live-verified at :5182 (chords inert, x-behind-modal inert, Escape single-action, flip/drop/back intact). Gate 2141 vitest (+18) + e2e 37/3 + build. Residual logged → LO-POPKEY (P3) |
| LO-FOCUS | "Back to the sheet" bare buttons rely on UA-default focus, not a focus-visible ring | harden | P2 | **done** | closed by F-STATE `2edcf6c` (base-layer rule; loupe-back is the e2e pin's subject + live-verified) |
| LO-THUMB | filmstrip thumbnails have no accessible frame identity (every one reads "Detector image, button"); add aria-label + aria-current/pressed | harden | P2 | todo | ✓W1 · WCAG 4.1.2 |
| LO-ERR | no failure feedback on drop/representative/tag mutations | harden | P2 | todo | ✓W1 · → F-ERRSILENT |
| LO-GRID | grid `[minmax(0,1fr)_286px]` never stacks — **honest at 1024px+ (in scope), rigid below** | adapt | P2 | todo | ✓W1 DOWNGRADED from P1 (in-scope OK) |
| LO-POPKEY | single-letter page shortcuts still fire from non-input controls inside open non-modal Popovers (TagList suggestion button + x drops the frame) — the residual the aria-modal-only suppression design accepts | harden | P3 | todo | spec-review observation from LO-KEYD 2026-06-10 |
| LO-REPDROP | **Dropped frame silently remains the representative for indexing** — set rep on frame 2, press x: hero "Dropped", panel "Dropped — Restore" sits directly above an unchanged accent "Representative for indexing" box; aria reads "representative, dropped"; nothing warns or prompts a reassign, so the sample carries a discarded frame to Index | clarify+harden | P1 | **done** | `f5054e1` · verify-first confirmed the backend truth (comparisons.jl:405 + events.jl:977: highest-id selected wins, status NEVER consulted — the dropped rep genuinely carries to Index). RepresentativeBox `representativeDropped` prop: sample-level warning on EVERY frame ("Warning. The representative frame is dropped. It still carries to the Index stage. Restore it or set another frame as representative." — severity-as-word in ink, small warning-hue text fails AA on plate; copy names both live remedies, spec-review traced each). Predicate `exposures.some(e => e.selected && e.status === "rejected")` is safe under the exactly-one-selected invariant (zero-all-then-set transactional, routes_exposures.jl:139). spec-review APPROVE-WITH-NITS (3 cosmetic, applied incl. strongest-state story) → frontend-reviewer NEEDS-CHANGES caught REAL focus loss (optimistic flip unmounts the still-focused set button → focus to body) → fixed: tabIndex=-1 wrapper + useLayoutEffect gated on the button's OWN activation (flag self-consumes every transition so foreign/SSE flips never yank focus; negative test pins it) → APPROVE (StrictMode-safe: prevIsRep init-to-current). Live: drop→warning, flip→persists+remedy button, restore→clears. Gate 2171 vitest (+13) + e2e 42/3 (+1) + build |
| LO-REPLIES | "Set as representative" stays live on the CURRENT representative and fires a success toast for the no-op — controls-don't-lie + false feedback | harden | P2 | **done** | `f5054e1` · button OMITTED on the representative (removed-not-disabled house rule); keyboard R guard announces "Already the representative frame", no mutation, no false toast; 3 existing tests that encoded the lie updated to flip off the rep first (incl. re-arming the ⌘R chord test against vacuous pass) |
| LO-NEXT | no next/prev-SAMPLE navigation from the loupe — culling N samples = N contact-sheet roundtrips (arrows already flip frames; sheet roundtrip is the only sample step) | flow | P2 | todo | ✓Loupe re-score #1 |
| LO-FOCUSRET | Esc-return to the sheet drops focus to `<body>` — no focus restoration to the originating row/cell | harden | P2 | todo | ✓Loupe re-score #1 · WCAG 2.4.3 |
| LO-TERM | "exposure" vs "frame" used interchangeably for the same object on one screen (kicker THIS EXPOSURE vs caption/keys "frame") | clarify | P2 | todo | ✓Loupe re-score #1 |
| LO-POLISH3 | re-score P3 cluster: tag editor no autofocus + silent empty-key rejection + no removal undo + 7×15px remove targets · Esc with editor open-unfocused exits the page · dead "integration —"/"collected —" rows · no h1 · generic "Detector image" canvas labels everywhere | craft+harden | P3 | todo | ✓Loupe re-score #1 · doc-title half → TY-DISPLAY |

## Iteration log

- _2026-06-10 — **FO-EDIT done (`45c9d42`) — the last substantive Focus P2; the surface's 2.1.1 gap is closed at the owner-decided scope.** AskUserQuestion settled the fork (full keyboard model vs minimal vs blocked-on-M4): Jonathan chose discoverability + minimal keyboard. The implementer's build survived a three-round review: spec-review traced the armed gate end-to-end through TracePlate→TracePlot→PlotPeaks (disarmed marks are roleless because the HANDLER is absent, not just unused) and caught the per-verb hint MEDIUM; frontend-reviewer added the key-repeat guard + Alt-scope micro-hardening (orchestrator-applied with discriminating pins). Notable verify-before-review artifacts: QNumInput does NOT exist in this tree (legacy-only — the no-port rule held); the live add-at-q drive initially looked failed but the peak HAD landed (mutation roundtrip outran the probe window) — the follow-up a11y-tree read showed "Peak at q = 0.2100" and Enter removed it net-zero. Gate: lint:design + tsc + vitest 2226 (+14) + e2e 42/3 + build. Next: Focus re-score #1 (F-LIVE/F-CONTRAST/FO-RAIL/F-STATE/EX-EMPTY/UI-MENUKBD/FO-DIM/FO-EDIT have all landed on Focus since its 24/16 baseline)._

- _2026-06-10 — **FO-DIM done (`df47fa4`) — first Focus-band item; also a verify-before-review catch.** The tracked item claimed a Focus "drag drop-edge hard hairline" — Focus has NO drag affordance at all today (greped page + rail + cart); that half re-scoped to AN-MOTION where the actual reorder drop edges live (scoping/builder). The dim fix is the rare zero-new-CSS animate: fill/stroke were simply missing from the global transition property list, so routing peak paint through CSS color + currentColor (inline style.color on the per-peak <g> that already carries data-dimmed) put the dim on the house 120ms ease-out rule, with reduced-motion inherited from the global !important zeroing. Spec-review traced the full glyph matrix (filled/hollow/caret/hot + paper-halo exception) and proved the figure-export path cannot receive currentColor (detached SVG, resolved literals); also ratified the broader effect (assigned-index recolors now ease too — motion-conveys-state, and dim-only scoping would snap the RELEASE since the attribute is gone before a [data-dimmed] rule could animate it). Live: hover dims 11 groups w/ computed 0.12s color transition. Gate: lint:design + tsc + vitest 2212 + e2e 42/3 + build. Next: FO-EDIT (the big Focus discoverability/keyboard item), then Focus re-score #1._

- _2026-06-10 — **UI-MENUKBD done (`e84149c`) — the menu-button pattern finished in the PRIMITIVES, so all four consumer sites gained it at once.** Spec-review ratified the three judgment calls (focus the ACTIVE item not literal-first for value-bearing fields; Escape stopPropagation as innermost-popup containment — traced every window/document keydown listener to prove nothing relied on the old leak, and it protects ModalShell whose Escape listener has no suppression guard; Home/End omitted on 2-4-item menus) and caught the real-browser blur-on-disable divergence on ExportButton's pending window — fixed with the parked-focus pattern (self-consuming own-activation flag + layout effect; the JSDOM-blur()-is-a-no-op gotcha is documented in the test). frontend-reviewer APPROVE (advisory → wiring contract doc on the props). Live keyboard drive: ArrowDown on the scoping Ordered-by opens focused on "dose" (data-active) with the 1px ghost ring; Escape returns focus to the trigger. e2e covers menu CLICK paths only — arrow-open/escape-return are JSDOM pins (noted in UI-MENUTAB). Gate: lint:design + tsc + vitest 2212 (+19) + e2e 42/3 + build. Next: FO-DIM then FO-EDIT (the Focus band — Focus is the furthest surface from GOLD and untouched by re-scores so far)._

- _2026-06-10 — **EX-EMPTY done (`2b4382c`) — one extract, four surfaces, three sibling rows closed.** EmptyState grew the action slot the folio error always needed; the three bespoke bordered divs (Samples error, Focus/Loupe not-found) migrated onto it, deleting their one-off appearance. Error copy now states what happened and the control offers the way forward (retryable: refetch + busy-disabled; not-found: a navigation door). Spec-review APPROVE-WITH-NITS (busy-disabled applied with red-first tests; heading follow-up FO-NFHEAD P3 logged) -> frontend-reviewer APPROVE (0 findings; verified mock-shape additions don't leak into sibling specs, useNavigate has Router context everywhere). Live: loupe not-found renders the unified state and its action lands on /samples; Focus not-found is NOT live-reachable (slug fallback routes garbage to sample 1 by design) so JSDOM + AppRoutes pins carry it. Render-verify note: /sample/:slug falls back rather than 404ing, so the not-found branch needs the AppRoutes mount-proof, not a URL probe. Gate: lint:design + tsc + vitest 2193 (+8) + e2e 42/3 + build. Next: UI-MENUKBD (a11y, multi-surface Field/Menu keyboard pattern)._

- _2026-06-10 — **EM-DASH done (`b05fcd2`) — the cross-cutting copy-law sweep.** 11 prose strings across 9 src/print files (the comment-stripped scan found the tracker's list undercounted — AssignmentCart, CombsPanel default label, BuilderRail rail label, ScopePlate preview kicker, SamplesPage head, the dev-shell h1 all carried them too). Constructions chosen per case from DESIGN.md's enumerated list; the spec-review's letter-of-the-law nit (4 semicolons → two-sentence form) applied so future evaluators can't dock Consistency against the documented rule. Standalone "—" placeholder literals (`?? "—"`, dead-metadata rows) ruled IN-scope-exempt: typographic missing-value convention, not prose copy — recorded here as the settled interpretation. New regression pin (file-reading guard pattern) keeps src/print at zero; "--" deliberately unchecked (CSS custom-property names). Live: scoping AND folio body text render 0 em dashes. Cadence: implementer → spec-review SPEC-COMPLIANT (independent completeness scan; pin red-checked 4-way) → frontend-reviewer APPROVE (0 findings). Gate: lint:design + tsc + vitest 2185 (+1) + e2e 42/3 + build. Next: EX-EMPTY (multi-surface error-state unification: closes FOL-ERR + SA-RETRY + FO-ERR)._
- _2026-06-10 — **Scoping re-score #4: 26→30 critique / 15→17 audit** (fresh unanchored evaluator, same method; snapshot `2026-06-10T19-20-00Z__aui-…-seriesscopingpage-tsx.md`). The honesty pair held: evaluator independently verified the seeded-untagged dead-end now routes to the cold worksheet, the preview drops skipped members, and candidates show smp ids — "no reachable dead-end remains". **ZERO P0/P1.** Heuristics: aesthetic 4, consistency 2 (em-dashes + cold-plate header drop), status 3 (no busy state on the build chain; stale "low to high"), rest 3s. Dims: theming 4 · anti-patterns 4 · a11y/perf/responsive 3. AI-slop PASS, cognitive load low. The GOLD gap (+6/+1) is now entirely the P2 band: EM-DASH (4 strings, cost a Consistency point), SC-POLISH2's busy-state + stale-caption halves, SC-COLDHEAD (new P2: cold plate drops kicker+h1), P3s SC-PREVCLAIM/SC-PROV re-confirmed. Evaluator wrote nothing to the DB. Next: EM-DASH sweep (cross-cutting, also flagged on Folio)._
- _2026-06-10 — **SC-CANDID done (`04612bb`) — both Scoping honesty P2s landed; Scoping re-score #4 due NOW.** Candidate rows gained the member-row mono smp identity (inline sibling span, not a third stacked line — dimmed informational rows keep their height; classes verbatim from ScopeSampleRow:98 so the identity vocabulary is one thing). The composite mirrored trivially (optional prop) and the assembly story passes its real smp_NN ids (the spec-review's one optional nit, applied by the orchestrator). Live verify hit the exact baselined contradiction: candidates "1-2 Only smp_5" vs "1-2 Only smp_7" beside member rows, now distinguishable. Pacing note: this iteration ran back-to-back with SC-PREVIEWSKIP in ONE turn under the debugged chain rule (`23ae6d1`). Gate: lint:design + tsc + vitest 2184 (+4) + e2e 42/3 + build. Next: Scoping re-score #4._
- _2026-06-10 — **SC-PREVIEWSKIP done (`37e6128`) — first of the two Scoping honesty P2s gating re-score #4.** The strip previewed a series that would never be built. Root treatment = predicate unification: new `isKept` in scopingDerive.ts is THE commit-membership definition; payload/gate/counts/create-body/preview/story all derive through it (the page had FOUR inline copies of `include && !flagged && value !== ""`). Preview filters `sorted` so it tracks displayed order — the spec-review's one real catch was that the order claim was vacuously pinned (seed3's picker order coincides with value-sorted order); strengthened via the SC-KBD grip harness + red-checked against the `rows.filter` mutation (exact failure captured). All-skipped omits the section entirely (zero-segment PhaseStrip paints an empty bar + orphaned "No clear phase" — checked, not assumed); foot's warn carries the message; committed-but-unindexed members still paint as Unindexed cells (spec-review traced the index-loading path: section never hides while queries are in flight). Harness rebuilt this turn (fresh disposable DB copy from canonical devdata → :8091; vite :5182; dose tags seeded onto smp 1–4 — note the batch-tag POST 500s from curl, unresolved, seeded via sqlite3 instead; backlog if it recurs from the UI). frontend-reviewer APPROVE (0 findings; Pick-structural-signature + story include:true + e2e/bones blast radius all judged clean). Gate: lint:design + tsc + vitest 2180 (+9) + e2e 42/3 + build. Next: SC-CANDID (last Scoping honesty P2 before re-score #4)._
- _2026-06-10 — **LO-REPDROP + LO-REPLIES done (`f5054e1`) — the re-score's new P1 closed; ZERO open P1s again.** Verify-first confirmed the evaluator's claim at the backend: both indexing-exposure resolvers (comparisons.jl:445-480, events.jl:977-984) are status-blind, so a dropped representative truly carries to Index — the warning copy's claim is literally true in every reachable state (exactly-one-selected is transactionally enforced). The honest-surface treatment: sample-level `representativeDropped` warning in RepresentativeBox on every frame (severity-as-word "Warning." in ink per the Toast pattern — NOT --color-warning small text, 3.66:1 fails AA on plate), set-button omitted on the current rep (controls-don't-lie), R-guard announces instead of false-success-toasting. Cadence earned its keep TWICE: spec-review verified all 7 hard items with file:line evidence (predicate boolean-safety via routes_exposures.jl bool_keys; both copy remedies traced to live affordances; red-checked the e2e by neutering the predicate) and frontend-reviewer caught a REAL focus loss (the optimistic flip unmounts the still-focused button → body) — fixed with the parked-focus pattern (tabIndex=-1 wrapper, useLayoutEffect, activation-gated flag that self-consumes so SSE flips never yank focus; positive + negative tests red-checked; Card primitive untouched — no forwardRef in React 18.3, wrapper has zero classes so the look is byte-identical). Live at :5182: drop→warning, frame-flip→warning persists with the remedy button, restore→clears, DB state restored. Gate: lint:design + tsc + vitest 2171 (+13) + e2e 42/3 (+1) + build. Next: Scoping honesty P2s (SC-PREVIEWSKIP + SC-CANDID) before its re-score #4._
- _2026-06-10 — **Loupe re-score #1: 30→30 / 15→17** (fresh unanchored evaluator; snapshot `frontend/.impeccable/critique/2026-06-10T12-00-00Z__loupe-rescore.md` — note path drift from the root `.impeccable/`). The landed a11y work held and lifted the audit (a11y 3: focus outlines, aria-pressed thumbs, live region, logical tab order all credited; perf 4 zero-console-errors; anti-patterns 4 "zero AI tells, distinctive editorial print aesthetic"). Critique flat at 30 because the evaluator found a NEW P1 deeper in: **LO-REPDROP** (dropped frame silently remains the representative for indexing — honest-surface gap, the panel shows "Dropped — Restore" directly above an unchanged accent "Representative" box) + P2s LO-REPLIES (no-op rep click fires a success toast), LO-NEXT (no sample-step from the loupe), LO-FOCUSRET (Esc-return focus to body), LO-TERM (exposure/frame drift), P3 cluster LO-POLISH3. Kicker-contrast finding = the SETTLED F-CONTRAST owner decision → now recorded in `.impeccable/critique/ignore.md` (decorative paper-kicker entry) so evaluators stop re-flagging it; also noted the evaluator was pointed at a frontend-relative ignore path — future re-score briefs must cite the ROOT `.impeccable/critique/ignore.md`. Next: LO-REPDROP (the only open P1)._
- _2026-06-10 — **Wave 2 / F-STATE enforcement done (`2edcf6c`) — THE LAST P1; Wave 2 COMPLETE.** The state-taxonomy enforcement sweep (DESIGN.md:234-248) found one systemic gap: focus-visible. ~15 bespoke `<button>`/`<a>` sites rode the UA-default ring; everything else audited clean (disabled→primitives/documented AutoGroup inert treatment, aria-pressed→Thumbnail/FlagButton, read-only→Field static branch, busy/error→Honest-Surface-State machinery). Fix = ONE `@layer base` rule (`button:focus-visible, a:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }`) — the same close-the-class shape as suppressGlobalKeys/inert. Cascade-layer order (base<components<utilities) preserves every refinement and suppression, with ONE proven exception spec-review caught from the emitted dist CSS: layer override is PER-PROPERTY, so `outline-1` refiners inherited the 2px offset (the shipped ghost look is 1px offset-0, and on Chip/TagList × glyphs the floated ring crossed the chip border) — the 7 1px sites now redeclare `focus-visible:outline-offset-0`. Pins: stylesheet-text unit test (brace-counted @layer base extraction, contrast-tokens precedent; red-checked) + real-browser e2e (Tab-walk to loupe-back → computed solid/2px/accent; mouse-click → none; red-checked against a neutralized selector; discriminates :focus-visible vs :focus because Chromium focuses buttons on mousedown). Cadence: implementer → spec-review APPROVE-WITH-NITS (built the bundle and verified layer offsets empirically; found the offset leak + filed ring-clipping; confirmed Tab-walk determinism and pin brittleness acceptable) → offset fix → frontend-reviewer APPROVE (verified `outline-offset-0` is a real Tailwind 4.2.4 utility emitting `outline-offset:0px` in the build; exhaustive grep = zero missed 1px sites). Live at :5182: loupe-back + thumbnail keyboard rings solid 2px oklch(0.555 0.15 38) offset 2px, Chip × ring 1px offset 0, mouse click `:focus-visible` false/outline none. Closes LO-FOCUS (P2) + SC-MISC's ring half. Residual → UI-RINGCLIP (P2): ring partially clipped on full-bleed buttons in overflow scrollports (NavModal items, filmstrip). Gate: lint:design + tsc + vitest 2158 (+3) + e2e 41/3 (+1) + build. **Every P0/P1 ever filed in this tracker is now closed → re-score waterfall begins (Loupe + Samples #3 due; Scoping needs SC-PREVIEWSKIP/SC-CANDID first).**_
- _2026-06-10 — **Wave 2 / Samples a11y triple done** (`4535bd3`): SA-HDRCONTRAST + SA-CULLFOCUS (the re-score #2 P1 pair) + SA-PLURAL in one pass. Headers: Kicker grew `tone="soft"` (new `.text-kicker-soft` named class — appearance stays in the primitive/styles layer) and ONLY the 5 SheetTable headers switched; the styles.css comment claiming 11.5px/700-uppercase = WCAG "large text" was factually wrong (large-bold starts at 18.66px) and is corrected — faint is now documented as decorative-on-plain-paper only. Decorative kickers untouched (settled owner decision). Bars: the aria-hidden + per-Button tabIndex=-1 hide mechanism on CullBar AND ComposeBar (same hazard, found by twin-check) replaced with the `inert` attribute via ref + layout effect — inert blurs focused descendants natively, so the click-Drop-while-focused warning is gone at the root, and the per-button conditional spreads disappear entirely. Cadence: implementer (TDD, 7 red first) → spec-review APPROVE-WITH-NITS (ran the FULL vitest suite for the JSDOM-inert ripple question — RTL doesn't honor inert, one within() scoping was the only collision; empirically red-checked the e2e against old code AND proved the console assertion alone is toothless headless → comment-downgraded; dropped a toHaveClass violating house precedent) → frontend-reviewer APPROVE (StrictMode idempotence traced; greped all aria-hidden×tabIndex combos — no divergent siblings remain; suggested the test/AGENTS.md JSDOM-inert entry, added). Live: headers oklch 0.467 over the sunk band, bar shows "1 frame selected", Drop → focus lands on BODY, zero aria-hidden warnings, frame restored after. Gate: lint:design + tsc + vitest 2155 (+5) + e2e 40/3 (+1) + build. Samples P1s closed AGAIN; remaining global P1 = F-STATE enforcement._
- _2026-06-10 — **Samples re-score #2: 31→33 / 15→16** (fresh unanchored evaluator, same method; snapshot `2026-06-10T09-05-00Z__imalayaui-…-samplespage-tsx.md`). The landed P1s held: the evaluator independently judged the 1024 scroller+frozen-column treatment "honest and usable" (responsive 3), table semantics sound, AI-slop PASS, Help 4 (the KbLegend reads true), Aesthetic 4. The gap is now an a11y pair it found deeper in: SA-HDRCONTRAST (column headers = small informational ink-faint on paper-sunk, 2.92:1 — exactly what FA-SUNK's audit predicted; falls under the OWNER-APPROVED F-CONTRAST principle, informational→ink-soft, so no re-ask) + SA-CULLFOCUS (aria-hidden lands on a bar still holding focus — live console warning). Plus honesty P2s: SA-PLURAL ("1 frames selected"), SA-UNDO2, SA-SCALE. Audit a11y stayed 2 BECAUSE of the pair; theming/anti-patterns 4/4 again. Next: fix the pair (+SA-PLURAL same files) then F-STATE._
- _2026-06-10 — **Wave 2 / SA-RESP done** (`4f1809a`): the last Samples P1, and a render-verify vindication. Live look at 1024×800 showed the baseline UNDERSOLD it: the Card's `overflow-hidden` was silently CLIPPING the Status column (944px box, 1054px content) — invisible data loss, not the "page-level scroll" the snapshot described. Built the Carbon/Polaris pattern the code's own comment had promised: one shared `overflow-x-auto` scroller around header+rows (the alignment invariant now includes shared scroll), `min-w-min` inner wrapper, sticky checkbox+Sample identity columns with offsets single-sourced from new `CHECKBOX_TRACK_PX`, role=table relocated for a contiguous ARIA chain. Two real catches in cadence: spec-review NEEDS-CHANGES on a bare `group` (would have lit every thumbnail's hover frame on row hover — fixed as named `group/row`; bg-plate surface-mirror deviation judged CORRECT), and MY live re-verify caught `min-w-max` resolving the grid at max-content (3397px wide against the real 139-sample corpus — fr tracks absorbed an unwrapped flex-nowrap gallery; the implementer's mocked e2e passed because mock samples had few exposures). Fixed to `min-w-min` (grid min-content = the fixed track minimums = 1054 exactly) and the e2e upgraded to discriminate: 12-exposure mock row, scrollWidth<1200 bound, internal-gallery-scroll pin, red-checked against the old code. frontend-reviewer APPROVE (boneyard capture unaffected — it measures the static fixture). Live after: scrollW 1054, Sample frozen at x=76.5 through a full scroll, Status readable at 1024; 1440 byte-identical layout (no scroll). Gate: lint:design + tsc + vitest 2150 (+9) + e2e 39/3 (+2) + build. **All six surfaces' original P1 sets are now closed except F-STATE enforcement; Samples re-score next.**_
- _2026-06-10 — **Wave 2 / LO-KEYD done** (`ea73229`): the window-shortcut guard defect was one CLASS across five sites, so the fix is one shared predicate. New `lib/keys.ts` `suppressGlobalKeys(e)`: typing/selection targets (INPUT/TEXTAREA/SELECT/contenteditable — paired property + `closest("[contenteditable]")` fallback because JSDOM doesn't implement `isContentEditable`) OR any open `[role="dialog"][aria-modal="true"]` in the document (document-level deliberately: aria-modal declares outside-content inert, and focus transiently sits on body before the trap engages). Modifier chords stay per-site (⌘K/⌘Z owners bind chords). Verify-before-review found the live defect WORSE than baselined: Loupe had no modifier guard at all — ⌘R (reload chord) fired set-representative, ⌘X fired drop; and the scoping ⌘Z skip-undo hijacked native text-undo inside inputs (no editing guard, preventDefault unconditional — guard now sits before it). Escape behind an open NavModal no longer ALSO navigates the loupe back (the double-action). Announce half was already F-LIVE's. Cadence: implementer (TDD, 7 behavior tests + 10-test helper suite) → spec-review APPROVE-WITH-NITS (story ⌘Z drift + a ⌘K pass-through pin; applied; observation logged as LO-POPKEY P3: non-modal Popover-internal buttons still pass single-letter shortcuts) → frontend-reviewer APPROVE (1 advisory: keys.test.ts probe param typed EventTarget vs root-tsconfig, fixed). Live keyboard drive at :5182: ArrowLeft flips, ⌘R/Ctrl+X inert, /-modal + body-focused x inert, Escape single-action (NavModal's closeOnEsc=false from-body inertness is pre-existing and now CORRECT — previously that press navigated under the modal), plain x drop/restore + Escape-back intact, 0 console errors. Gate: lint:design + tsc + vitest 2141 (+18) + e2e 37/3 + build. ALL Loupe P1s closed (LO-KEYD was the only one) → Loupe re-score due; SA-KEYD reduced to its Enter-loupe-open half._
- _2026-06-10 — **Wave 2 / SC-SEEDDEAD done** (`27ef097`): the seeded-untagged dead-end is closed at its root. `scopedTags` memo scopes the ordering-variable universe to the seed (seeded samples' own tags, first-match-per-key dedup) while direct visits stay byte-identical; it feeds BOTH proposeOrdering and the orderKeys dropdown so the two frequency maps can't diverge and the dropdown can't offer an unsatisfiable key; seeded-no-shared-key now reaches the EXISTING cold-assign card (the escape it was designed to be — `isColdPath` was previously gated on a tag-free CORPUS); DYO carries the whole selection on seeded visits, members-only on direct. Cadence: implementer (TDD, 4 red + 1 deliberate non-regression pin) → spec-review APPROVE-WITH-NITS (traced overrideKey/dual-freq-map safety, the cold-effect re-fire after warm→cold reseed, DYO disjointness; nits = 2 stale comments + dedup honesty, applied incl. first-match dedup) → frontend-reviewer APPROVE (memo dep-chain referential stability verified — no render loop; comma-expression predicate restyled to house if/return). proposeOrdering docstring un-corpused (pure over its argument). Live-verified the exact evaluator path: 3 untagged samples → + New series → cold worksheet with all 3 rows, honest gated foot, dead copy gone. Gate: lint:design + tsc + vitest 2123 (+5) + e2e 37/3 + build. Scoping P1s closed again; next re-score after SC-PREVIEWSKIP/SC-CANDID land (the audit-15 drag is the honesty cluster, not a11y now)._
- _2026-06-10 — **Scoping re-score #3: 31→26 / 17→15** (fresh unanchored evaluator, same method, snapshot `2026-06-10T06-45-04Z`). The landed Wave-2 work held: keyboard reorder ("real craft… polite live region… focus retention"), skip toggle + ⌘Z, honest foot, op-differentiated chain banner, controls-don't-lie custom gate, copy "reads human", AI-slop PASS, no console errors, clean 640px reflow. The drop is depth, not regression: first evaluator to walk the SEEDED-UNTAGGED flow (selected untagged samples on the contact sheet → + New series) and found the SC-SEEDDEAD dead-end pair (key proposed from corpus-wide frequency over a seed that lacks it → 0-member plate + unsatisfiable gate; DYO from that state renders zero rows and drops the selection) — new P1. Plus SC-PREVIEWSKIP/SC-CANDID (P2), SC-PROV (P3) merged. Heuristics: status 2 · match 3 · control 2 · consistency 3 · prevention 2 · recognition 2 · flexibility 3 · aesthetic 4 · recovery 2 · help 3; dims a11y 3 (up from 2 — SC-KBD landed) · perf 3 · responsive 3 · theming 4 · anti-patterns 2 (the dead-end + honesty leaks). Strategic read: GOLD is bounded by the worst REACHABLE state; the P1 burn-down list must absorb what each deeper evaluator finds. Next: SC-SEEDDEAD._
- _2026-06-10 — **Wave 2 / SC-KBD done** (`0b5b1e9`): the scoping worksheet finally honors GripHandle's own "PHASE-2 CONSUMER RESPONSIBILITY" promise. ScopeSampleRow grew an optional `onMoveBy?: (delta: -1|1)` contract — with it the grip is a real button (sr-only "Reorder {name}", aria-keyshortcuts, ArrowUp/Down + preventDefault, modifier-guard so Cmd/Ctrl/Alt arrows stay native); without it the visual-only grip (no dead control, FOL-KBD law). Page moveRow + drag converge on one applyReorder; boundaries announce "{name} is already first/last."; persistent sr-only polite region + LiveRegion trailing-space flip (pinned by a raw-textContent test since toHaveTextContent normalizes whitespace). Focus persists across the DOM move via sampleId keying — BOTH reviewers independently verified this in real Chromium (JSDOM can't prove it). Cadence: implementer → spec-review APPROVE-WITH-NITS (modifier-key trap; flip untested; aria-keyshortcuts; all applied + new e2e arrow-key test with semantic toContainText assertions) → frontend-reviewer APPROVE (0 findings; confirmed per-region flip ref satisfies LiveRegion's documented invariant). Live keyboard verify at :5182: move + announce + boundary + no scroll. Builder adoption = BU-ORDERINERT (P2); reorder-not-in-undo = P3. Gate: lint:design + tsc + vitest 2118 (+13) + e2e 37/3 (+1) + build. **ALL SCOPING P1s NOW CLOSED → Scoping re-score next.**_
- _2026-06-10 — **Wave 2 / SC-FOLD done** (`b6a9af7`): the fold inversion was a missing DATA CAP, not a markup problem. Verified live before fixing: unseeded /series/new rendered one row per loose candidate — 133 rows / 8,077px, Confirm & build at y=9042 (10 viewports down) — and `allSampleIds = [...rows, ...loose]` eagerly fetched traces/indices for all 139 samples (the snapshot P3 was the same bug in a perf hat). Fix: SCOPE_CANDIDATE_PREVIEW_COUNT=3 exemplar rows (keep sparkline + "lacks the {key}" caption); the 133×-repeated instruction collapsed into ONE section note (`scope-candidates-note`) carrying a computed remainder ("…and 130 more lack the dose", pluralized "1 more lacks"), the tag instruction, and a real navigate("/samples") control (also closes the P3 no-link nit); fan-out capped to rows+visibleLoose (9 trace fetches on the wire, was 139); EmptyState gating + remainder still read the FULL loose list. Cadence: implementer → spec-review APPROVE-WITH-NITS (real catch: 6-loose fixture made hidden==visible so a sliced-list remainder mutant passed — bumped to 7; e2e pin re-grounded row-level `scope-candidate getByRole(button) toHaveCount(0)` instead of name-based /add/i) → frontend-reviewer APPROVE (one stale comment). **Evaluator gotcha discovered: Playwright page.goto to the SAME URL re-uses history.state — the Wave-2 re-score carried seedSampleIds=[1,2,3] from an earlier flow and saw 3 members/0 candidates, which is why this P1 vanished from its findings; `history.replaceState(null)` before judging direct-visit states.** Render-verified at 1440×900: full worksheet in ~2 viewports, build at y=1166. Gate: lint:design + tsc + vitest 2105 (+7) + e2e 36/3 + build. Next: SC-KBD (last scoping P1)._
- _2026-06-10 — **Wave 2 / SC-FLAG done** (`7442b6f`): the FlagButton now speaks the skip model it implements. aria-pressed toggle + sr-only "Skip this read: " purpose prefix → constant accname "Skip this read: {value}" (state lives in pressed, per ARIA APG); "▸ skipped" caption aria-hidden so it can NEVER join the name; titles "Skip this read"/"Restore this read"; JSDoc rewritten; `flagged`/data-state kept as stable keys. Cadence: implementer → spec-review APPROVE-WITH-NITS (two real catches: the accname tests were vacuous — RTL name regexes are SUBSTRING matches, anchoring required + mutation-verified; and ScopingAssembly's story still simulated the retired flagged-BLOCKS-build model in the same render → story re-grounded on the real buildFootState import + kept===0 gate per story-simulates-page-computation) → frontend-reviewer APPROVE (one straggler: AutoGroup story "needs a look" → swept; confirmed pages/-import precedent via ColdAssignPanel; sr-only can't perturb layout — button is block, pattern = Field.tsx:85). Live keyboard-driven verify on /series/new via real contact-sheet selection: accname + [pressed] + foot "2 values ready to commit · 1 skipped". Gate: lint:design + tsc + vitest 2098 (+2) + e2e 36/3 + build + build-storybook. Next: SC-FOLD or SC-KBD._
- _2026-06-10 — **Wave 2 / SC-PARTIAL done** (`efbe79a`): the chain-failure banner no longer lies about committed data. Copy now derives from WHICH op failed: Op-A failure "Could not save the ordering tags. The series was not created." (avoids "Nothing was saved" — a prior attempt's tags may persist); Op-B failure "The ordering tags were saved, but the series was not created. Try Confirm & build again." Precedence: scopeSeries.error first (handleBuild re-fires scopeSeries.mutate which clears it, so it's always the fresher attempt; createSeries.error can be stale). Cadence: implementer → spec-review APPROVE-WITH-NITS (verified the batch tag route is one with_idempotency transaction = atomic, retries share clientOpId so post-commit retries return the cached success; corrected the comment's LWW claim — sample_tags has no unique constraint, retries duplicate rows and READERS take first match) → frontend-reviewer APPROVE (0 findings; verified the stale-Op-B-copy window during a retry is true-but-stale, not a lie). Tests 25→27 incl. a both-errors precedence pin. Live render waived w/ note (failure-state-only change). Follow-up logged: SC-RETRYBANNER (P3). Gate: lint:design + tsc + vitest 2096 (+1) + e2e 36/3 + build. Next: SC-FLAG._
- _2026-06-10 — **Wave 2 / BU-WYSIWYG done (honest-state half)** (`14c6337`): the Builder re-score's new P1. Orchestrator treatment call (not a product guess): option (b) honest state — the standing copy-doesn't-lie mandate applied to the DOCUMENTED lazy-draft render model — over option (a) live draft preview, which is a scope expansion left as a logged product decision. Draft-only plate subtitle via the existing SeriesPlate subtitle slot + new BuilderRail `caption?: ReactNode` prop (`??`-defaulted to the WYSIWYG sentence, JSDoc states the override-when-not-true contract). Cadence: implementer → spec-review APPROVE-WITH-NITS (sharp catch: the first draft caption itself overstated staleness — draft title AND offset slider update the plate live; wording rescoped to "Membership and order on the plate are from the last confirmed figure.") → frontend-reviewer APPROVE (verified export-spec mirrors the plate exactly, so the copy stays honest about exports too). Render-verified both states at /series/2. Gate: lint:design + tsc + vitest 2095 (+4) + e2e 36/3 + build. Next: SC-FLAG or SC-PARTIAL (scoping's re-score P1s)._
- _2026-06-10 — **Builder re-scored: 27→32 critique / 16→17 audit** (fresh unanchored evaluator, same Wave-1 method; evidence = live /series/2 read+draft @1440 + read @1120 + real-Chrome a11y trees + source). Largest rise of any surface (+5) — the four Builder P1s (rail-adapt, BU-DEAD, BU-FIELD, BU-EXPORT) all moved the needle; per-heuristic now Match 4 / Aesthetic 4 / Error-Recovery 4, drag is Help 2 + a trio of 3s (Status/Control/Recognition). Audit: theming 4, anti-patterns 4, a11y/perf/responsive 3. New P1: **BU-WYSIWYG** (draft plate stale for membership edits vs the "what you compose is what you publish" caption — treatment decision: live preview vs honest-state notice). New P2s: BU-PICKER (130-option flat select), BU-ORDERINERT (control-shaped inert Field), BU-NAMES (three label vocabularies). P3 cluster logged as BU-POLISH3. Snapshot persisted (gitignored): `2026-06-10T04-37-09Z__seriesbuilderpage-rescore.md`. Read: GOLD +4 needs Help/Status/Recognition work — i.e. BU-WYSIWYG + the P2 honesty band, consistent with the Scoping/Folio re-score lesson._
- _2026-06-10 — **Wave 2 / BU-EXPORT done** (`4d699f0`): the last Builder P1. Full export affordance (ExportButton split: Copy + Download-as-PNG/SVG menu, wired via useFigureExport — restoring the hook's only-engine-consumer rule the page's hand-rolled copy violated) surfaced in a NEW `actions` slot on SeriesPlate mirroring TracePlate's (Focus precedent; mockup's topbar button adapted to the plate head since the live topbar is the global shell). Rail's copy-only "Copy as PNG" removed (single contextual export control — the rail's own documented q-scale-toggle reasoning), foot row now conditional on onAddSample. Cadence: implementer → spec-review NEEDS-CHANGES (MUST-FIX: `rows.length===0` gate never fires — toWaterfallRows pads EMPTY_TRACE, so a failed trace fetch left Copy enabled producing a blank figure with a SUCCESS toast → `!rows.some(r => r.trace.q.length > 0)`, test pinned red-under-old-gate) → frontend-reviewer APPROVE (0 findings). Live-verified /series/2: split beside q-scale, menu roles correct, real Download-as-PNG produced a clean-scientific 6-trace figure ("Series by dose-2026-06-09.png"). Gate: lint:design + tsc + vitest 2091 (+7) + e2e 36/3 + build. Next: Builder re-score._
- _2026-06-10 — **Wave 2 / BU-DEAD done** (`c9e7cd6`): the builder rail's two inert controls (Collapse chevron + rail "+ Add sample") now render only when wired, per the rail's own onAdjust conditional-affordance pattern; doc block documents both. The page correctly passes neither (collapse behavior doesn't exist; the native AddSampleSelect in the traces slot IS the add path), so production loses both lies; the Assembly story keeps its working collapse demo. Spec-review judged hide-correct vs wire-to-focus-the-select (indirection, breaks when addable=[]); frontend-reviewer verified both layout states degrade cleanly (Kicker flush-left; Copy-as-PNG full-row). Render-verified /series/2 incl. rail screenshot. Gate: 2084 vitest (+2) + e2e 36/3 + build. Follow-up noted: onCopyPng remains conditional-spread (can render inert; live page always wires it) — sweep candidate for Wave 3._
- _2026-06-10 — **Wave 2 / BU-FIELD done** (`65fbd64`): one-prop mirror of SC-FIELD (`srLabel="Ordering variable"` on BuilderRail's ordering Field). Two semantic tests covering the modes actually exercised (the PAGE renders the static roleless div — the SC-FIELD commit only tested dropdown mode, so the static composition is newly pinned; the story exercises dropdown). spec-review SPEC-COMPLIANT, frontend-reviewer APPROVE (noted the visible-label + sr-prefix double-announcement is the accepted SC-FIELD pattern, consistency not regression). Live-verified /series/2. Gate: 2082 vitest (+2) + e2e 36/3 + build._
- _2026-06-10 — **Wave 2 / UI-STRIP done** (`c0e0d80`): PhaseStrip per-member cells now real to AT. role="img" added to all three segment branches (phase/coexist + form_factor + null-state — `segLabel` already produced the right names) per the Semantic-Dot pattern; sr-only "to" added beside the aria-hidden caption arrow. Cadence: implementer → spec-review SPEC-COMPLIANT (nits only; judged per-cell img CORRECT vs a single group label — member-by-member identity is the strip's reason to exist) → frontend-reviewer APPROVE (verified non-visual: sr-only is position:absolute so no flex-gap slot; ran the 9-file/83-test blast radius). Live a11y-tree verified on /series. Gate: lint:design + tsc + vitest 2080 (+2) + e2e 36/3 + build. Note: Playwright MCP snapshots INCLUDE aria-hidden nodes — don't read the visible → in a snapshot as an exposure bug; the JSDOM tests pin aria-hidden semantics._
- _2026-06-10 — **Wave 2 / Scoping + Folio re-scored** (fresh, unanchored, same Wave-1 single-evaluator method: rubrics + source + 1440 renders + SAXS ignore.md; detector centrally `[]` on both): **Scoping 28→31 critique / 16→17 audit** (aesthetic 4/4, cognitive-load now 1/low; a11y still 2 — SC-KBD outstanding) · **Folio 30→31 / 17** (a11y 3 after FOL-KBD; help 2 is the drag). Both rose; both under GOLD. Each re-score surfaced honest NEW P1s now merged: **SC-FLAG** (FlagButton three-vocabulary lie + bare-number accname), **SC-PARTIAL** (Op-A-committed/Op-B-failed shows "Nothing was saved" — false), **UI-STRIP** (PhaseStrip per-cell labels on role-less divs — primitive-level, multi-surface). Plus P2 clusters (SC-POLISH2, FOL-EMPTY2, EM-DASH sweep, UI-MENUKBD) recorded with details in the snapshots. Snapshots persisted at `.impeccable/critique/2026-06-10T03-11-37Z__*`. Read: the original named drags are landing, but GOLD (≥36) requires burning down the P2 honesty/feedback band too — Wave 3 is load-bearing, not optional._
- _2026-06-10 — **Wave 2 / SC-ORDER done** (commit 2/2 `99e1a8d`; turn also retitled the harness docs loop→goal, `05adb6e`): the "Define your own…" sentinel (decision A, locked 2026-06-10) is live. Always-rendered ordering dropdown (sentinel makes ≥2 options on every corpus; the old single-key static test deliberately flipped); selecting it inline-converts the warm worksheet to the ColdAssignPanel flow seeded with the current members in displayed order (Card mirrors ScopePlate's shell: Kicker + live h1 "Series by {key}" + the same order Field as escape hatch + caption + panel + gated foot), committing through the UNTOUCHED two-op scope→create chain. Cadence: fresh implementer → spec-review (SPEC-COMPLIANT, 1 SHOULD-FIX: shared ColdAssignPanel's cold-corpus intro is false in custom mode → new `intro?: ReactNode` prop, null suppresses; default byte-identical) → frontend-reviewer (REQUEST-CHANGES, 1 real hole: `canColdBuild` vacuously true on zero rows made an EMPTY series buildable from a zero-member warm worksheet → fixed in the helper + unit test + page test reproducing the exact scenario; NIT logged as SC-CUSTSSE P3). Render-verified live at :5182 (harness rebuilt: fresh disposable DB copy + re-seeded 2 tag keys): sentinel → custom card (6 seeded rows, no false copy, h1 live-updates) → escape hatch re-grouped by temperature → re-entry re-seeded → full build wrote 6 dose tags + series 2 (6 ordered members), navigated to /series/2, 0 console errors, screenshot read back (plate identity consistent). Gate: lint:design + tsc + vitest 2078 (+15) + e2e 36 passed/3 skipped (+1 DYO spec) + build, all green. Scoping has now landed 3 P1s → re-score next turn._
- _2026-06-10 — **Wave 2 / SC-ORDER commit 1/2 done** (`20a7b2b`): the Scoping "Ordered by" control was inert static text → now a data-driven dropdown. **Verify-before-review against the backend first** settled decision #4's "variable set" half deterministically: there is NO manifest/adapter variable schema — `ordering_variable` is a free-form TEXT column and the corpus's real ordering vocabulary = the distinct `sample_tags` keys (`GET /api/sample-tags`). So the dropdown is sourced from `tagsQ.data` distinct keys (freq-desc/lex-tie order, mirroring proposeOrdering), NOT the mockup's hardcoded time/dose/temperature (which would lie). `proposeOrdering` gained an optional `preferredKey` (honour a real corpus key, else byte-identical freq-winner fallback); the page holds `overrideKey` (+ stale-reset effect) and wires the EXISTING ScopePlate/Field/Menu dropdown (built in SC-FIELD, no primitive change). Option `value`=humanized label so Menu's strict activeValue match highlights; `labelToKey` inverts the selection. controls-don't-lie: dropdown only at ≥2 keys (else static); of-note data-driven. **The "define your own" affordance was a genuine product fork → AskUserQuestion (SC-ORDER is the harness's named example): Jonathan chose A (inline-convert — reuse ColdAssignPanel seeded with current members, same scope→create chain) + two commits.** So custom is DEFERRED to commit 2 with no dead entry now. cadence: implementer → spec-review (SPEC-COMPLIANT) → frontend-reviewer (APPROVE), no findings. Render-verified at :5182 (seeded `lipid_ratio`+`temperature` onto smp 1–6 in the disposable DB copy; prod :8080 untouched/down): selecting temperature re-grouped the whole worksheet (heading/AutoGroup/values 10..60/foot), 0 console errors. Gate: lint:design + tsc + 47 vitest (+ proposeOrdering/scoping suites) + 9 e2e + build all green. Next: **SC-ORDER commit 2** (DYO inline-convert)._
- _2026-06-09 — loop designed, configured (4 decisions), tracker seeded (`docs` commit)._
- _2026-06-09 — **Wave 0 done** (`c525c93`): foundation doc refresh (PRODUCT.md/DESIGN.md). Closed F-DOC, MO-DOC; F-STATE doc-half done._
- _2026-06-09 — **Wave 2 / F-CONTRAST done** (`275c421`): measured the real contrast first (oklch→linear-sRGB→luminance: ink-faint=3.16:1 AA-large-only, ink-soft=6.50:1). Surfaced the fix-approach as a genuine design decision (AskUserQuestion) — owner chose "keep the token, reassign small-text uses to ink-soft" over darkening. Implementer audited all ~72 small-text usages across 50 files, left kickers/glyphs/icon-controls/data-viz faint, documented the WCAG-context rule in styles.css. frontend-reviewer APPROVE (0 misclassifications, verified by me too); render-verified on Focus; full gate green (2036 vitest, e2e 35, build). The "~6-10 files" estimate was wrong — there were genuinely ~50; the approved principle required all of them. Next: rail-adapt (layout, backend warm)._
- _2026-06-09 — **Wave 2 / harness rebuilt + F-ERRSILENT closed**: dev backend had died (its /tmp DB copy reclaimed); restored it from the surviving persistent `~/projects/himalaya-devdata/himalaya.db` (.backup'd to a disposable /tmp copy, served from-source on :8091, /api 200). Then verify-before-review on the error paths found **F-ERRSILENT already resolved** by `useQueueMutation` (4xx assertive toast + 5xx/network auto-retry + InfrastructureBanner Refresh) — the baseline's `queries.ts:888` cite was retired comparison code. Closed as resolved-by-architecture (no code needed; the honest outcome of checking live source). Next: F-CONTRAST / rail-adapt (backend now warm for render-verify)._
- _2026-06-09 — **Wave 2 / F-LIVE done** (`c07cdce`): cross-cutting aria-live status layer via the subagent cadence (fresh implementer → spec-review SPEC-COMPLIANT + frontend-reviewer APPROVE → 5 follow-ups incl. a real per-region-flip re-speak bug → verified). New `lib/announce.ts`+`LiveRegion` SR-only announcer + assertive error toasts; reused the already-mounted `showToast`/`ToastContainer` (verify-before-review corrected the baseline's "no global toast" claim). Gate: lint:design + tsc + 84 vitest + e2e 35 + build all green. Live render-verify blocked by a dead dev backend (DB copy reclaimed) — waived-with-note for this invisible change; harness must be rebuilt next wake. Partly advanced F-ERRSILENT (queue onError now assertive). Next: F-ERRSILENT remainder / F-CONTRAST / rail-adapt._
- _2026-06-10 — **Harness switched `/loop` → `/goal`.** Jonathan set a native `/goal` (continuous: a Haiku evaluator checks the GOLD finish condition against the transcript after each turn, auto-continuing until met — no ScheduleWakeup heartbeat). Same substance (this backlog + the synthesis doc, re-read each turn; absolute constraints; subagent cadence). Evaluator caveat: it only judges the transcript → the finish requires surfacing a real per-surface re-score table + green gate. The last stale `/loop` wake carries a guard to no-op._
- _2026-06-10 — **Wave 2 / SC-FIELD done** (`21777a0`): the Scoping "Ordered by" `Field` trigger had an accessible name of just its value (WCAG 4.1.2). Fix conveys label+value ("Ordered by {value}", WAI select-only-combobox pattern) via a NEW optional `srLabel` prop on the `Field` primitive that renders an `sr-only` text prefix inside the trigger (+ static div) — deliberately NOT `aria-label` (which overrides content and would DROP the value). Chevron stays aria-hidden. ScopePlate passes `srLabel="Ordered by"`. Post-review the prop was renamed `ariaLabel`→`srLabel` (a prop named ariaLabel that doesn't set aria-label = a lie / footgun). Additive (other consumers byte-identical), zero visual change; invisible-change → semantic accessible-name tests primary (incl. negative value-never-dropped case), live render waived (JSDOM uses the real accname algorithm; reviewer confirmed real-browser name). frontend-reviewer APPROVE. Logged follow-up BU-FIELD (BuilderRail Field, same gap, trivial — prop now exists). Gate: lint:design+tsc+2063 vitest (+3)+e2e 35+build green. Next: SC-ORDER._
- _2026-06-10 — **Wave 2 / FOL-KBD done** (`a100c2e`): Folio series cards keyboard-operable (WCAG 2.1.1/2.4.7). DECISION (orchestrator, a11y-pattern call not product): inclusive card pattern — title becomes a native `<button>` inside the `<h2>` (focusable primary action, Enter+Space, `stopPropagation` vs the still-present mouse-only `<article onClick>`), NOT whole-article-as-role=button (which would flatten the card's count/phase/provenance into one button name). Focus-visible outline = verbatim Button token util (design-guard OK). Plain text when `onClick` absent. Render-verified by DRIVING the keyboard (Tab→Enter navigated /series→/series/1), title pixel-identical (19px/500/Newsreader), h2/SC-HEAD preserved. frontend-reviewer APPROVE. Gate: lint:design+tsc+2060 vitest (+4)+e2e 35+build green. Next: SC-FIELD._
- _2026-06-10 — **Wave 2 / SA-SEM done** (`fa3eacc`): ARIA table semantics on the Samples contact sheet (WCAG 1.3.1/4.1.2). Verify-before-review established the table is TWO separate CSS grids (header in SheetTable, each row its own grid) → native `<table>` can't span them → layered roles on existing divs. SheetTable=table/rowgroup/row/columnheader (incl. the formerly-aria-hidden checkbox header → labelled "Select"); SampleTableRow=row + `role=presentation` inner-grid flatten + cells; Thumbnail button=computed `aria-label`+`aria-pressed`-when-toggle. Render-verified /samples (a11y tree: role=table, 6 colheaders, 140 rows, 6 cells/row, thumb labels; pixel-identical). frontend-reviewer APPROVE + resolved the orchestrator's body-rowgroup-asymmetry concern (leaving it is correct — a rowgroup wrapping the empty-state message would be invalid ARIA). Gate: lint:design+tsc+2056 vitest (+16)+e2e 35+build green. Next: FOL-KBD._
- _2026-06-09 — **Wave 2 / F-A11Y contrast-half done** (`046b440`): operationalized the F-CONTRAST settled colour roles as regression tests (test-only). MEASURED every token pair first (oklch→linear-sRGB→WCAG in python): ink 13.3–15.0, ink-soft 6.0–6.8, ink-faint 3.16/3.29 (2.92 on paper-sunk), accent 4.40–4.97, success 4.46–5.03, warning 3.24–3.66. New `test/contrast-tokens.test.ts` PARSES the live `--color-*` from styles.css (not a copy) + table-driven role-floor asserts; helpers extracted to shared `test/helpers/contrast.ts`; phases.test.ts refactored to import them (still green). frontend-reviewer APPROVE + empirically verified fail-on-regression (mutated ink-soft → row red; rename → presence-check red). Corrected one cosmetic `measured` annotation (15.0→14.4). Gate: lint:design+tsc+2040 vitest+e2e 35+build green; no render (test-only). Logged FA-SUNK (P2). Next: surface P1s, lead SA-SEM._
- _2026-06-09 — **Wave 2 / SC-HEAD done** (`32a11a4`): real heading tree on Scoping + Folio (WCAG 1.3.1) via the subagent cadence. Verify-before-review found `Kicker` already polymorphic (`as` prop, `LoupeSidePanel` already h3) → no primitive change; and via Playwright that Folio was h1→**h3** (level-skip) while Scoping was h1-only. Fix: ScopePlate's 4 faint section labels → `as="h2"` (accent eyebrow stays div); SeriesCard title `<h3>`→`<h2>` (consumers = folio gallery only). Semantic tests (1×lvl-1 + 4×lvl-2 ScopePlate; SeriesCard lvl-2), reviewer-verified fail-on-revert. Gate: lint:design+tsc+2038 vitest+e2e 35+build green. Folio render-verified live (skip:none); populated ScopePlate live-waived (invisible tag change). Review surfaced two honest follow-ups → logged SC-FIELD (P1 4.1.2) + SC-COLD (P2 1.3.1). Next: F-A11Y._
- _2026-06-09 — **Wave 1 done** (scored baseline): seeded SAXS `.impeccable/critique/ignore.md`; rendered all 6 surfaces (1440 + 1120 for rail/grid surfaces) via Playwright MCP against the dev-DB server (:5182, prod :8080 untouched); ran `impeccable detect` (all `[]`) + a per-surface critique/audit. Scores: Samples 31/15 · Folio 30/17 · Scoping 28/16 · Builder 27/16 · Focus 24/16 · Loupe 30/15. No P0s. Persisted 6 snapshots. Merged ~10 new P1s (F-LIVE, F-ERRSILENT, F-CONTRAST, SC-HEAD, SA-SEM, SA-RESP, FOL-KBD, SC-FOLD, BU-DEAD, FO-COMB) + confirmed SC-ORDER/SC-KBD/BU-RAIL/LO-KEYD/LO-FOCUS; downgraded LO-GRID→P2. **Next: Wave 2** — open with cross-cutting P1s (F-LIVE → F-ERRSILENT → rail adapt → F-CONTRAST → SC-HEAD)._
