# I3.6 (#177) — Compare-page cutover (Phase 3 cutover) — Execution Plan

**Status:** PLAN — awaiting human approval AND the merge of #176 (I3.5b) + #181 (I4.4).
**Branch/worktree:** `i3.6` at `.claude/worktrees/i3.6` (created off `origin/main` @ `b516671`; will be rebased onto post-#176/#181 `main` before execution).
**PR body must contain:** `Closes #177`.

---

## 0. TL;DR + the one scope decision that needs a human ruling

This is Phase 3's cutover: retire the Compare *UI surface*, its *REST routes*, its *Compare-only business logic*, and its `activePage:"compare"` branch; redirect `/compare*` → `/series`; add the migrated-DB `rebuild_views_from_log!` round-trip test. The `comparison*` **tables**, the `comparison_*` **dispatcher branches** in `events.jl`, and `ConflictModalShell` (extracted by #176, reused by series) **stay forever**.

**HEADLINE OPEN DECISION (frontend — needs a human ruling): removing `"compare"` empties the `PageId` union — who kills the `activePage` model, I3.6 or I5.1?** Per the orchestrator's 2026-05-27 correction (from I3.5b/PR #213 execution), the union is **2-WAY, not 3-way**: I3.5b did NOT add `"series"` to `PageId` (the whole series stage is CorpusShell, which never mounts `useStateFromUrl`/`useUrlFromState` — verified: those hooks live only in `AppShell.tsx`). So the only union editors are I4.4 (drops `"index"`) and I3.6 (drops `"compare"`). I rebase after I4.4, inheriting `PageId = "compare"` (one member; I4.4 parked the `coerceActivePage` fallback + initial `activePage` on `"compare"`). **When I remove `"compare"` the union becomes empty (`never`)** — and at that point all three legacy AppShell pages are cut (inspect ✅ I1.7, index ← I4.4, compare ← me), so the entire `activePage` model is vestigial. A zero-member `PageId` breaks `coerceActivePage` / the default / the initial store value.
- **Option A (RECOMMENDED — plan-supported):** I3.6 removes the `"compare"` branch/route/parser arm and leaves a coherent *degenerate placeholder* (union narrowed to a harmless single sentinel literal that nothing renders, e.g. `PageId = "none"` with the fallback returning it, OR keep the now-empty machinery typed-but-unreached) and **defers the model's death to I5.1 (#182)** — which is explicitly scoped to "delete… the Zustand `activePage` model, the nav-bridge" with acceptance "no `activePage` references remain", and is the very next issue. This keeps I3.6 a *cutover* (matching its "direct" card) and avoids pulling Phase-5 work forward. The I4.4 plan's own forward-compat note anticipates exactly this seam.
- **Option B:** removing `"compare"` forces pulling I5.1's `activePage`-model removal into I3.6 (delete `PageId`/`VALID_PAGE_IDS`/`coerceActivePage`/the model + nav-bridge now). Larger blast radius, overlaps I5.1's scope, and `AppShell`/`TabRocker`/`WorkspaceGrid` (also I5.1) still depend on the model so a partial removal is awkward.
- **My read:** Option A. I3.6 ends with a compiling, never-reached degenerate union + a `// I5.1 (#182) deletes this model` marker; I5.1 finishes it. I will implement A unless the human picks B. (This supersedes the earlier "PageId has no members; delete it" wording inherited from the I4.4 forward-compat note — that note predates the 2-way correction and conflated I3.6's narrowing with I5.1's deletion.)

**RESOLVED (reviewer-converged — baked in): `comparisons.jl` is NOT deleted wholesale.** The issue text + master plan §6.3 say "delete `comparisons.jl`", but that is **unsafe as written** and contradicts the "dispatcher branches stay forever" constraint. Verified by grep: `series.jl` (header L7-12) reuses `canonical_json`, `user_id_for_event`, `comparison_now_iso`, `compute_member_snapshot`, `is_member_stale`, `compute_content_hash`, `member_ids_for_comparison`, **and `_topk_phases`/`_count_distinct_phases`** (`_series_listing_rows`, L68/L86); `routes_picker.jl` reuses `recently_used_exposures` + `picker_samples`; the kept `comparison_*` dispatcher branches in `events.jl` call `comparison_now_iso`, `user_id_for_event`, `compute_content_hash`, `member_ids_for_comparison`, `_insert_comparison_member!`, `_update_view_for_comparison_*!`. **Resolution:** keep `comparisons.jl` as a shared/replay-helper module; **surgical-delete the route-only public functions now** (per-symbol grep-gated; see §2.2 for the exact SAFE-TO-DELETE vs KEEP split). The per-symbol grep guard is mandatory — it is what catches the `_topk_phases` mislabel. (Earlier draft offered a "defer to I5.3" alternative; reviewers + himalaya converged on surgical-now.)

The parallel **frontend** Compare-draft-slice decision is ALSO resolved (decision #2 below): remove the Compare-draft slice from `state.ts` now (not defer to I5.3) — see §3.2. Keep the shared render core the series builder imports + I3.5b's `seriesDraft` slot + the shared `pendingConflict`/`ConflictError` slot.

---

## 1. Context the executor must hold (verified against the tree)

### 1.1 Where this sits in the union-narrowing sequence (2-WAY — corrected 2026-05-27)
`PageId` evolves: `"index"|"compare"` (current main, after I1.7 dropped `"inspect"`) → I4.4 removes `"index"` → **I3.6 removes `"compare"`** → I5.1 deletes the model. **Only two union editors** — I4.4 and I3.6. I3.5b did **NOT** add `"series"` (per the orchestrator's correction from PR #213): the series stage is CorpusShell, and `useStateFromUrl`/`useUrlFromState` (the URL↔`activePage` sync) mount **only in `AppShell.tsx`, never CorpusShell** — verified by reading `AppShell.tsx` (both hooks imported + called there; its docstring: "The URL↔Zustand sync hooks live here … so they never run on a corpus route"). So the series stage was never in the `activePage` model; adding `"series"` would have been dead code. I3.5b's `state.ts` change is **append-only and disjoint** from the union: the `seriesDraft` slot + a widened `pendingConflict`/`ConflictError` type.

**I rebase AFTER #176 + #181**, so at execution time `state.ts` will read `PageId = "compare"` (one-member union, set by I4.4 — confirmed by reading the i4.4 worktree's `state.ts`) plus I3.5b's additive `seriesDraft` slot. Exact lines I4.4 documents for me: L23 (`PageId`), L27-31 (`VALID_PAGE_IDS`), L32-40 (`coerceActivePage` fallback → `"compare"`), L289 (initial `activePage` → `"compare"`).

**Endgame for these symbols — Option A with a `"none"` sentinel (RESOLVED decision #3):** removing `"compare"` empties the union. Rather than delete the model (that's I5.1's job — issue-map L299-304: "delete… the Zustand `activePage` model, the nav-bridge", acceptance "no `activePage` references remain"), I3.6 narrows `PageId` to a **single harmless `"none"` sentinel** — `export type PageId = "none";` + `VALID_PAGE_IDS = new Set(["none"])` + `coerceActivePage`/default/initial `activePage` → `"none"`. **NOT a zero-member `never` union** — that makes `coerceActivePage`'s return type `never` and the `Set<PageId>` uninhabitable (uncompilable / type-broken). The `"none"` sentinel keeps `coerceActivePage`/`mergePersistedState`/the default type-valid and never-reached (bare `/` redirects to `/samples` per I4.4; no surface renders off `activePage`). Mark with `// I5.1 (#182) deletes this whole model`. I do NOT bump the persist `version` (I5.2's job with a `migrate`; an unguarded bump wipes user prefs — risk register).

### 1.2 The kept-forever list (do NOT touch)
- `comparison*` **SQLite tables** (`comparisons`, `comparison_members`, `comparison_messages`, `comparison_pins`) in `db.jl` — untouched.
- `comparison_*` **dispatcher branches** in `events.jl` (`comparison_created`, `comparison_submitted`, `comparison_deleted`, `comparison_pinned`, `comparison_unpinned`) + their `_update_view_for_comparison_*!` / `_insert_comparison_member!` helpers — untouched.
- `migrate_comparisons_to_series!` + `MIGRATION_COMPARISONS_TO_SERIES` in `db.jl` (merged #205) — untouched; my round-trip test *exercises* it.
- `ConflictModalShell.tsx` (extracted by #176) + `SeriesCommitConflictModal.tsx` + all `lib/series/*` + the series pages — untouched.
- Shared render core the series builder imports: `MultiTracePlot.tsx`, `MemberTraceLayer.tsx`, `lib/comparison/labels.ts`, `lib/comparison/coloring.ts` (verified: `SeriesBuilderPage.tsx` imports `resolveDisplayLabels`, `GroupingMode`, `MultiTracePlot`). Plus whatever else survives a "still-imported" grep.
- `comparisons.jl` shared helpers used by series/picker/dispatcher (see §0).

### 1.3 What I do NOT author (already on main / owned by another issue)
- **Series folio/scoping/builder Playwright specs already exist on `main`** (`series-folio.spec.ts`, `series-scoping.spec.ts`, `series-builder.spec.ts`). The acceptance criterion "folio → scoping → builder Playwright specs pass" = I **verify they stay green** post-cutover (the cutover must not break their setup, e.g. any compare-route mocks they happen to share). I do not write new ones.
- **Stale-queued `comparison_*` op handling** (drop-as-toast, queue `schema_version` bump) is **I5.2's** explicit scope (issue-map I5.2 behavioral note ties the rationale to I3.6 but assigns the work to I5.2). I do not bump the queue schema_version.

---

## 2. Backend deletion inventory (Julia)

### 2.1 Delete `routes_comparisons.jl` + de-register — BUT relocate two shared helpers first
- **BLOCKER discovered by grep:** `routes_comparisons.jl` **defines** `_json_error` (L65) and `_view_fields_error` (L85), and **`routes_series.jl` reuses both** (its header comment L14 says so; it calls them). So `routes_comparisons.jl` **cannot be deleted wholesale** — that would break the series routes. **Step 0 of this deletion:** relocate `_json_error` + `_view_fields_error` to a surviving home before deleting the file. Best home: `json.jl` (already the shared JSON-helper module) — or a new tiny `route_helpers.jl`, or fold into `routes_series.jl` if those are their only two callers (grep confirms exactly two caller files: `routes_comparisons.jl` itself + `routes_series.jl`). Recommended: move both into `json.jl`, update the `routes_series.jl` reference if it relied on include-order, re-run the suite.
- **Then delete file:** `packages/HimalayaUI/src/routes_comparisons.jl` (the 12 routes: list ×2, get, forks, create, submit, delete, messages ×2, pins get/post/delete).
- **`HimalayaUI.jl`:** remove `include("routes_comparisons.jl")` (line 28).
- **`server.jl`:** remove the `register_comparisons_routes!()` call (line 129).
- **Verify** (grep, must come back empty across surviving `src/*.jl`): `register_comparisons_routes!`, `_comparison_member_payload`, and each `/api/comparisons` / `/api/experiments/{eid}/comparisons` route string. Plus confirm `_json_error`/`_view_fields_error` now resolve from their new home.

### 2.2 `comparisons.jl` — surgical delete of route-only functions (RESOLVED: surgical now, keep the shared module)
**Decision (reviewer-converged):** delete only the route-only public functions; keep the module as a shared/replay-helper home. **Enforce the per-symbol grep guard exactly** — it is what catches the `_topk_phases` class of mislabel below. For each candidate, grep the surviving `src/` tree; delete ONLY if zero non-test, non-self caller remains.

- **SAFE TO DELETE (grep-verified route-only — callers are only `routes_comparisons.jl` + their own defs):** `comparisons_for_experiment` (L609), `comparisons_listing` (L652), `forks_of_comparison` (L790), `is_author` (L205), `current_content_hash` (L190), `fetch_comparison_with_members` (L398), `_comparison_listing_rows` (L699), and `_comparison_member_payload` (lives in `routes_comparisons.jl`, goes with the file). `is_author` note: `series.jl:12` only *mentions* it in prose ("no `is_author` gate") — not a call.
- **KEEP — `_topk_phases` (L739) + `_count_distinct_phases` (L772) are NOT route-only.** **VERIFIED:** `series.jl`'s `_series_listing_rows` calls both (`series.jl:68` `_topk_phases(phases_str, 3)`, `series.jl:86` `_count_distinct_phases(phases_str)`; the L62-63 comment explicitly says it reuses the in-module phase-token helpers). Deleting them breaks `series_listing`/`forks_of_series`. **DO NOT DELETE.**
- **KEEP (proven live consumers — series/picker/dispatcher):** `canonical_json`, `compute_content_hash`, `member_ids_for_comparison`, `user_id_for_event`, `comparison_now_iso`, `compute_member_snapshot`, `is_member_stale`, `recently_used_exposures`, `picker_samples`, `_picker_samples_projection`, `CONFIRMED_INDEX_R2_GATE`, plus `_topk_phases`/`_count_distinct_phases` (above).

### 2.3 Backend test deletions / additions
- **Delete** `test/test_routes_comparisons.jl` (route tests for the deleted routes) and remove its `include(...)` from `runtests.jl` (line 43).
- **`test/test_comparisons.jl`** (line 42 include): KEEP the testsets covering the shared helpers that survive (`canonical_json`, `compute_content_hash`, `compute_member_snapshot`, `is_member_stale`, `recently_used_exposures`, `picker_samples`, dispatcher-fold helpers). **Delete** only testsets that exercise the deleted route-only functions (`comparisons_for_experiment`, `comparisons_listing`, `is_author`, `forks_of_comparison`). Re-grep before cutting each. If the §0 ruling is "defer", leave this file fully intact.
- **Add** the migrated-DB round-trip test (see §5). New file `test/test_migrate_comparisons_to_series_roundtrip.jl`, `include`d in `runtests.jl` right after `test_migrate_comparisons_to_series.jl` (line 6) so the migration helper symbols are already in scope ordering-wise (it `using`s the same exports anyway).

### 2.4 Surviving-importer verification (RUN BEFORE DELETING — gate) — grep results
Grepped the whole `src/` tree. Findings (verified, pre-execution):
- **`routes_comparisons.jl` is referenced by:** `HimalayaUI.jl` (include L28), `server.jl` (register L129), and **`routes_series.jl`** — the last reuses `_json_error` + `_view_fields_error` which are *defined in* `routes_comparisons.jl` (L65/L85). ⇒ **NOT a clean delete** — those two helpers must be relocated first (§2.1 Step 0). Once relocated, no surviving importer remains.
- **Route-only business functions — SAFE TO DELETE** (grep shows callers ONLY in `comparisons.jl` defs + `routes_comparisons.jl` call sites): `comparisons_for_experiment`, `comparisons_listing`, `forks_of_comparison`, `is_author`, `current_content_hash`, `fetch_comparison_with_members`, `_comparison_listing_rows`. `series.jl:12` only *mentions* `is_author` in a prose comment — not a call.
- **`_topk_phases` / `_count_distinct_phases` — KEEP (corrected; NOT route-only).** `series.jl:68/86` (`_series_listing_rows`) call both. Deleting them breaks `series_listing`/`forks_of_series`. Earlier draft wrongly listed them as conditionally-deletable — struck.
- **Shared helpers — KEEP** (`canonical_json`, `compute_content_hash`, `comparison_now_iso`, `user_id_for_event`, `compute_member_snapshot`, `is_member_stale`, `member_ids_for_comparison`, `recently_used_exposures`, `picker_samples`, `_picker_samples_projection`, `CONFIRMED_INDEX_R2_GATE`): consumed by `series.jl`, `routes_picker.jl`, `events.jl` (dispatcher).

---

## 3. Frontend deletion inventory (TS/React)

### 3.1 Pages + routes
- **Delete** `pages/Compare.tsx` (the routed `Compare` component; also covers ComparePage/ComparePageEdit which live in/under it — verify exact file layout at execution and delete the Compare-page composition).
- **`components/AppRoutes.tsx`:** remove the `import { Compare }`, the `EditToBareRedirect` helper (Compare-only), and the eight `/experiments/:eid/compare*` + `/compare/all*` `<Route>` entries. **Add** `/compare*` → `/series` redirects that sit **outside both layout shells** (mirror I4.4's pattern + its comment: redirects placed outside `<AppShell>` so the `activePage` nav-bridge effect can't mount and race the redirect). Cover both roots: `/compare/all/*` and `/experiments/:eid/compare/*` → `<Navigate to="/series" replace />`. `PageBody`'s `activePage==="compare"` reachability comment updates to "Compare retired (#177)".

### 3.2 `state.ts` — union narrowing + Compare-draft slice
- **Union (Option A — §0/§1.1):** narrow `PageId` / `VALID_PAGE_IDS` off `"compare"` to a coherent degenerate sentinel; re-home `coerceActivePage` + initial `activePage` accordingly. Leave compiling + never-reached, `// I5.1 (#182) deletes this model` marker. Do NOT delete the model (I5.1) or bump persist version (I5.2). NOTE: `seriesDraft` (I3.5b, append-only) and the widened `pendingConflict`/`ConflictError` type are NOT union members — leave the `seriesDraft` slice fully intact.
- **Compare-draft slice — REMOVE NOW (RESOLVED decision #2, not deferred to I5.3).** Reviewer rec: the slice's ~25 actions + the `withDraftMirror`/`loadDraftFromSession` init machinery serve ONLY deleted Compare surfaces; removal is confined to `state.ts` and does not touch the shared render core or I3.5b's `seriesDraft` slot. Remove: `activeDraft` slot + its ~20 draft/member/review actions (`startNewDraft`, `startForkDraft`, `loadDraftFromComparison`, `setDraftTitle/Description`, `setDraftForkOf`, `addMember`, `removeMember`, `updateMember`, `reorderMembers`, `resizeBands`, `resetBandHeights`, `cyclePeakDisplayForMember`, `discardDraft`, `setDraftViewGroupingMode`), `compareXDomains` (+ `setCompareXDomain`), `showPeakTicks`/`showPeakLabels` (+ setters), `highlightedCompareMemberId` (+ setter), `withDraftMirror`, and the `lib/comparison/draft`/`draftFactories`/`peakCycle` imports + the `Comparison` import (if unused after). 
- **KEEP the `pendingConflict`/`ConflictError` slot — I3.5b WIDENED it for series.** `SeriesCommitConflictModal` reads the same `pendingConflict` slot (verified: it `useAppState((s) => s.pendingConflict)` + `setPendingConflict`, routes by `isFullSeries`). KEEP `pendingConflict` + `setPendingConflict` + the `ConflictError` import; remove only Compare-specific wiring (none lives in `state.ts` itself — the Compare side wired through the `useSaveComparison` 409 path, which the bridge handles). **Per-field grep gate before cutting each.**

### 3.3 App.tsx — CORRECTED: the conflict bridge is now SHARED, keep it
- **Verified against i3.5b/#213:** I3.5b *widened* `conflictBridge.ts` and `App.tsx`'s `attachConflictBridge` effect to handle BOTH `comparison_save` AND `series_commit` — `SeriesCommitConflictModal` reads the **same** `pendingConflict` slot (routed by `isFullSeries` vs comparison). So the `attachConflictBridge` effect is **NO LONGER Compare-only — KEEP it** (series needs it).
- **Remove** the `<ConflictModal />` mount (Compare-only modal) + its import. **Keep** `<SeriesCommitConflictModal />` and the `attachConflictBridge` effect + the `pendingConflict` slot.
- **`conflictBridge.ts`:** remove only the `COMPARISON_SAVE_KIND`/`comparison_save` arm (its source mutation — the Compare Save — is gone); keep `SERIES_COMMIT_KIND`. Verify the bridge still wires `series_commit` correctly after the comparison arm is pulled. (If the bridge's conflict-kind set is data-driven, this is a one-line edit; confirm at execution against the final #213 shape.)
- **Delete** `components/ConflictModal.tsx` (Compare-only; distinct from the kept `ConflictModalShell` and the kept `SeriesCommitConflictModal`).

### 3.4 Compare-only components / hooks / lib — CORRECTED lists (every deletion still per-symbol grep-gated)
The lists below are the *authoritative* KEEP/DELETE split (reviewer M1+M2, verified by grep against the i3.5b worktree). **The per-symbol grep at execution is still the final gate — if grep disagrees with a list entry, trust grep and flag it.**

- **Components — SAFE TO DELETE (truly Compare-only):** `Compare.tsx` (ComparePage/ComparePageEdit composition), `CompareToolbar`, `CompareTitleStrip`, `CompareStatusSurface`, `ComparisonSidebar`, `ComparisonPickerBody`, `ComparisonPickerPanel`, `ConflictModal` (the Compare modal; §3.3).
- **Components — KEEP (imported by `SeriesBuilderPage`, directly or transitively — NOT Compare-only):** `MultiTracePlot`, `MemberTraceLayer`, **`MemberMetaGutter`, `GroupingModeToggle`, `AnnotationToggles`, `ActiveBandContext`** (verified: `SeriesBuilderPage.tsx` L8-12 imports `MultiTracePlot`/`MemberMetaGutter`/`GroupingModeToggle`/`AnnotationToggles`/`ActiveBandProvider`), plus **`BandResizeDivider`, `MemberMetaRow`** (transitively pulled by the kept `MemberMetaGutter`/`MultiTracePlot` — grep-confirm the edge at execution). **Corrected from v1, which wrongly listed all six for deletion.**
- **Hooks — SAFE TO DELETE (Compare-only):** `useCompareMode`, `useCompareDraftDirty`. **N2 consequence:** deleting these orphans `SavePill` + `InlineEditableText` (verified: their only importers are `useCompareMode`/`CompareTitleStrip`/`Compare.tsx`) — **add `SavePill` + `InlineEditableText` to the safe-delete component list** (grep-confirm zero surviving importer after the Compare surfaces go).
- **Hooks — EDIT, don't delete (shared):** `useUrlFromState`/`useStateFromUrl`/`parseLocation` carry `"compare"` arms (`useUrlFromState.ts:42,49`; `useStateFromUrl.ts:15,32-38,51-54`; `parseLocation.ts:10,34-42`). Drop the `compare` branch / `kind:"compare"` variant / the `"compare"` member of their local `VALID_PAGES`. (After I4.4 these are already `"index"`-free; I remove the `compare` arm.)
- **lib/comparison/ — KEEP (NOT Compare-only):** `coloring` + `labels` (series builder), **`yBands`** (MultiTracePlot/MemberMetaGutter/MemberMetaRow), **`normalization`** (MemberTraceLayer), **`dragThreshold`** (MemberMetaRow), **`draft`** (MemberMetaRow needs the `DraftMemberNormalization` type — can't fully delete the module), **`contentHash`** (imported by chat `MentionPicker`/`MentionChip` — verified, NOT Compare-only).
- **lib/comparison/ — SAFE TO DELETE (grep-confirm):** `effectiveGroupingMode`, `prefetchMembers`, `routes`, `relativeTime`, `peakCycle`, `draftFactories`, `snapshot` (its lone `api.ts` hit is a *comment*, not an import — confirm). Corrected from v1, which under-listed the KEEPs.
- **lib/queue (replay machinery) — KEEP EVERYTHING (RESOLVED, not "re-evaluate"):** KEEP the `comparison_*` dispatcher-mirror arms in `applyRemoteToCache.ts`, the `mutatorRegistry`/`resolveMutatorForEvent` comparison cases, AND `saveComparisonMutator`/`deleteComparisonMutator`/`postComparisonMessageMutator` **registered**. Rationale: a persisted/foreign `comparison_*` op then re-fires → server 404 → **graceful failed-replay** (a dropped op surfaced as a toast — `persistence.ts:120-122`), NOT a runtime throw. (Removing a still-referenced mutator *import* would be a **build** break, not a runtime throw — that's the §0 wording fix.) The stale-op drop + the queue `schema_version` bump are **I5.2's** scope. Delete only the Compare *UI* read-hook call sites in `queries.ts` that no surviving file calls (§3.5).

### 3.5 queries.ts
Remove the Compare **read** hooks with no surviving caller (grep): `useComparisons`, `useComparison`, `useComparisonMembers`, `useComparisonForks`, `useComparisonMessages`, the Compare pins read-hooks. **DO NOT remove `saveComparisonMutator`/`deleteComparisonMutator`/`postComparisonMessageMutator` imports** — they stay registered for replay (§3.4); removing their import would be a build break. **KEEP `queryKeys.comparison*`** entries (`applyRemoteToCache`/`mutatorRegistry` replay reference them). KEEP `api.ts` comparison fetchers the kept replay mutators call; delete only UI-only `api.ts` fetchers with zero surviving caller (grep each).

### 3.6 Frontend test deletions
Delete the Compare-UI Vitest specs (all confirmed present): `Compare.*.test.tsx` (baseHashCaptureMoment, emptyState, header.edit, header.review, routing, savePill.fork, stale-status, viewingStale), `ComparePage.test.tsx`, `ComparePageEdit.test.tsx`, `comparePath.test.ts`, `compareRouteFlatten.test.tsx`, `CompareStatusSurface/CompareTitleStrip/CompareToolbar.test.tsx`, `ComparisonPicker*/ComparisonSidebar.*.test.tsx`, `ConflictModal.test.tsx`, `useCompareMode/useCompareDraftDirty.test.tsx`.
**KEEP the 3 queue tests `test/queue/applyRemoteToCache.compare.test.ts`, `deleteComparison.test.tsx`, `saveComparison.test.tsx` (RESOLVED — not conditional).** They guard the kept `comparison_*` replay machinery (§3.4), which stays this issue; deleting them would lose coverage on live code. Earlier "delete only if I delete those mutators" framing is struck — the mutators stay, so the tests stay.
**Add** a small Vitest test asserting `/compare/all` and `/experiments/1/compare` redirect to `/series` (mirror I4.4's `AppRoutes.test.tsx` redirect-block pattern).

### 3.7 E2E (Playwright, mocked)
- **Delete** `e2e/compare.spec.ts` + `e2e/compare-sidebar-projection.spec.ts`.
- **`e2e/live/comparePickerInline.spec.ts`:** live spec — per the dispatch-plan §8 note, live-spec repoints (#207) are deferred. Delete it (its routes are gone) OR mark skipped with a `// I3.6: Compare retired, see #207` note. Default: delete (the routes 404 now); flag in PR.
- **Verify** `series-folio/scoping/builder.spec.ts` still pass (the acceptance criterion). Check they don't depend on any compare route mock that this cutover removes.

---

## 4. The `/compare*` → `/series` redirect (detail)
Place OUTSIDE both `<CorpusShell>` and `<AppShell>` layout routes (I4.4 precedent + its load-bearing comment: avoids the legacy `AppShell` `activePage` nav-bridge effect racing the redirect). Both:
```
<Route path="/compare/all/*" element={<Navigate to="/series" replace />} />
<Route path="/experiments/:eid/compare/*" element={<Navigate to="/series" replace />} />
```
`replace` so Back doesn't bounce. Splat (`/*`) covers `/new`, `/:id`, `/:id/edit`.

---

## 5. The headline deliverable — migrated-DB `rebuild_views_from_log!` round-trip test

**Goal:** prove a series row *born from migration* (not native creation) folds correctly through the pure-replace dispatcher branches. Distinct from I2.7's native round-trip.

**Fixture → migrate → empty → re-fold → assert** (templates: `test_migrate_comparisons_to_series.jl` for the build+migrate; `test_routes_series.jl` native round-trip for the empty+refold+assert shape; `rebuild_views_from_log!(db, entity_id; entity_type="series")` signature at `events.jl:1034`):

1. **Build a real comparison-era DB:** `db = SQLite.DB(); create_schema!(db); migrate_schema!(db)`. Insert a user; insert a `comparisons` row (title/description/content_hash/created_by/created_at) + 2-3 `comparison_members` (real exposure FKs — reuse the existing migrate test's fixture builder so the FK graph is valid) + a `comparison_messages` row + a `comparison_pins` row. This is the pre-migration corpus.
   - **Subtlety:** `migrate_schema!` already runs `migrate_comparisons_to_series!` once (it's in the sequence). So I must insert the comparison rows and then invoke `migrate_comparisons_to_series!(db)` AFTER — but the sentinel gate makes the in-sequence run a no-op on an empty corpus and blocks a second run. **Resolution:** build the comparison rows on a DB where I run `create_schema!` + the migration-sequence-minus-the-final-migration, OR (cleaner) insert comparisons, then DELETE the sentinel row (`schema_migrations WHERE name = MIGRATION_COMPARISONS_TO_SERIES`), then call `migrate_comparisons_to_series!(db)` directly so it actually folds. Mirror exactly how the existing `test_migrate_comparisons_to_series.jl` "non-empty corpus" testset arranges this (read it at execution — it must already solve the sentinel-ordering, since it tests a non-empty migration).
2. **Capture migrated state:** read the resulting `series`/`series_members`/`series_samples` rows for the migrated `series_id` (the `series_*` view rows the dispatcher wrote during migration).
3. **Empty the view rows — HARD RULE: empty ONLY `series` / `series_members` / `series_samples`.** `DELETE FROM series_members WHERE series_id=?; DELETE FROM series_samples WHERE series_id=?; DELETE FROM series WHERE id=?`. **DO NOT empty `series_messages` or `series_pins`.** The migration **copies** those via direct SQL (`db.jl:995-1007`) — it does NOT synthesize `series_pinned`/message events. A re-fold replays only `series_created` + `series_plate_committed`, so it will NOT reproduce messages/pins; emptying them would make the assertion spuriously fail. The `user_actions` log rows are also NOT deleted — they are the source of truth to re-fold from.
4. **Re-fold:** `rebuild_views_from_log!(db, series_id; entity_type="series")`.
5. **Assert equality:** rebuilt `series`/`series_members`/`series_samples` rows deep-equal the captured migrated state (same membership, ordering, recipe, title/description, hash). The two synthesized `user_actions` (`series_created`, `series_plate_committed`) are unchanged. **Do NOT assert on `series_messages`/`series_pins` in the empty/refold cycle** (they were never emptied; the rebuild does not touch them — they are copied, not event-sourced). Optionally assert they remain present/unchanged as a separate non-cycle check.

**Why this is the proof:** the migration synthesizes event payloads and folds them once; this test re-folds from the persisted log and asserts the dispatcher is a pure function of the log for a *migrated* series — guarding against migration writing view state the dispatcher couldn't reproduce.

---

## 6. Gate plan (execution-time, post-rebase)
- **Frontend build (REAL gate):** `cd packages/HimalayaUI/frontend && npm run build` (src-only tsc + vite). Ignore bare `npx tsc --noEmit` test/ noise (~183 pre-existing errors per memory).
- **Vitest:** `npm test` (one-shot). Expect deleted Compare specs gone, new redirect test green.
- **Playwright (mocked):** `npm run e2e`. `permalinks.spec.ts:74` is a known flake — re-run once if it's the only red. Verify series folio/scoping/builder specs pass; compare specs deleted.
- **Julia backend suite:** `julia --project=packages/HimalayaUI -e 'using Pkg; Pkg.test("HimalayaUI")' > /tmp/jl-test-i36.out 2>&1` (per-branch log — NEVER the shared `/tmp/jl-test.out`). `grep -E "Test Summary|did not pass|fail" /tmp/jl-test-i36.out; tail -50 /tmp/jl-test-i36.out`. Two known flakes (fast-skip P99 latency, `GET /api/health` port race) — not regressions. Capture ONCE (5-10 min); grep the file, don't re-run with different filters.

---

## 7. Self-review against the three rubrics

**himalaya-reviewer (backend deletion + replay correctness):**
- Did NOT delete `comparisons.jl` wholesale — surfaced it as a blocking decision; default keeps every helper the series stack / picker / kept dispatcher uses (grep-verified consumers). ✔
- `comparison*` tables + `comparison_*` dispatcher branches + `migrate_comparisons_to_series!` untouched. ✔
- Migrated-DB round-trip test exercises the migrated path, sentinel-ordering subtlety called out, re-uses the existing fixture builder for valid FKs. ✔
- Surviving-importer grep is a hard gate before any Julia deletion. `routes_comparisons.jl` is NOT a clean delete — it defines `_json_error`/`_view_fields_error` reused by `routes_series.jl`; those relocate first (§2.1 Step 0), then no surviving importer remains. ✔

**queue-reviewer (replay / idempotency):**
- Recognized the `comparison_*` foreign-event replay arms in `mutatorRegistry`/`applyRemoteToCache` are kept-forever replay machinery (mirror the kept dispatcher); default leaves them intact. ✔
- Recognized I3.5b made `conflictBridge` + `attachConflictBridge` + the `pendingConflict` slot SHARED (`comparison_save` + `series_commit`) — KEEP the bridge/effect/slot, remove only the `comparison_save` arm + the `<ConflictModal />` mount. ✔
- Stale-queued `comparison_*` op handling + queue `schema_version` bump correctly assigned to I5.2 (not this issue). ✔
- No persist `version` bump in I3.6 (an unguarded bump wipes prefs — I5.2 owns it with a `migrate`). ✔

**frontend-reviewer (cutover completeness):**
- 2-way (not 3-way) union narrowing — I3.5b did NOT add `"series"` (CorpusShell never mounts the URL↔activePage sync; verified in `AppShell.tsx`). Start from post-#181 `PageId="compare"`; narrow to a degenerate sentinel + re-home fallback/initial; **defer model death to I5.1 (Option A, §0 headline decision)**. ✔
- Redirect placed outside both shells (I4.4 race-avoidance precedent). ✔
- Shared render core (`MultiTracePlot`, `MemberTraceLayer`, `coloring`, `labels`) + shared `pendingConflict`/`ConflictError` slot explicitly KEPT; every deletion grep-gated. ✔
- `useUrlFromState`/`useStateFromUrl`/`parseLocation` `compare` arms EDITED not deleted (shared hooks). ✔
- Compare Vitest + E2E specs deleted; series specs verified, not authored. ✔

---

## 8. Execution order (when greenlit)
All three open decisions are RESOLVED (reviewer-converged, baked into §0/§2.2/§3.2/§3.4/§1.1): surgical-delete `comparisons.jl` route-only fns; remove the Compare-draft slice from `state.ts`; Option A `"none"` sentinel for the union endgame.
1. Rebase `i3.6` onto post-#176/#181 `main`; re-run `/worktree-setup` if deps changed. Re-confirm the i3.5b/i4.4 final shapes against §3.3 (shared conflict bridge) + §1.1 (`PageId="compare"` start).
2. TDD: write the migrated-DB round-trip test (per §5; failing → likely passes immediately since the path exists, so frame as a characterization/guard test) + the `/compare*`→`/series` redirect Vitest test first.
3. Backend deletions (relocate `_json_error`/`_view_fields_error` to `json.jl` FIRST → delete `routes_comparisons.jl` + de-register → surgical `comparisons.jl` route-only fns per §2.2 → test file removals/additions), **per-symbol grep-gated** (the gate that catches the `_topk_phases` class).
4. Frontend deletions (Compare page, routes+redirect, App.tsx `<ConflictModal/>` mount only [keep bridge/slot], state.ts union→`"none"` + Compare-draft slice removal, components/hooks/lib per the §3.4 corrected lists — **grep each**, queries.ts read-hooks, Compare-UI test deletions [KEEP the 3 queue tests], new redirect test).
5. Run all four gates (§6). Iterate to green.
6. Open PR via `request-pr-review` skill; body has `Closes #177` + a coordination note for I5.1/I5.2: "rebased onto #176+#181; `PageId` narrowed to `"none"` sentinel (Option A), `coerceActivePage`/default/initial moved off `"compare"`; `activePage` model + `comparison_*` replay mutators left for I5.1/I5.2; no persist version bump."
