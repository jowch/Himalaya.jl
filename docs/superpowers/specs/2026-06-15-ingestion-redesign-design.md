# Ingestion redesign — design spec

**Date:** 2026-06-15 (revised 2026-06-18: UI converged via an interactive prototype + a second implementation-feasibility research sweep that validated every backend change and inventoried the frontend against live code).
**Status:** Design + UI approved in iteration; backend/frontend changes validated against live code. Pending final review before an implementation plan.
**Register / design system:** product · "The Print" (see `DESIGN.md`, `PRODUCT.md`).
**Visual reference:** the **interactive prototype** `docs/redesign-mockups/ingestion-prototype.html` is canonical (clickable: walk experiments-home → directory picker → live ingest → corpus + review banner → grouping review → configuration → failed scan). It reuses shipped components and adds a few new ones (§8.6/§8.7). The earlier static per-surface mockups are superseded by it.

## 1. Goal

Make ingestion frictionless. Today a user hand-authors a `manifest.csv` (sample → exposure-stem grouping) and an `experiment.toml` (geometry + column mappings), then runs CLI `init`/`reingest`. The redesign replaces that with: **point Himalaya at a beamtime directory; it scans, groups the exposures into samples, derives the geometry, and lands you in the corpus.** North star: "dump a directory and it figures it out," with the smoothness of Gradescope's exam-ingestion flow.

The whole motivation is scale and friction: a beamtime is hundreds of exposures across ~a dozen rack-loads, and writing a manifest by hand for that is the cost we are removing.

## 2. Principles

1. **Assume as little as possible.** Infer jointly from timing, filename, and stage position; hard-code the minimum. Frame-count per acquisition is variable; sweep order is not guaranteed; conventions differ per experimenter. Numeric thresholds are *validated defaults*, not constants (§5).
2. **The database is the source of truth.** No hand-maintained manifest file. Ingestion appends rows; the working state lives in the DB.
3. **Additive, no "re-ingest."** Ingestion is one repeatable flow. Re-running it (or rescanning) only adds genuinely-new exposures and never clobbers human edits or curation. There is no reconcile-the-whole-manifest step.
4. **Surface the decision, hide the settled.** Auto-grouping clears the great majority; the human's attention is pulled only to the few ambiguous groupings.
5. **Lean to purpose, per surface.** Each screen does one job (e.g. the Samples screen verifies grouping; it does not show phase).
6. **Reuse the rails we have.** The event log, idempotency, SSE, the per-exposure analysis pipeline, and the background-timer pattern already exist. The redesign extends them; it does not build a parallel stack.

## 3. The flow

```
Point at a directory  →  Auto-process  →  lands in the experiment (corpus)
                          (scan · read PRP/setup · group · analyze)
        ↻ Rescan picks up new files later (cheap change-check; adaptive schedule; never a blind full re-read)
```

- **Point at a directory.** The user creates an experiment by selecting its data directory. An experiment **is** one setup = one root directory.
- **Auto-process** (staged, with live progress over SSE): enumerate exposures (TIFF + PRP + `.dat`); read geometry from PRP + beam center from the analysis-dir setup files (§6); group exposures into samples (§5); run per-exposure analysis (peak-finding + indexing) in the background.
- **Lands in the experiment.** No separate "ingest review" gate; the user lands in the experiment's Samples/corpus view, which populates live as processing proceeds.
- **Rescan.** A rescan never blindly re-reads everything. It first runs a **cheap change-check** (directory listing + max mtime vs. the stored scan signature); if nothing changed it is a no-op. If new files appeared, it ingests *only* those (insert-only, never clobbering human edits) and analyzes them. Rescan runs (a) on an **adaptive auto-schedule** while a beamtime is live and (b) on a manual **Rescan** action. There is no manual "add files." The auto-schedule (§9.4) starts polling when the first samples arrive and **backs off in tiers** (hourly → daily → off) as the run goes quiet, re-arming if new files appear, bounded by the fact that a beamtime lasts at most ~a week.

## 4. Information model

Two classes of information, opposite sources: **grouping + identity** is human knowledge; **everything instrumental** is derivable. The redesign splits them.

| Entity | Fact | Source |
|---|---|---|
| **Experiment** (= one setup = one directory) | name, description | human / auto (dir basename) |
| | geometry: energy, flight-path, detector, pixel pitch, q-units | **PRP** (constant across the experiment) |
| | beam center, calibrated detector distance | **analysis-dir setup files** |
| | data dir, analysis dir, file patterns | config (defaults; editable) |
| | scan signature (last scan time, file-set fingerprint) | computed |
| **Load** (one rack-load / "set"; §5 terminology) | index, session, time range, frame count | computed (time-gap segmentation) |
| **Sample** (a rack slot's specimen) | name, notes | human / auto-generated (§5 naming) |
| | grouping source (auto-position / auto-time / manual / user-merged), name source (auto / human) | computed / human |
| | which exposures (the grouping) | auto (§5) + human correction |
| **Exposure** (one TIFF + PRP + `.dat`) | image / trace / prp paths | computed from disk |
| | accept/reject status, selection | human curation |
| | content fingerprint (dedup) | computed |
| | **timestamp, exposure time** | PRP (the only per-exposure PRP fields kept) |
| | grouping signals: stage H-position, scan id, frame no | PRP / filename |

**No sample "role" field.** References (calibration `agbe`, background `emptycap`/water) are just samples the user adds if they want them; Himalaya does not model them specially. **Most PRP fields are dropped** (motor slits, attenuator, `dispx`, `Phi` — constant or noise to the user).

**Sample identity — id + a single `name` label.** A sample has one numeric identity, `samples.id` (autoincrement PK), used by every FK (`exposures.sample_id`), URL (`/sample/:id`), the mutation queue, and SSE. It has one human label, **`name`** (`HA85 (S01P15)`), auto-populated and user-editable. Today the schema carries *two* text columns — a machine identifier (`name` = the manifest id, e.g. `"JC001"`) and a friendly label (`display_name`); the redesign **drops the machine identifier** and **renames the surviving label `display_name` → `name`**, so a sample ends with exactly one label field. The dropped machine identifier's live uses are all subsumed or dead: display fallback (subsumed — the label is never null now), NavModal search (works on the label), export (emit the label), reingest upsert-by-name (`cli.jl:192`, dead with the manifest), CLI `-s <name>` filter, and the `[A-Za-z0-9._-]` validation (`validate.jl:3`). URLs are numeric (`/sample/:id`), but `GET /api/resolve` is a **live general slug→id resolver** whose sample branch queries `samples.name` (`routes_resolve.jl:90-92`, live caller `IndexSlugRedirect.tsx:25`) — *not* a retired `/index`-only path. **Decision (resolved): keep it.** After the rename it transparently resolves the *new* label column and keeps working at near-zero cost; only the manifest-era *upsert/charset* uses of the machine `name` are deleted, not slug resolution. The manifest CSV's own `sample_id` column was never the DB id and also disappears. Across rescans a sample is re-identified by its **(load, slot) coordinate** (§5), not by any text key.

**Provenance.** Every harvested fact is stamped with its source: `human | prp | setup | computed` (the `.dat` is dropped as a provenance source — it is only the Q/I/error integration and carries no identity or geometry). Provenance drives: (a) **dedup** — an exposure's identity is its `(experiment, filename)` + content fingerprint, so re-scanning only adds new files; (b) **never-clobber** — human edits (sample name, status, curation, geometry override) survive rescans while derived facts refresh (this generalizes the `display_name` reingest bug at `cli.jl:203`, see `docs/experiment-config.md`); (c) future LLM-sourced facts would be `proposed` pending confirmation.

**Name provenance is internal, not a Configuration-page badge.** Only *geometry* fields show a visible source tag on the Configuration page (PRP / setup files / you). A sample's `name_source` (auto vs. human) is tracked solely to power never-clobber (a human-edited `name` survives rescans); it is not surfaced as a source chip.

## 5. Grouping algorithm (data-derived)

Derived empirically from the real SSRL dataset (`/Volumes/data/ssrl/2026_04/1p7m`, 682 exposures; full analysis in the brainstorm). The workflow is a **stepping sample rack**: the stage sweeps through fixed slot positions shooting each, then the rack is reloaded and swept again.

**Terminology.** A **load** is one loading of the sample holder (synonyms the user uses: "set", "rack"); the holder has up to ~20 slots here but the count is not fixed in general. We use **load** as the canonical structural term throughout (it matches the mockups' "Load 1, Load 2…"); "set"/"rack" are accepted synonyms. A **slot** is one position within a load; a **sample** is the specimen in one slot; an **acquisition** is one contiguous burst of **frames** (exposures) at a slot.

**Deterministic backbone (no LLM):**
1. **Frames → acquisition.** Consecutive frames at one stage position within a short burst window. A slot boundary is a **position gap large relative to the local within-burst jitter** (gap ≫ the median consecutive-frame position delta), *not* an absolute spacing — so the same logic handles both the HA ~4 mm sweep and the JC well plate (H ≈ 39.5 / 71 / 87 / 107 mm, 16–31 mm spacing) that **coexist in the same directory**. Frame count per acquisition is **variable** (commonly 4 in this data — 161 of 170 slots — but never hard-coded).
2. **Time-gaps → loads.** A gap beyond the load-threshold starts a new load/sweep (clean separation in the data: intra-sweep gaps ≤ 53 s vs. reload gaps ≥ 533 s). Multi-hour gaps mark macro-sessions. The directory yielded ~13 loads, ≤ 24 slots/load, 4 macro-sessions.
3. **Slot identity = stepping-axis position ⨉ filename token, cross-checked.** The stepping axis is the **motor with the dominant inter-burst variance** (Horizontal position in this dataset, where `dispx`/`Phi` are constant — but the axis is beamline-dependent, so it is *detected*, not hard-coded). Position and the filename token agree across this run; the token corroborates and may provide the label. Agreement → near-certain; disagreement or an unparseable convention → flag for the human. **Fallback when no axis varies** (single fixed position, single-exposure samples, or a setup that doesn't record the stepping motor): cluster by time + filename only and flag for review — position is the *preferred* backbone, not a required one.
4. **Each slot within a load = one distinct sample** (no series treatment; the user can group into a series later).

**The rack can stay in place and a subset of positions get reshot in any order** (no reload gap) — a position revisited within a load is another acquisition of that same sample.

**Thresholds are gap-relative and per-load, not global constants.** Slot clustering keys off the *ratio* of a position gap to the local within-burst jitter (slot tolerance = k × median consecutive-frame delta), and the load boundary keys off the bimodal time-gap histogram (intra-sweep ≤ 53 s vs. reload ≥ 533 s) — both derived **per load** from that run's own distribution, since slot spacing is itself bimodal across the directory (4 mm HA sweep vs. 16–31 mm JC plate) and no single global tolerance fits both. The measured figures (within-burst jitter ≈ 0.30 mm median, ±0.5 mm for the tight JC wells, ~4 mm HA spacing, ~300 s reload gap) are sanity bounds and starting points, not magic constants. This honors Principle #1: the numbers are re-validated per run, never baked in.

**Edge-case fallbacks (specify before building):** the load-boundary detector must not *assume* a clean bimodal gap histogram. Define the segmentation as `gap > k × median(all_gaps)` (validated `k`) with: a **unimodal fallback** (no clear separation → treat the directory as one load and flag for review) and a **>2-mode** handling rule. Slot clustering keyed on "gap ≫ median consecutive-frame delta" is undefined for **single-frame acquisitions** (no consecutive pairs) → fall back to the absolute-position gap learned from the multi-frame bursts in the same load, else flag. The **counter-reset-to-1** signal is demoted to a *corroborating* hint (it contradicts "assume as little as possible"); the filename-label match stays the primary cross-load identity signal.

**Naming.** One human label per sample, `name` (the single label field after the rename in §4):
- **Default:** the filename-derived label with the set/position coordinate as a parenthetical anchor — `<label> (SNNPMM)` (season/episode style: `S` = set/load index, `P` = slot position, zero-padded — e.g. `HA85 (S01P15)`), or a confident per-slot token like `JC C04 (S02P05)`.
- **No character restrictions** — the label is plain TEXT, only whitespace-trimmed (`routes_samples.jl:158`). The old `[A-Za-z0-9._-]` rule was a *manifest-parse* validation (`validate.jl:3`) the redesign retires; samples are addressed by numeric id, so there is no URL/slug constraint.
- The `SNNPMM` coordinate is the deterministic, always-available backbone; the filename label is corroboration. (HA is exactly the case that needs it: the HA filename number is a *global frame counter*, not a slot identity, so the coordinate disambiguates.)
- The user can rename anytime; a human edit sets `name_source = human` and is never clobbered on rescan. Across rescans a sample is re-identified by its **(load, slot) coordinate**, not by its label — so renames are safe.

**Needs the human / label (surfaced, never blind):**
- **Cross-load identity.** Slot number is **not** stable across loads (the same sample can land in a different slot; the same slot holds a different sample after a reload). So cross-load "is this a reshoot of that sample?" comes from the filename label, not position, and is surfaced as a Merge prompt (default: each load's slots are new samples). A counter resetting to 1 signals a fresh rack.
- **Within-load split.** When stage position jumps mid-group, suggest a Split at that boundary.

**Regroup is bookkeeping only — no reanalysis (within one experiment).** Verified against the schema: peaks (`auto_peaks`), curations (`peak_curations`), indices (`indices`/`index_peaks`), and assignments (`assignment_members`) are all keyed to `exposure_id`; `samples` is a pure container with no computed column. Changing which sample an exposure belongs to touches no derived data — **so long as the move stays within one experiment**: `analyze_exposure!` resolves `analysis_dir` + the integration pattern through the exposure's experiment, so a cross-experiment move would change the resolved `.dat` path and require reanalysis. Structural edits are therefore constrained to within-experiment (§9.2). (This retires the earlier brainstorm decision that "editing grouping reanalyzes affected samples" — it predated confirming analysis is exposure-keyed and geometry-independent. A saved Series is preserved by re-pointing membership on merge, never by deleting a sample — §9.3.)

## 6. Geometry derivation

No PRP-parsing code exists today (geometry is hand-entered in `experiment.toml`'s `[beamline]` block and read defensively by `_beamline_from_config` at `routes_experiments.jl:16`). The redesign adds it. Both source formats are now confirmed against the real SSRL data (2026-06-18).

- **From PRP** (constant across the experiment; verified in `data/HA_85_422_S2404_0_001.prp`, `Key=Value` lines): `Beam energy` → energy_kev (`9000.03 eV` → 9.00 keV), `Detector` → model (`Pilatus 1M`) → pixel pitch via a small **detector→pitch lookup** (a static table in `geometry.jl`, e.g. Pilatus 1M → 172 µm; an unknown detector leaves pitch unset and **flags for manual entry** rather than guessing), q-units, and `Pipe length` (`1700 mm`) as the *nominal* flight path (see authority below).
- **From the analysis-dir setup file** (format confirmed: `analysis/setup_info_<YYYYMMDD_HHMMSS>.txt`, 11 lines). Parse: `Beam center is at (X, Y)` → beam_center_x/y; the calibration-tif reference line (`File used for calibration: ...agbe...tif`); the per-ring distance block; and `Mean distance: N mm` → the **AgBe-ring-calibrated detector distance** (1809.5 mm here). Glob `setup_info_*.txt`; multiple → latest by the timestamp in the name (≈ mtime). Parse-fail/absent → leave unset, `source=default`, flag for manual entry. (Plan should still confirm the glob holds across other beamlines/experiments.)
- **Geometry authority — RESOLVED (data-backed):** the setup file's `Mean distance` is a **silver-behenate ring calibration** (the rigorous sample-to-detector distance used to compute q), while the PRP `Pipe length` is a nominal mechanical value (1809.5 vs 1700 mm ≈ 6.4%, propagating into every q). So **`flight_path_m` is sourced from the calibrated `Mean distance` (source `setup`)** when a setup file is present, falling back to PRP `Pipe length` (source `prp`) otherwise, with a >tolerance discrepancy flag. (Confirmed by Jonathan 2026-06-18: the flight path sets the accessible q-range, so the AgBe-calibrated distance is the correct basis; use the calibration numbers.)
- **Per-field source tag** (`prp | setup | human`, default `default` before derivation). An **Override** affordance lets the user correct any value; overrides are `human`-sourced and never refreshed on rescan.
- **Multi-setup discrepancy detection.** A directory should hold exactly one setup, but since we read every PRP during the scan anyway, we **verify the constant fields actually are constant** (beam energy, pipe length, detector) and surface any variance as a likely error to correct (a "geometry check found N issues" banner on the Configuration tab), rather than silently averaging or picking the first. This catches data-entry/beamline mistakes for free.
- This removes the hand-edited `[beamline]` block from `experiment.toml`.

## 7. Navigation & information architecture

Settled by the interactive prototype.

- **Two top-level axes in the global nav: Experiments | Series.** Experiments is the nav-default and the entry point; **Series is the cross-cutting folio** (it can span experiments) and **ships as the existing folio surface**. The retired single-corpus model's `Samples · Index · Series` stage-tabs and the flat global `/samples` root are gone (the corpus is now a tab inside an experiment).
- **Experiments home** = a gallery/list of experiments (status dot · serif name · `date range · directory` · sample/exposure counts · "N need grouping review"), with **+ New experiment**. There is **no persistent experiment sidebar** (dropped: it costs the dense tables their width). Experiments are switched via a **contextual header dropdown** shown only while inside an experiment.
- Selecting an experiment **persists the choice to Zustand**; return visits route straight to that experiment.
- **An experiment is one page with two tabs** at `/experiments/:id`: **Corpus | Configuration** (not two pages). The **Corpus tab reuses the real contact sheet** (`SheetTable`), scoped to the experiment. A **shared experiment header** sits above the tabs: kicker · **edit-in-place** serif name · **editable** description · a meta line (`date range · directory`) · rescan/ingest status · a quiet **stat ledger** (`loads · samples · exposures · sessions`, hairline-divided cells, no heavy rule) · the tab bar.
- **Grouping review is a dedicated surface, not a permanent tab**, reached from a sticky **"N samples need grouping review →" banner** on the Corpus tab (Gradescope-style: a task you return to, gone when the grouping is clean). Routing tree: `/experiments` · `/experiments/new` · `/experiments/:id/{corpus,grouping,config}`.
- **Soft-lens scope** (settled earlier): the experiment scopes the corpus and is the entry, but the data model is not partitioned — Series span experiments and samples stay globally addressable.
- **Open (§13):** whether the experiment switcher replaces or coexists with the SampleStepper in the topbar right slot.

## 8. UI surfaces (the reference)

The canonical reference is `docs/redesign-mockups/ingestion-prototype.html`. All in The Print: warm paper/ink, rationed terracotta, Newsreader for titles, mono for measurements, detector frames as dark windows, flat-except-the-plate, 5px radius. Faithful to `src/print/ui/` primitives. The surfaces (see §8.6 for the existing-component each maps to):

- **Corpus tab** — the experiment's primary view; **the real `SheetTable` contact sheet reused**, scoped to the experiment, with the sticky review banner above it. The first-ingest live-unfold (§8.3) is woven in here.

### 8.1 Configuration tab — `experiment-configuration.html`
Below the shared header: editable description + directory; a two-column body of **Geometry** (the auto-derived ledger, source-tagged, with Override, and the multi-setup discrepancy banner when triggered) and **Acquisition** (a timeline plate of the sessions/loads, the one lifted figure); then **Sources** (data/analysis directories + the `{name}.dat/.tiff/.prp` patterns). This is the experiment's identity + setup verification.

### 8.2 Grouping review surface (the centerpiece)
A **dedicated surface** reached from the Corpus review banner (not a tab). Purpose, narrowly: **verify every sample loaded, has its exposures, and is split/grouped correctly.** No phase, no scores. It carries a **fuzzy/glob filter** (e.g. `HA8*`, `JC C0?`) whose checkbox selection **persists across filtering**, so a cross-load merge is: check one sample → filter to its partner → check it → Merge (no scrolling at 170-sample scale). A **three-level fold**:
- **Load** (collapsible): operator · N samples · M exposures · time · status (`✓ grouped cleanly` / `⚠ N to check`).
- **Sample** (collapsible): serif name (e.g. `HA85 (S01P15)`) · "N exposures · time" · status; controls (Rename / Split / for flagged: Merge…/Split… with a dismiss option) in the fold header.
- **Exposure** (leaf, all shown — **no `+N` truncation**): small detector thumbnail · **filename** (mono) · stage H-position · time. Showing every filename + position is what makes the split *verifiable*; a `position jumps X → Y mm` divider marks an auto-suggested split boundary.

Flagged samples are pre-expanded; clean ones fold to one line. A per-exposure **Move…** allows fine regrouping.

**Flag provenance (resolved 2026-06-19): the grouping flag is backend-produced, not frontend-derived.** The per-sample merge/split flag is computed by the grouping core (§9.1) and returned on the loads roll-up (§9.2) — the frontend is a pure consumer that reads `sample.flag`, never re-deriving it. Rationale: the **split** flag is the grouper's own clustering confidence (it knows when it fell back to the single-frame path §5 — the frontend cannot reconstruct that from positions alone), and the **merge** flag is a global cross-load comparison the grouper already performs while iterating every exposure. Computing it anywhere else would duplicate grouping logic and risk the UI suggesting edits the grouper wouldn't make. **Dismiss ("Keep separate") is a durable event** (`grouping_flag_dismissed`, §9.3), so a dismissed suggestion stays suppressed across rescans (it is reconciled into the roll-up), not session-local.

### 8.3 First ingest (live unfold) — `experiment-first-ingest.html`
Woven into the Corpus tab, not a separate screen. The header shows ingest progress (`Processing · 312 / ~680 exposures`) instead of "up to date"; the stats ledger shows discovered-so-far counts with span/sessions pending; loads appear and fill as they are scanned (done · in-progress with a per-load bar · queued), with a footer "scanning · 5 of ~13 loads." Driven by `ingest_*` SSE frames (§9).

### 8.4 Empty / first-run — `experiments-empty.html`
An EmptyState: "No experiments yet" + the value proposition + a primary **New experiment** action (point at a directory). Sidebar reads "none yet."

### 8.5 Failed scan — `experiment-scan-failed.html`
Honest error (not a masquerading empty): "No exposures found in that directory" + a mono detail box (path · `looked for {name}.tiff + {name}.prp · matched 0 of 0 files`) + recovery (Retry scan / Edit file patterns / Choose another directory). The sidebar flags the experiment.

### 8.6 Component reuse mapping (reuse, don't rebuild)
Validated against live code (2026-06-18 sweep). All paths under `packages/HimalayaUI/frontend/src/`. Every primitive the prototype leans on already exists.

| Prototype element | Existing component | Path |
|---|---|---|
| Experiment switcher / Move-to-sample picker | `Menu<T>` | `print/ui/Menu.tsx` |
| Directory-picker suggestion dropdown | `Popover` | `print/ui/Popover.tsx` |
| Topbar + wordmark | `TopBar`, `Wordmark` | `print/ui/TopBar.tsx`, `Wordmark.tsx` |
| Top nav (Experiments \| Series), route-based | CorpusTopbar Link pattern (**not** `StageTabs`) | `print/shell/CorpusTopbar.tsx:125-151` |
| Corpus tab body + row parts | `SheetTable`, `SampleTableRow` | `print/components/SheetTable.tsx`, `SampleTableRow.tsx` |
| Detector thumbnail · phase chip | `Thumbnail`, `PhaseChip` | `print/components/Thumbnail.tsx`, `print/ui/PhaseChip.tsx` |
| Floating bulk-select bar | `CullBar` (labels hardcoded — parameterize) | `print/components/CullBar.tsx` |
| Guarded merge/split/move confirm | `ModalShell` + `ModalHead` + `ModalFooter` | `print/ui/ModalShell.tsx`, `print/components/Modal*.tsx` |
| Empty / failed states · buttons · cards · kicker · progress · search | `EmptyState`, `Button`, `Card`, `Kicker`, `ProgressBar`, `SearchInput` | `print/ui/*` |
| Inline name/description edit | `Input variant='title'` | `print/ui/Input.tsx:17` (live in `SeriesBuilderPage.tsx:910`) |
| Page centering · plate/folio headers | `PageFrame` (add width keys), `PlateHeader`, `FolioHeader` | `print/components/*` |
| App layout route | `CorpusShell` + `AppRoutes` | `print/shell/*` |
| "Undo last change" stack | inline `HistoryEntry[]` — **extract to `useUndoStack`** | `print/pages/SeriesScopingPage.tsx:77,312,492` |
| Series folio (the Series nav axis) | the existing folio surface | `print/pages/SeriesFolioPage.tsx` (reuse as-is) |

### 8.7 New components (build these)
| Name | Purpose | Builds on |
|---|---|---|
| `ExperimentsHomePage` | `/experiments` gallery: cards + EmptyState + New CTA | `FolioHeader`, `Card`, `Button`, `EmptyState` |
| `ExperimentShell` | `/experiments/:id` layout: shared header (edit-in-place name/desc, rescan, stat ledger) + tab bar + `<Outlet>` | `CorpusShell` pattern, `TopBar`, `Input`, `Kicker`, `ProgressBar` |
| `StatLedger` | hairline-divided mono stat band (no existing multi-stat primitive) | tokens |
| `GroupingReviewPage` | centerpiece: back/summary/filter+search/accordion/bulk bar; owns selection `Set` + undo | `CullBar`, `ModalShell`, `SearchInput`, `useUndoStack` |
| `LoadFold` / `SampleFold` / `ExposureLeaf` | the three accordion levels | `Card`, `Input variant='title'`, `Checkbox`, `Thumbnail`, `FlagButton` |
| `DirectoryPickerField` | path input + live validation + Tab-complete (**needs new backend endpoints**, §9.2) | `Popover`/`Menu`, `Input` |
| `LiveIngestUnfold` | scan-progress panel: ProgressBar + SSE counters + skeleton rows | `ProgressBar`, `StatLedger`, Skeleton |
| `GeometryLedger` | per-field value + provenance chip (prp/setup/human) + Override/Revert + discrepancy banner | `Card`, `PlateHeader`, `Button`, `Input` |
| `AcquisitionTimeline` | exposure-count-per-slot bar chart clustered by session — **new chart type; `TracePlot` cannot render it** | new d3/SVG (`PlotFrame`/`Axis` scaffolding) |
| `SourcesCard` | editable `{role, path}` rows + edit-in-place | `Card`, `Input`, `Button` |
| `FailedScanPage` | honest scan-failure recovery | `Card`, `Button`, `PageFrame` |
| `ExperimentConfigurationPage` / `ExperimentCorpusPage` | child route bodies under `ExperimentShell` (Corpus tab + Configuration tab) | the above + new queries (§9.6) |
| `useUndoStack<T>()` | shared undo hook extracted from SeriesScopingPage so grouping-review and scoping don't diverge | — |

### 8.8 Canonical cross-plan contracts (defined once in Phase E1, consumed by E2)

To prevent the two frontend plans from defining incompatible shapes, these are pinned **once** in `api.ts` / `queries.ts` by Phase E1 and **imported** (never redefined) by Phase E2. They mirror the backend `get_loads_rollup` JSON (§9.2) verbatim.

```ts
// api.ts — the loads roll-up (nested; matches GET /api/experiments/{id}/loads)
export type GroupingFlag =
  | { kind: "merge"; merge_with_sample_id: number; merge_with_label: string }
  | { kind: "split"; split_at_index: number; jump_from: number; jump_to: number }
  | null;
export interface LoadExposure { id: number; filename: string; horizontal_position: number | null; timestamp: string | null; }
export interface LoadSample {
  sample_id: number; name: string; slot_index: number;
  grouping_source: string; name_source: string; merged_into_id: number | null;
  flag: GroupingFlag; exposures: LoadExposure[];
}
export interface Load {
  load_id: number; load_index: number; session_id: number | null;
  start_time: string | null; end_time: string | null;
  frame_count: number; note: string | null; samples: LoadSample[];
}
```

- **`queryKeys.loads(id)` = `["experiment", id ?? "none", "loads"]`** — one shape, mirroring the live `queryKeys.samples` family so an experiment-prefix invalidation also refreshes loads. Signature `loads(id: number | undefined)`. `useLoads(id)` is gated `id > 0`. (E2's earlier `["loads", id]` is dropped.)
- **`Sample.name` is non-null** (`string`) after the `display_name → name` collapse — the always-populated label subsumes the old fallback. The collapse + `persistence.ts` `SCHEMA_VERSION` 4 → 5 bump are owned by **exactly one** sequenced task in E1; E2's collapse step is verification-only.

## 9. Backend & API surface

The redesign is mostly net-new backend work, but it slots onto existing rails.

### 9.1 Shared grouping core (new modules)
A single ingest core, callable from both the HTTP API and the CLI:
- **`prp.jl`** — `parse_prp(path) → NamedTuple` (key=value lines; strip units; defensive: unparseable → `missing`).
- **`geometry.jl`** — `derive_geometry(prp_paths, setup_files) → (geometry, discrepancies)`; samples PRPs, verifies the constant fields, maps detector→pitch, parses the latest setup file for beam center.
- **`grouping.jl`** — the §5 backbone: `scan_directory(dir, patterns)` (filesystem discovery, reusing `resolve_files`/`resolve_file_path` from `config.jl:202`), then `group_into_samples(meta) → loads/samples`. (Named distinctly: `auto_group` already exists in `pipeline.jl:30` for *index*-candidate grouping — an unrelated concept.) **`grouping.jl` also exposes `derive_sample_flags(load_rows) → Dict{sample_id, GroupingFlag}`** (§8.8) — a **pure** function over the grouped sample/exposure rows (no DB writes): a **merge candidate** when a sample's filename label recurs in another load (the cross-load identity comparison the grouper already performs), and a **split suggestion** (`split_at_index` + `jump_from`/`jump_to`) when a sample spans a position jump beyond the local within-burst jitter (including the single-frame round-robin fallback §5 the grouper could not split). It is a **read-time derivation, never a stored column** (consistent with "grouping view is re-derived from `(load_id, slot_index)`", §9.3c): `get_loads_rollup` (Phase D, §9.2) calls it over the rows it reads and suppresses any flag with a non-undone `grouping_flag_dismissed` event. Keeping the logic in `grouping.jl` (not the frontend) means the split flag reflects the grouper's *own* clustering confidence, which the frontend cannot reconstruct.
- **`scan_and_group!(db, exp_id, dir; additive)`** — the transactional orchestrator. Reuses `_DB_WRITE_LOCK` (`server.jl:29`), the per-exposure `analyze_exposure!` (`pipeline.jl:913`, idempotent + hash-guarded), and the insert-only upsert discipline already in `_reingest_inner!` (`cli.jl:156`). On rescan it inserts only files absent from the dedup key and analyzes only those.
- **Required edit to `analyze_exposure!`:** today it resolves the experiment (and thus `analysis_dir` + integration pattern) by JOINing `exposures → samples → experiments` (`pipeline.jl:920-922`), so an exposure with no `sample_id` yet produces no row → `error("exposure … not found")` (`pipeline.jl:925`). Rewire it to read the new denormalized `exposures.experiment_id` directly. Ingest order: group into samples first, then analyze (or decouple analysis from `sample_id`) — analysis must not depend on the sample assignment.

**Validated 2026-06-18 (corrections folded in):**
- *Reuse is pattern-only, not a call-through.* `_reingest_inner!` requires a manifest and returns `:no_manifest` early; `scan_and_group!` is net-new and clones the discipline, it does not call it. And the matched-sample branch at `cli.jl:203` does an **unconditional** `UPDATE samples SET display_name = ?, notes = ?` — it is insert-only for *exposures* but **clobbers sample fields**. The never-clobber contract (§4) must therefore gate human-set fields on `name_source = 'human'`; scan/rescan only refreshes derived facts.
- *`analyze_exposure!` signature migrates.* It currently takes `analysis_dir::String` as a caller-passed param (`pipeline.jl:913`); resolving the experiment internally changes the public signature. **All** call sites must move (or keep a compat shim): `cli.jl` `_analyze_experiment!` **and** `routes_experiments.jl:102` (inside `POST /api/experiments/{id}/analyze`, `analyze_exposure!(db, Int(ex.id), String(analysis_dir))`). Decide shim-vs-atomic in the plan.
- *Directory-scan dispatch gap.* `resolve_files`/`resolve_file_path` take an `ExperimentConfig` for dispatch only (`config.jl:202,237`); `scan_directory` has no fully-formed config yet, so either loosen the dispatch arg to a config-free form or construct a minimal config (§13).

### 9.2 REST endpoints (new / changed)
- **`POST /api/experiments`** — create-from-directory: `{ path, name?, patterns? }`. Returns the experiment id immediately and kicks off the first scan asynchronously; progress streams over SSE. (Replaces the pre-create-`experiment.toml`-then-`init` dance.)
- **`POST /api/experiments/{id}/scan`** — rescan: cheap change-check, then additive ingest of new files. Idempotent (a no-change scan does nothing). Supersedes `POST /api/experiments/{id}/reingest`.
- **`GET /api/experiments/{id}`** — extended to include typed geometry + per-field source, ingest status, and the loads/stats roll-up.
- **`GET /api/experiments/{id}/loads`** — the Load ▸ Sample ▸ Exposures roll-up for the grouping-review surface. **A dedicated endpoint** (resolved: not folded into the flat corpus samples response), keyed `queryKeys.loads(id)`, distinct from the flat corpus `queryKeys.samples(experimentId)` (§9.6). Returns the **nested `Load[]` shape of §8.8** including each sample's `flag` (the rollup builds the nested rows from the DB, then calls `grouping.jl`'s `derive_sample_flags`, §9.1, and suppresses any flag with a non-undone `grouping_flag_dismissed` event, §9.3), and each exposure's `horizontal_position` + `timestamp` (the data the exposure leaf renders + the inputs `derive_sample_flags` needs).
- **Structural grouping edits** — `rename` / `move` are thin single-entity event-sourced routes; `merge` / `split` are route-level orchestrations over per-exposure moves + a sample create/retire (§9.3). All are **within-experiment only** — a cross-experiment move would change the resolved `analysis_dir`/integration pattern and invalidate the analysis (§5). None trigger reanalysis for same-experiment moves.
- **Geometry override** — `PATCH /api/experiments/{id}` writes the field + sets its source to `human`; never refreshed on rescan. This **replaces** today's read-only PATCH stub (`routes_experiments.jl:73-85` returns 400 "experiment metadata is read-only" for *every* PATCH) and requires widening the frontend `updateExperiment` helper, whose patch type is currently `Record<string, never>` (`api.ts:125`, literally uncallable today). The typed geometry columns (§10) must land first.
- **Directory picker support (new)** — the `DirectoryPickerField` needs two small read-only endpoints that **do not exist today**: path autocomplete suggestions and a `validate-path` probe (returns matched file counts + a derived-geometry preview). Backend prerequisite for the create flow.

### 9.3 Event kinds

**The event log is single-entity and replayed.** `apply_event!` keys each event to one `entity_id`, and `rebuild_views_from_log!` re-folds events per `(entity_type, entity_id)` from empty. An edit is "free via the dispatcher" only if it mutates exactly one entity and is replay-idempotent. That shapes the classes below.

- **`exposure_moved` and `sample_renamed` are genuine single-entity events** (durable, replay-safe). Moving one exposure to a different sample is keyed to that exposure's `entity_id`; renaming is keyed to the sample. These inherit idempotency + SSE + undo (`undoes_event_id`) and can splice the cache directly.
- **Structural event payloads carry `experiment_id` (resolved 2026-06-19), so the frontend can invalidate the right experiment's `loads(id)`/`samples(id)`.** The frontend reads these payload fields (never `remote.entity_id` for the experiment). Canonical payloads:
  - `exposure_moved` (entity = exposure): `{ sample_id, from_sample_id, experiment_id }` — `sample_id` is the destination (the dispatcher writes `exposures.sample_id` from it); `from_sample_id` lets the frontend invalidate the source sample's exposure list too.
  - `sample_renamed` (entity = sample): `{ name, experiment_id }`.
  - `sample_created` (entity = the new sample): `{ experiment_id }` — emitted by `split` (and the create step of a merge-rename). The frontend **must** carry a `sample_created` cache arm that does invalidate-only refresh of `samples`/`corpusSamples`/`loads`; without it the frame falls into the `peaks`/`indices`-poisoning `default:` arm.
  - `sample_split` (entity = the source sample): `{ new_sample_id, exposure_ids, experiment_id }`.
  - **There is no `sample_merged` event kind** — a merge fans out as per-exposure `exposure_moved` frames (+ a `sample_renamed`/`update_sample` if the survivor is renamed) and retires the loser via `merged_into_id`. The frontend has **no `sample_merged` arm**; it reconciles a foreign merge from the `exposure_moved` burst.
- **`grouping_flag_dismissed` (new, durable, single-entity, undoable).** "Keep separate" on a merge/split suggestion writes this event (entity = the flagged sample), payload `{ flag_kind: "merge" | "split", merge_with_sample_id?, experiment_id }`. `get_loads_rollup` (§9.2) joins against non-undone dismiss events and **suppresses** the matching flag, so the suggestion does not reappear on rescan. It inherits `undoes_event_id`, so an undo un-suppresses it.
- **`sample_merged` / `sample_split` are orchestration, not single-entity view-writes.** They touch *two* sample rows plus N exposures, and minting sample rows via `AUTOINCREMENT` is **not** replay-idempotent through `update_view_for_event!` (a replayed "create" mints a new id, breaking FKs). So: (a) implement them in the route as a sequence of per-exposure `exposure_moved` writes plus an explicit sample create/retire, each authored directly — durable `user_actions` rows for audit/SSE, the **whole route body is wrapped in one `with_idempotency` block** (the HTTP-response cache covers the entire multi-event sequence; the inner per-exposure `apply_event!(InTransaction())` calls are each individually idempotent via the `(client_op_id, action, entity_id)` UNIQUE index). **Undo is session-local** (the frontend `useUndoStack`, cleared on reload): the `user_actions` rows are an audit trail, not a server-side multi-row undo API; a single `undoes_event_id` stamp cannot reverse a multi-row reassignment. The retired loser is represented by a nullable `merged_into_id` (not a hard delete); if the merge renames the survivor, the inverse sequence includes an `update_sample`. (b) **merge is a re-point, never a delete** — reassign the loser's child rows to the survivor across **all** tables that reference `samples(id)`: `exposures` (`db.jl:50`), `series_samples` (`db.jl:820`), `sample_tags` (`db.jl:42`), `sample_messages` (`db.jl:156`). Most have **no `ON DELETE` → default RESTRICT** under `PRAGMA foreign_keys=ON`, so a naive DELETE of the loser FK-errors before it can start; `series_samples` is `ON DELETE CASCADE`, so even with the others handled a delete would silently drop the sample from saved Series. Re-point every child first, then retire the loser. **Two pre-merge dedups (resolved):** (i) `series_samples` has `UNIQUE(series_id, position)` (`db.jl:824`) but **no `UNIQUE(series_id, sample_id)`** — so a re-point UPDATE changing only `sample_id` does *not* trip the constraint; the real hazard is **semantic** (the survivor would appear twice in one series). Resolve by **silently dropping the loser's membership in any series the survivor already belongs to, with a warning toast naming the affected series** (erroring would block any merge of a sample that's in a series): inside the single transaction, `DELETE FROM series_samples WHERE sample_id = loser AND series_id IN (SELECT series_id FROM series_samples WHERE sample_id = survivor)` **before** the re-point UPDATE (decide whether to compact position gaps). (ii) `sample_tags` has `UNIQUE(sample_id, key)` (`db.jl:1596`) — same pattern: drop the loser's tag row where the survivor already holds that key (survivor wins) before re-point. `sample_messages` re-point is a plain UPDATE with no uniqueness risk. The sample-create step follows the `comparison_created` precedent (`events.jl:538`: route mints the id, dispatcher upserts). (c) the **loads/samples grouping view is re-derived** from the `(load_id, slot_index)` coordinate (a derive, per the plotting-redesign "rebuild-not-log-derivable" precedent), not folded from the log.
- **SSE cache handling for merge/split is invalidate-only.** Because they create/destroy sample rows whose ids aren't payload-derivable, their `applyRemoteToCache` handlers **refetch** the `loads(id)` roll-up (and the flat `samples` list) (the `series_created` precedent), not surgical splices.
- **Transient ingest progress** broadcasts **without** a durable `user_actions` row. `broadcast_event!` hardcodes `event: curation` and forces `entity_id => Int(entity_id)` (`events.jl:1106-1120`) and requires an `event_id` from a `user_actions` INSERT — so do **not** reuse it. The new helper **`broadcast_progress!` calls `_try_put!` directly** (`events.jl:1067`): no `user_actions` row, no `event_id`, no FK contract. **Resolved frame shape:** it rides the existing **`curation`** SSE event name (cheapest — no new `addEventListener` in `App.tsx:33`); the payload carries `kind ∈ {ingest_started, ingest_progress, ingest_complete, ingest_failed}` + a **positive, non-zero `experiment_id`** (never a sentinel `entity_id`) + counts. **Frontend (resolved):** the `ingest_*` cases discriminate on **`kind`** and read **`payload.experiment_id`**, **never** `remote.entity_id` (which `applyRemoteToCache.ts:105` reads unconditionally before the switch); the four arms land **before** the `default:` arm (`applyRemoteToCache.ts:334`, which fires bogus `peaks`/`indices` invalidations). The channel cap is 64 (`server.jl:92`); a 680-exposure scan can drop progress frames, so treat `ingest_complete` as the authoritative terminal state. **`broadcast_progress!` (backend) and the four `ingest_*` arms in `applyRemoteToCache.ts` (the cache path) ship as one deploy unit** so no progress frame reaches the poisoning `default:` branch. The companion `ingestInFlight` **store writes live in a separate `App.tsx` SSE listener**, not in `applyRemoteToCache` (which stays pure — §9.6).

### 9.4 Auto-rescan scheduler
Follows the existing GC-timer pattern (`start_gc_timer!`, `server.jl:144` — a `Timer(0; interval)` held in a module `Ref`):
- A **per-experiment poll Timer** keyed by `experiment_id`, started when the first samples arrive from a scan.
- Each tick runs the **cheap change-check** first; only a changed directory triggers ingest + analysis + broadcast. Unlike the GC timer's fast single-DELETE callback, a scan can outlast its interval, so the tick needs a **reentrancy guard** (skip-if-already-running, or stop/restart the Timer around each scan) and the heavy scan/analyze work must run **off the timer callback** (`@spawn`) so it doesn't stall the libuv timer loop.
- **Tiered backoff** (exponential-retry style): start at the **fast tier** (every 1–6 h) while exposures are actively arriving; after N consecutive empty ticks (the run has gone quiet) back off to **daily**; after a few more empty daily ticks, **stop** the Timer → the experiment drops to **manual-rescan-only**. Bounded by max beamtime ~1 week. A manual Rescan (or auto-tick) that finds new files **re-arms** the schedule at the fast tier.
- Timers are torn down on server shutdown (mirror `stop_gc_timer!`) and on experiment delete — **note no experiment-delete route exists today** (`routes_experiments.jl` has GET/GET-id/PATCH/analyze/reingest only), so the scheduler work includes building that delete path with timer teardown. All writes go through `_DB_WRITE_LOCK`.

**Validated 2026-06-18:** the GC timer is a *single* `const GC_TIMER` Ref (`server.jl:134`); the rescan scheduler needs a **new per-experiment `Dict{Int,Timer}`** keyed by `experiment_id`, not reuse of that Ref. **Reentrancy (resolved):** a bare `Dict{Int,Bool}` "running" flag set on the libuv timer thread and read by the `@spawn` task thread has a **TOCTOU race**; protect it with an explicit **per-experiment `ReentrantLock`** (a new lock, **not** `_DB_WRITE_LOCK`, which carries write-ordering semantics at `server.jl:25-28`), or stop/restart the Timer around each tick. The GC callback is a fast inline DELETE with no guard, insufficient for a multi-minute scan. **Backoff-tier state (resolved): a DB column** — `experiments.last_scan_tier` + `consecutive_empty_ticks`, written on tick completion, so a server restart doesn't reset every quiet experiment to the fast tier (an in-process `Dict` would). Auto-tick-emitted events use `_system_request()` (`events.jl:292`) for the NULL `user_id`.

### 9.5 Fate of the CLI
The CLI stays as a headless/scriptable path and for server boot (`serve` is unchanged). `init`/`reingest` are rewritten to call the **same `scan_and_group!` core** instead of the manifest path; manifest support is retained only as a legacy fallback (or dropped during migration — decided in the plan). The grouping engine is shared, so CLI and API never diverge.

### 9.6 Frontend wiring (validated against live code)
Non-component changes for the new surfaces (paths under `packages/HimalayaUI/frontend/src/`):

- **Routing (`print/shell/AppRoutes.tsx`):** add `/experiments` (home), `/experiments/new`, and an `/experiments/:id` layout route (`ExperimentShell`) with children `corpus` / `grouping` / `config`, placed **outside** `CorpusShell` so the experiment header doesn't stack on `CorpusTopbar`. **Decisions (resolved):** `/` redirects to **`/experiments`** (was `<Navigate to="/samples">` at `AppRoutes.tsx:116`); because `ExperimentShell` sits outside `CorpusShell` it **renders its own top chrome** (wordmark + Experiments|Series nav + the experiment header + Corpus/Configuration tabs), so the "add Experiments link in CorpusTopbar" instruction does not apply there. "Experiments" is also added to `CorpusTopbar`'s **`STAGES` array** (`CorpusTopbar.tsx:27-30`, with `pathname.startsWith` active-detection) for any non-experiment surface, not just a bare `<Link>`. Update e2e specs targeting the `/samples` root.
- **Store (`state.ts`):** ephemeral `ingestInFlight: Record<number,{processed,total,status}> | null` + setters; **NOT** in `partialize`, **no** store-version bump (UI-only state).
- **Queries (`queries.ts`):** add `queryKeys.experimentConfig(id)` and `queryKeys.loads(id)` (the Load▸Sample▸Exposure roll-up) — distinct from the existing `queryKeys.samples(experimentId)` (flat `Sample[]`), no collision. New hooks: `useExperimentDetail/Config/Loads`, `useUpdateExperiment` (the geometry-override mutation — E1 ships only read hooks; this and the structural-edit hooks are E2's), `useStartIngest`, `useIngestProgress`, `useSaveGeometryOverride`, `useRevertGeometryField`, and the structural-edit hooks **`useMoveExposure`** (moves one exposure between samples — the accurate name; the earlier `useMoveSampleToLoad` was a misnomer), `useRenameSample`, `useMergeSamples`, `useSplitSample`, `useDismissGroupingFlag`.
- **Structural SSE arms (`applyRemoteToCache.ts`), aligned to the real Plan-D payloads (§9.3):** `exposure_moved` reads `payload.sample_id`/`payload.from_sample_id` (invalidate both samples' exposure lists) + `payload.experiment_id` (invalidate `loads(id)`); `sample_renamed` splices the entity + invalidates `samples(payload.experiment_id)`; **add a `sample_created` arm** (invalidate-only `samples`/`corpusSamples`/`loads(payload.experiment_id)`) — without it the split's create frame falls into the `peaks`/`indices`-poisoning `default:`; `sample_split` invalidates `loads`/`samples(payload.experiment_id)`; **there is no `sample_merged` arm** (merge fans out as `exposure_moved`, §9.3). All read `payload.experiment_id`, never `remote.entity_id`. **Own-op reconciliation:** the initiating tab's merge/split/move/rename mutators **invalidate `loads(experimentId)` in their `onSuccess`** (the real `series_created`/`saveSeries` precedent) — the replay coordinator's own-op path runs `applyPostStateOnly` (which bails with no `post_state`) and never calls `applyRemoteToCache`, so relying on the SSE arm alone leaves the optimistic negative-id split sample unreconciled.
- **Queue (`lib/queue/`):** four explicit `ingest_*` cases in `applyRemoteToCache.ts` **before** the `default:` (line 334), discriminating on top-level `kind` and reading `payload.experiment_id` (never `remote.entity_id`, read unconditionally at line 105). The backend `broadcast_progress!` (§9.3, Plan C) **must wrap its data as `{kind, payload:{experiment_id, processed, total}}`** to match this parse path and every other `curation` frame; ingest frames carry no `client_op_id`, so the `App.tsx` listener **short-circuits `ingest_*` before `handleRemoteEvent`** (does the store write + cache invalidation inline and returns) to avoid a full pending-queue rollback/rerun on every progress frame. **Keep `applyRemoteToCache` pure** — it imports only TanStack types today and AGENTS.md mandates Zustand=client / TanStack=server; worse, the replay-as-rerun loop calls it mid-rollback, so a Zustand write there would flash stale progress. The `ingest_*` **store writes go in a separate `App.tsx` SSE listener** (Zustand already imported there), the cache invalidation stays in `applyRemoteToCache`. The `update_sample` branch (line 314) payload key flips `display_name → name` (note the untyped `{...payload}` spread is a false-green cross-version hazard — sequence the deploy so a pre-deploy SSE payload carrying `display_name` can't land an invisible key). `mutators/trivial.ts` `UpdateSampleInput` (line 41) / `patchOf` (99) / `onSuccess` (86) updated.
- **Persistence (`persistence.ts`):** bump `SCHEMA_VERSION` **4 → 5** (it is `4` today at line 14, not 2 — the "v2→v3" was draft residue). The framework **drops** any op whose `schemaVersion` mismatches on rehydrate (line 122) with the existing "N edits from a previous session couldn't be restored" toast — it does **not** rewrite payloads; pre-deploy `update_sample` ops carrying `display_name` are simply dropped (acceptable, the user redoes the rename).
- **SSE (`print/App.tsx:33`):** the subscriber listens only on the `"curation"` event; `ingest_*` **rides `curation`** (§9.3) and a separate listener updates `ingestInFlight`.
- **Types (`api.ts`):** widen `updateExperiment` from `Record<string,never>` (line 125) to `ExperimentGeometryPatch`; extend `Experiment` (typed geometry + `*_source` + `last_scanned_at`/`scan_signature`/`ingest_status`); add a `Load` interface + `listLoads`/`createExperiment`/`triggerScan`; drop `display_name` from `Sample` (line 35).
- **Label sweep (atomic, ~32 occurrences across ~14 files):** `lib/sample/displayName.ts:8` (collapse to `s.name || 'Sample #${id}'`, **and drop `display_name` from its `Pick<Sample,'id'|'name'|'display_name'>` constraint** — this ripples to every caller), `api.ts` `Sample` type (line 35), `mutators/trivial.ts` (`UpdateSampleInput` 41 / `patchOf` 99 / `onSuccess` 86), `applyRemoteToCache.ts:314` (the untyped-spread hazard above), `FocusPage`, `LoupePage`, `SeriesBuilderPage`, `SeriesScopingPage`, `AddSamplePicker`, `lib/comparison/labels.ts`, `lib/scoping/proposeOrdering.ts`, `NavModal.tsx` (drop the secondary machine-name line entirely), plus `persistence.ts`/`builderAdapters.ts` comments. `IndexSlugRedirect.tsx` needs no change (already 404→`/samples`). Land it as one focused refactor commit; the implementation plan should regenerate the exact checklist with a fresh `grep -rn display_name`.
- **`PageFrame.tsx`:** add `home` (~1080px) and `experiment` (~1280px) keys to `PAGE_WIDTHS`.
- **`Toast.tsx`:** `ToastContainer` has no action slot; the prototype's bottom-center undo toast needs an optional `{label,onClick}` action or a `CullBar`-style undo bar.

## 10. Schema direction

Additive; the existing schema already carries some bones (source-tagged `sample_tags`/`exposure_tags`, `trace_hash`/`analysis_inputs_hash`, `exposure_sources`). Concrete sketch (exact DDL + a `schema_migrations` sentinel belong to the implementation plan):

- **experiments:** promote geometry to typed columns (`beam_center_x`, `beam_center_y`, `pixel_size_um`, `q_units` — today these live only in the TOML blob) alongside the existing `energy_kev`/`flight_path_m`; add a **per-field source** column for each (`*_source ∈ {prp, setup, human, default}`); add `last_scanned_at` + `scan_signature` (max mtime + file count) for the cheap change-check, `ingest_status`, and the scheduler's `last_scan_tier` + `consecutive_empty_ticks` (§9.4); retire `manifest_path`. **`flight_path_m` is the single authoritative sample-to-detector distance used for q** (§6: the calibrated AgBe `Mean distance` when a setup file exists → `flight_path_m_source = 'setup'`, else PRP `Pipe length` → `'prp'`). There is **no separate `detector_distance_mm` column** — one physical quantity, one column; the nominal-vs-calibrated gap is computed at derive time and surfaced as the discrepancy flag, with only the chosen value persisted.
- **loads:** **new table** (`id`, `experiment_id`, `load_index`, `session_id`, `start_time`, `end_time`, `frame_count`, `note`) so Load ▸ Sample ▸ Exposures is first-class and queryable; computed at ingest from the time-gap segmentation.
- **grouping flag: no new column.** The per-sample merge/split flag (§8.8, §9.1) is **derived** by `group_into_samples` and recomputed on every grouping pass (consistent with "the grouping view is re-derived from `(load_id, slot_index)`", §9.3c) — it is never persisted on `samples`. The only durable flag-related state is the **`grouping_flag_dismissed`** event (§9.3), which rides the existing `user_actions` table (no new table/column); `get_loads_rollup` left-joins non-undone dismiss events to suppress the matching flag.
- **samples:** still a pure container; collapse the two text columns to one (both `name` `db.jl:1442` and `display_name` `db.jl:1455` exist today). **Migration order (validated, SQLite-safe).** The hazard is subtle: `migrate_samples_naming!` (`db.jl`) creates `samples_unique_name ON samples(experiment_id, name)` (`db.jl:1492`), and **SQLite `RENAME COLUMN display_name → name` re-points existing indexes onto the renamed column** — so a surviving `samples_unique_name` would silently re-impose uniqueness on the *user label* (auto-generated `HA85 (S01P15)` collides across loads), the exact thing we must avoid. Steps, sequenced in `migrate_schema!` **after** `migrate_samples_naming!`: (1) `DROP INDEX samples_unique_name` (required both because SQLite refuses to drop a column in a UNIQUE index *and* so it isn't inherited by the renamed column); (2) `DROP COLUMN name` (the manifest machine id); (3) `RENAME COLUMN display_name → name`; (4) **do NOT** recreate any UNIQUE on the label. Also **retire the duplicate-suffix renaming pass** in `migrate_samples_naming!` (`db.jl:1463-1489`) — it disambiguated a unique machine name and is wrong once labels may legitimately repeat. Final shape: `(id, experiment_id, name, notes)` + `load_id` (FK), `slot_index`, `grouping_source ∈ {auto_position, auto_time, manual, user_merged}`, `name_source ∈ {auto, human}` (never-clobber: a `human` name survives rescans; the scan UPDATE is gated on `name_source = 'human'`, fixing the unconditional clobber at `cli.jl:203`). **`create_sample!` (`db.jl:1828`) both accepts `display_name:` and names it in its INSERT** (`INSERT INTO samples (experiment_id, name, display_name, notes)`, `db.jl:1834`) — so the INSERT **fails** against the post-rename schema until updated. The column drop and the `create_sample!` INSERT change must land in the **same migration commit** (no dual-accept window). Callers to update atomically: `cli.jl:199` (`create_sample!(...; name=, display_name=)`), the unconditional clobber `UPDATE samples SET display_name=…` at `cli.jl:202-204` (also the `name_source='human'` gate target), and any test fixtures passing `display_name:`. Across rescans a sample is re-identified by its **(load_id, slot_index)** coordinate, which replaces the old by-name upsert match. The manifest-era machinery is deleted (reingest upsert-by-name `cli.jl:192`, CLI `-s <name>` filter, charset validation `validate.jl:3`); **`GET /api/resolve` is KEPT** — after the rename its sample branch transparently queries the renamed `name` column (§4).
- **exposures:** add `experiment_id` (denormalized — the dedup key moves to `(experiment_id, filename)` since `sample_id` is ephemeral during grouping, and `analyze_exposure!` reads it directly to resolve the experiment — §9.1), `prp_path`, `timestamp`, `exposure_time`, `horizontal_position` (or the detected stepping-axis position), `scan_id`, `frame_no`, `load_id`, and a content fingerprint for dedup.
- **dedup key:** `UNIQUE(experiment_id, filename)` (replacing today's `UNIQUE(sample_id, filename)` at `db.jl:1551`); rescan inserts only absent files. **Migration order:** add `exposures.experiment_id` → backfill from the `samples` JOIN → drop the old `UNIQUE(sample_id, filename)` index → create the new index (SQLite cannot enforce the new uniqueness until the column is backfilled and the old index dropped; mirror the dedupe-then-enforce discipline of `migrate_exposures_unique_filename!` at `db.jl:1504-1551`). `create_exposure!` takes only `sample_id` today — it must accept + persist `experiment_id`. Explicitly `DROP INDEX exposures_unique_filename` (`db.jl:1550`) before creating the new one so the old `(sample_id, filename)` key doesn't coexist and block a legitimate same-filename-different-sample insert. **Two backfill hazards (validated):** (a) `exposures.sample_id` is nullable (`db.jl:50`) and `migrate_exposures_unique_filename!` already scopes `WHERE sample_id IS NOT NULL` (`db.jl:1523`), proving NULL rows exist; a NULL `experiment_id` escapes `UNIQUE(experiment_id, filename)` (SQLite treats each NULL as distinct) and is invisible to the denormalized-`experiment_id` `analyze_exposure!`. **Policy (resolved): fail-fast** — the migration aborts and lists any `exposures` row with no derivable `experiment_id`, requiring manual cleanup, rather than silently tombstoning. (b) moving from per-sample to per-experiment filename uniqueness assumes no legitimate cross-sample duplicate filenames exist — verify against real data before dropping the old constraint.
- **untouched:** `indices`, `auto_peaks`, `peak_curations`, `index_peaks`, `index_groups`, `assignments`, `assignment_members` — all keyed to `exposure_id`; re-grouping reassigns `exposures.sample_id` only and leaves every derived table intact (§5).

## 11. Implementation TODOs (specify before/while building)

- **Net-new core (build spine, phase first):** the modules and surfaces below do not exist yet and gate everything else — `prp.jl` / `geometry.jl` / `grouping.jl` (+ `scan_and_group!`), the `POST /api/experiments` create-from-directory endpoint, the `loads` table + the exposures/samples/experiments migrations (§10), and the `broadcast_progress!` helper (§9.3). Phase the plan core-first: schema + grouping core → scan/rescan API + SSE progress → structural-edit events → frontend `/experiments/:id` page.
- **Guarded structural edits:** `Merge…` / `Split…` / `Move…` open an inline confirm that previews the result and commits with **undo** (the critique's P0 — regrouping is the product, it must be safe). `undoes_event_id` backs single-row undo (rename, one move); merge/split undo is **route-authored inverse writes** (§9.3), since one stamp cannot reverse a multi-row reassignment. Demote primary actions from filled-accent to outline-accent so the dismiss option is co-equal.
- **Auto-split threshold:** the position jump that triggers a split suggestion — gap-relative per §5 (a jump ≫ the local within-burst jitter), derived per load, not a fixed ~4 mm.
- **`name`/`display_name` consolidation sweep (multi-layer, atomic):** collapsing to one label column touches SQL + the queue persistence blob + ~32 TS occurrences across ~14 files + the PATCH route + SSE payload keys, all of which must move together (or the backend PATCH accepts both keys during a transition window). (1) Delete the manifest machine `name`'s by-name uses: reingest upsert-by-name, CLI `-s <name>`, charset validation. **Keep `GET /api/resolve`** (a general slug resolver whose sample branch queries `samples.name` at `routes_resolve.jl:91`, live caller `IndexSlugRedirect.tsx`) — after the rename it transparently resolves the renamed `name` column, no change needed. (2) Rename `display_name → name` everywhere (full list + the queue `update_sample` branch + the `persistence.ts` 4→5 bump in §9.6). **Delete** (don't rename) NavModal's secondary machine-name line. Sequence so no reader references a column mid-migration; land it as a focused refactor commit ahead of the feature work. Also extract **`useUndoStack`** (currently inline in `SeriesScopingPage.tsx:77`) before building grouping-review so the two surfaces share one undo implementation.
- **Accessibility / efficiency:** `aria-expanded` on the folds, a keyboard path through Load/Sample/Exposure, and a bulk-select for renaming/regrouping many samples at once (none shown in the mockups; required for 170-sample scale).
- **Rescan cost model:** cheap change-check (listing + mtime vs. `scan_signature`) before any hashing; hash/parse only new or changed files. The adaptive schedule (§9.4) bounds background work.
- **Frontend scaffolding (full validated list in §9.6):** new `/experiments/:id` layout route with **Corpus/Configuration tabs + a dedicated grouping-review route** (`AppRoutes.tsx`); ephemeral `ingestInFlight` store field — added to `AppState` + a named action but **NOT** to `partialize` and **NOT** a persist-version bump (it's ephemeral; bumping `version` without a key-preserving migrate risks wiping the persisted blob); `queryKeys.experimentConfig(id)` / `loads(id)` (the Load▸Sample▸Exposure roll-up — distinct from the existing `samples(experimentId)` key); explicit `ingest_*` cases in `applyRemoteToCache` routed ahead of the `entity_id` guard (§9.3); widen `updateExperiment`'s patch type from `Record<string, never>` to a typed geometry partial; a live-unfold skeleton pattern (append new rows, don't reorder).
- **Test strategy (TDD + the six-layer contract rule apply — see `docs/contract-testing.md`):** Julia unit/property tests for `prp.jl` / `geometry.jl` / `grouping.jl` / `scan_and_group!` against real fixtures, using **regression-floor** assertions for grouping (per the repo convention), not exact counts; `test_events.jl` rebuild-round-trips for the new `exposure_moved` / `sample_renamed` kinds and the merge/split orchestration (incl. the child-row re-points + series/tag dedup); a `broadcast_progress!` test asserting it emits on `curation` with no `user_actions` row; migration tests for the name collapse + dedup-key + NULL-fail-fast; Vitest for the four `ingest_*` `applyRemoteToCache` arms (kind-discriminated, no `default:` poisoning) + the separate store listener; Playwright for the grouping-review flow (merge/split/move/undo, filter-then-select). Pair backend+frontend per the six-layer rule for the queue/SSE/cache paths.

## 12. Out of scope / deferred

- **LLM-assisted extraction** (PromptingTools + a small local model reading the experimenter's notebook/spreadsheet to draft names/groupings). The deterministic backbone made this non-essential for v1; it remains a future enhancement that produces the same form, always human-reviewed.
- **Phase on the Samples screen** (removed by design — phase is not this screen's job and may not be computed yet).
- **Series treatment of slots** (each slot is one sample; series grouping is a separate, later user action).
- **Multi-setup auto-splitting** (we *detect and flag* geometry variance within a directory; we do not auto-split one directory into multiple experiments).

## 13. Open questions

- **Auto-schedule tier values (tuning).** The backoff *mechanism/state* is resolved (DB column, §9.4); the *values* still need a call: the fast-tier interval (1–6 h), the N empty ticks before dropping to daily, and the empty-daily count before stopping. Expose as a per-experiment setting or keep implicit?
- **Corpus vs. per-experiment Samples IA — RESOLVED (§7).** The flat-corpus `/samples` root + `?beamtime` chip are retired; the corpus becomes the experiment's **Corpus tab** (the real `SheetTable` reused, scoped), and grouping verification is the dedicated review surface reached from the banner. There is one corpus surface, scoped to the experiment.

**Resolved by the 2026-06-18 review sweep (now folded into the sections):** SSE channel + frame shape for `ingest_*` (ride `curation`, `payload.experiment_id`, `_try_put!` direct — §9.3); rescan reentrancy (per-experiment `ReentrantLock`) + backoff state (DB column — §9.4); switcher/topbar composition (`ExperimentShell` renders own chrome; `/`→`/experiments` — §7/§9.6); merge `series_samples`/`sample_tags` collisions (drop-loser dedups — §9.3); persistence bump 4→5 (§9.6); migration index/INSERT/NULL hazards (§10); merge undo = session-local (§9.3).

**Resolved from the real SSRL data (2026-06-18):** geometry authority — `flight_path_m` ← the setup file's AgBe-ring-calibrated `Mean distance`, PRP `Pipe length` as fallback (§6, **confirmed by Jonathan**); setup-file format — confirmed `analysis/setup_info_*.txt`, beam-center + mean-distance parse pinned (§6).

**Still open — need a decision before/while building:**
- **Directory-picker backend endpoints.** Path autocomplete + `validate-path` don't exist; confirm scope (server-side FS access + sandboxing). (§9.2)
- **Cross-sample filename uniqueness.** Verify real data has no legitimate cross-sample duplicate filenames before moving to `UNIQUE(experiment_id, filename)`. (§10)
- **`resolve_files` dispatch arg.** Loosen to a config-free form, or construct a minimal `ExperimentConfig` for `scan_directory` (`config.jl:202,237`). (§9.1)
- **Manifest legacy.** Recommend: drop `manifest.csv` entirely; migrate existing experiments by running `scan_and_group!` on first reingest (preserving `name_source='human'` edits). Confirm vs. keeping a read-only fallback. (§9.5)
- **Cross-load merge default per convention** (unchanged): "each load's slots are new samples; merge on matching label" — confirm across experimenter conventions.

## References

- Design system: `DESIGN.md`, `PRODUCT.md`.
- Today's ingestion contract: `docs/experiment-config.md`, `packages/HimalayaUI/src/{cli.jl,config.jl,manifest.jl,pipeline.jl}`.
- Backend rails the redesign reuses: `server.jl` (lifecycle, `_DB_WRITE_LOCK`, SSE, `start_gc_timer!`), `events.jl` (`apply_event!`, `update_view_for_event!`), `idempotency.jl` (`with_idempotency`), `pipeline.jl` (`analyze_exposure!`).
- Mockups + captures: `docs/redesign-mockups/*.html`.
- Memory: `project_ingestion_redesign` (decision log + data-derived parameters).
- Research sweep (2026-06-18): five-explorer workflow mapping API/CLI, pipeline, schema, PRP/geometry, and frontend — findings folded into §§4–11.
- Review sweep (2026-06-18): five-reviewer validation (himalaya/queue/saxs-physics/frontend reviewers + consistency) — corrections to the event architecture (§9.3), `analyze_exposure!` resolution (§9.1/§5), gap-relative grouping (§5), and the rename/scheduler/migration details folded in.
- Implementation-feasibility sweep (2026-06-18): 7-agent workflow (4 backend + 2 frontend explorers + synthesis) validating every backend change and inventorying the frontend against live code — produced the §8.6/§8.7 component maps, §9.6 wiring, the six backend corrections (§9.1/§9.3/§10/§11), and the new §13 open questions.
- Spec-review verdict (2026-06-18): 6-agent adversarial workflow (himalaya/queue/saxs-physics/frontend lenses + consistency + synthesis) → **ready-with-fixes**; 5 P0 + 14 P1 folded in (broadcast-progress shape §9.3, migration index/INSERT/NULL hazards §10, merge series/tag dedups §9.3, persistence 4→5 §9.6, reentrancy lock §9.4, label sweep §9.6, test strategy §11). Geometry authority + setup-file format were subsequently resolved from the real SSRL data (§6).
- Interactive prototype: `docs/redesign-mockups/ingestion-prototype.html` (the canonical UI reference).
